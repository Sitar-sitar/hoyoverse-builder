# 設計書：第15バッチ20名を API 境界の上書きから基底データへ正規化する

第15バッチだけが `server/batch15*.ts` の API 境界上書きとして実装されており、第1〜14・16・17 の正規パターン（基底データを直接更新）から外れている。上書き経路を撤去して基底データへ畳み込み、他バッチと同じ1系統でガイド・推奨PT・凸・履歴・メタデータを返す状態にする。

- **作成日**: 2026-09-10
- **対象アプリ**: hoyoverse-builder（HoYoverse Builder）
- **種別**: 修正設計書
- **対象バージョン**: 版番号は動かさない（内部データ構造の正規化。公開値の変更は §2.1 の範囲に限定）
- **前提コミット**: `0822df2`（main。Phase 20 リファクタリング第1期のマージ後）
- **ステータス**: 着手前（正本）
- **関連**:
  - [リファクタリング計画書_2026-09-08.md](リファクタリング計画書_2026-09-08.md) §4.3 #3（本設計書で回収する残課題）
  - [all-character-update-ledger.md](all-character-update-ledger.md)（「実装方式の変遷」＝第15バッチが例外である経緯）
  - [character-identity-foundation.md](character-identity-foundation.md)（`game:sourceId` 主キー方針・`variantOf` ガード）
  - [実装ログ.md](実装ログ.md)（本設計の実行は Phase 21 として記録する）
  - 親リポ `my-app2/docs/修正設計書_hoyoverseスキル再設計_2026-09-08.md`（運用スキル側の設計。リポジトリ横断のためリンクなし。本設計の §4.1 の根拠メモ作成はスキル側の担当）

---

## 1. 目的・背景

### 1.1 現状（前提コミット `0822df2` で実測）

第15バッチ20名（HSR 8 / 原神 8 / ZZZ 4）は、基底データを持たず**2箇所で API 境界上書き**されている。

| 経路 | 上書き箇所 | 使う関数 |
|---|---|---|
| UID 照会 | `server/routers.ts` の `applyBatch15CuratedOverrides(result)` | `server/batch15CuratedData.ts` |
| 図鑑（UID不要） | `server/characterReference.ts:76-81` の `batch15GuideFor` / `batch15PartyFor` / `batch15ConstellationFor` | `server/batch15{Guides,Parties,Constellations}.ts` |
| 更新履歴 | `server/routers.ts` の `applyBatch15History(guideUpdateHistory())` | `server/batch15History.ts` |

このため次の不整合が残っている（すべて前提コミットで実測）。

1. **メタデータ日付が基準日のまま**: `CHARACTER_GUIDE_METADATA` の20名は `dataAsOf` / `updatedAt` とも `2026-08-18`（基準日）で、応答上の `2026-09-05` は `batch15GuideFor` / `applyBatch15History` が上書きしている値。監査スクリプトは基底メタデータを見るため「精査済みなのに日付が基準日のまま」と警告する。
2. **推奨PTが基底の `generated-*` のまま**: `partyRecommendationsFor("hsr", "御空")` は `generated-hsr-御空-1` を返す（自動生成）。応答上の3案は `batch15PartyFor` が差し替えている。
3. **`communitySources` が空**: `batch15Parties.ts:45` は `communitySources: []`。`assertPartyCatalogIntegrity()`（`partyRecommendations.ts:1230`）の契約「各案に `communitySources` 1件以上」を満たさないため、そもそも基底カタログへ登録できない。
4. **凸の基底データが無い**: 20名とも `constellationProfileFor` は `preparing` を返し、`batch15ConstellationFor` が curated を供給している（20名の source ID はすべて一意に解決できる。§4.3 の表）。
5. **根拠メモが無い**: `docs/batch-15-research-notes.md` が存在しない（第1〜14・16・17 は全て存在する）。

### 1.2 なぜ直すか

- 上書き経路が残る限り、**新しい解決経路を足すたびに「第15バッチにも同じ上書きを足したか」を人が覚えておく必要がある**。実際に Phase 20 の R8（図鑑の凸索引統一）では、第15バッチ上書きが凸にも優先されるという例外を受入条件へ書き足す必要が生じた。
- 監査スクリプト（親リポ `hoyoverse-character-audit`）が基底メタデータを正としているため、20名分の誤検知が恒常的に出る。
- 第16バッチ以降は基底直接更新へ戻しており、第15バッチだけが例外として残っている。

---

## 2. スコープ

### 2.1 IN（公開応答の変更を許可する範囲）

本設計は**挙動不変ではない**。許可する変更は次の3点だけで、それ以外の値は前後で一致させる。

| # | 変わるもの | 変更前 | 変更後 | 理由 |
|---|---|---|---|---|
| V1 | 推奨PT各案の `communitySources` | `[]`（0件） | 1件以上 | 基底カタログの契約（`assertPartyCatalogIntegrity`）を満たすため。§4.1 で調達する |
| V2 | 推奨PT各案の `id` / `title` / `sourceLabel` / `synergy` / `targetSummary` / メンバーの `role` | `batch15-<game>-<name>-<rank>` ほか第15バッチ専用文言 | `curated-<game>-<name>-<rank>` ほか他200名と同じ `manualOptions` 由来の文言 | 他バッチと同じ登録経路へ寄せるため。編成メンバー（`members[].name`）と案の順序は変えない |
| V3 | `CHARACTER_GUIDE_METADATA` の20名の `dataAsOf` / `updatedAt` / `sourceLabel` | `2026-08-18` / 基準日の出典 | `2026-09-05` / 第15バッチの出典 | 応答上は既に `2026-09-05` を返しており、**基底を応答へ合わせる**変更。API 応答としては不変 |

- 上記以外（ガイド本文、目標値、凸効果、履歴イベントの文言、図鑑の一覧、`build.guideHistory` の構造）は**前後でバイト一致**させる。

### 2.2 OUT（変更しない）

- 画面仕様・UI・DB スキーマ・ビルド/デプロイ構成は変更しない。
- 第16・17バッチの登録方式（基底直接更新 / hook）は触らない。
- 第15バッチの**編成メンバー構成・凸効果の本文・ガイド本文**は再調査しない（今回は「置き場所を移す」変更であり、データの再精査ではない）。文言の誤りを見つけた場合も本設計では直さず、`docs/batch-15-research-notes.md` に記録して個別精査バッチへ送る。
- `server/fixtures/identityCatalogSnapshot.ts` の内容は変更しない（source ID の参照元として使う）。
- 推奨PTメンバー名の表記ゆれ（リファクタリング計画書 §4.3 #2）は本設計の対象外。第15バッチのメンバー名も現状のまま移す。

---

## 3. 全体方針

**「上書きを消す」ではなく「基底へ移してから上書きを消す」**。1コミットで消すと差分が読めなくなるため、データの移設（応答が変わらない）と経路の撤去（応答が変わらないことの確認）を分ける。

同名で複数 source ID を持つ `三月なのか` の扱いが本設計の中心的な設計判断になる。

### 3.1 `三月なのか` 問題

- `hsr:1001`（存護）と `hsr:1224`（巡狩）は、公開メタデータ上いずれも `providerName: "三月なのか"`（`identityCatalogSnapshot` で実測）。`CURATED_IDENTITIES` に 1224 の登録は無く、`variantOf` は `null` になる。
- 第15バッチのガイドは**存護（1001）専用**で、現状は `batch15CuratedData.ts:43` の `isSafeBatch15Identity` が `sourceId === "1001"` を確認して適用を絞っている。
- したがって、ガイドを名前キーの `GUIDE_OVERRIDES["三月なのか"]` へそのまま移すと、**1224（巡狩）の UID 照会にも存護のビルドが適用される回帰**になる（現状 1224 は `generatedHsrGuide` の crit ビルドを返す）。

**採用する方式**: HSR の個別ガイド解決に **source ID キーの索引を1段追加**し、`三月なのか` の存護ビルドだけをそこへ登録する。

```ts
// server/buildAdvisor.ts
/** 同名で複数の実装がある場合だけ使う source ID キーの個別ガイド。名前キーより優先する。 */
const GUIDE_OVERRIDES_BY_SOURCE_ID: Record<string, GuideDefinition> = {
  "1001": /* 三月なのか（存護）の第15バッチ確定ガイド */,
};

export function guideFor(name: string, path: string, identity?: Pick<CharacterIdentity, "variantOf" | "sourceId">): GuideDefinition {
  const byId = identity?.sourceId ? GUIDE_OVERRIDES_BY_SOURCE_ID[identity.sourceId] : undefined;
  const individualGuide = byId ?? (identity?.variantOf ? undefined : GUIDE_OVERRIDES[name]);
  // 以降は既存のまま
}
```

- 図鑑側（`characterReference.ts` の `baseGuideFor`）は、R8 で導入した名前→source ID 索引を使って identity を解決してから `guideFor` に渡す。これで図鑑の `三月なのか` は従来どおり存護のビルドを表示する。
- **退けた代替案**: 名前キーのまま `guideFor` 内で `sourceId === "1224"` を除外するガードを書く方式。1行で済むが、名前キーの表に ID の例外条件が埋まり、次に同名 ID が増えたときに同じ分岐が増える。[character-identity-foundation.md](character-identity-foundation.md) の ID 主キー方針とも合わないため採らない。
- 凸は既に `game:sourceId` キー（`CURATED["hsr:1001"]`）のため、この問題は起きない。推奨PT・メタデータはカタログ名が主キーで、カタログには `三月なのか` が1件しか無いため、存護として登録すれば足りる（1224 は図鑑に出ない）。

---

## 4. 詳細仕様

### 4.1 前提作業（コード変更の前に完了させる）

**この作業が終わるまで §4.2 以降へ着手しない。**

1. `docs/batch-15-research-notes.md` を作成する。第16バッチの [batch-16-research-notes.md](batch-16-research-notes.md) と同じ構成で、20名それぞれのビルド・凸・推奨PTの出典 URL と確認日を記録する。既存の `batch15*.ts` に埋まっている `sourceUrl`（`batch15Parties.ts` の `SPECS[].sourceUrl` と `batch15Constellations.ts` の `SPECS[].sourceUrl`）が起点になる。
2. 各キャラクターの推奨PTに付ける `communitySources` を1件以上確定する（V1）。他バッチと同じ扱いにするため、`communitySourcesFor(game)` の共通出典（`partyRecommendations.ts:74-78`）をそのまま使う。**個別の出典を新たに探す必要はない**が、`checkedAt` は第15バッチの確認日 `2026-09-05` ではなく、この作業で実際に照合した日付にする。
3. 作業担当: 調査と根拠メモの作成は運用スキル `hoyoverse-character-curation` の担当。コード変更（§4.2 以降）は本設計書の担当。

### 4.2 ガイドの移設

| ゲーム | 移設先 | 対象 |
|---|---|---|
| HSR | `server/buildAdvisor.ts` の `GUIDE_OVERRIDES`（7名）と `GUIDE_OVERRIDES_BY_SOURCE_ID`（`三月なのか` のみ・§3.1） | 御空 / 長夜月 / 停雲 / 白露 / 緋英 / 彦卿 / 姫子 ＋ 三月なのか |
| 原神 | `server/gameProviders.ts` の `GI_GUIDE_OVERRIDES` | ディルック / ドゥリン / トーマ / ドリー / ナヴィア / ニィロウ / ネフェル / ノエル |
| ZZZ | `server/gameProviders.ts` の `ZZZ_CHARACTER_GUIDES` | 猫又 / 盤岳 / 葉瞬光 / 潘引壺 |

- `batch15Guides.ts` の `PATCHES` は**部分パッチ**（`headline` / `relicSet` / `planarSet` / `mainStats` / `targetContext` / `sourceLabel` / `profileId`、一部は `targets`）で、`batch15GuideFor` が基底ガイドへマージして完成形を作っている。移設先には**マージ後の完成形**を書く。
- マージ後の完成形は、実装者が手で組み立てず、前提コミットの実測値から生成する。`corepack pnpm exec tsx -e` で `batch15GuideFor(game, name, baseGuide)` の戻り値を JSON で出力し、それを TypeScript のリテラルへ書き写す。`targets` は `mergeTargets` の結果（基底の目標値＋パッチ）であることに注意する。
- `dataAsOf` / `updatedAt` は各エントリに `"2026-09-05"` を明示する（`batch15GuideFor` が付けていた値と同じ）。`sourceLabel` / `profileId` もパッチの値をそのまま持たせる。
- 姫子は既に `GUIDE_OVERRIDES` にエントリがあるため、**新規追加ではなく置換**になる。

### 4.3 凸の移設

`server/batch15Constellations.ts` の `SPECS` 20件を、`server/characterConstellations.ts` の `CURATED` へ **`game:sourceId` キー**で移す。source ID は前提コミットで実測済み（`identityCatalogSnapshot` 由来、`三月なのか` は存護の 1001）。

| game:name | source ID | | game:name | source ID |
|---|---|---|---|---|
| hsr:御空 | 1207 | | genshin:ディルック | 10000016 |
| hsr:三月なのか | 1001 | | genshin:ドゥリン | 10000123 |
| hsr:長夜月 | 1413 | | genshin:トーマ | 10000050 |
| hsr:停雲 | 1202 | | genshin:ドリー | 10000068 |
| hsr:白露 | 1211 | | genshin:ナヴィア | 10000091 |
| hsr:緋英 | 1505 | | genshin:ニィロウ | 10000070 |
| hsr:彦卿 | 1209 | | genshin:ネフェル | 10000122 |
| hsr:姫子 | 1003 | | genshin:ノエル | 10000034 |
| zzz:猫又 | 1021 | | zzz:葉瞬光 | 1431 |
| zzz:盤岳 | 1471 | | zzz:潘引壺 | 1421 |

- 20名とも移設先に既存エントリは無い（前提コミットで `constellationProfileFor` が全員 `preparing` であることを実測済み）。
- 効果本文・`caution` の文言・`sourceUrl` は `batch15Constellations.ts` の値をそのまま移す。R8 で図鑑とUID照会は同じ索引を使うため、ID キーへ入れれば両経路に届く。

### 4.4 推奨PTの移設

`server/batch15Parties.ts` の `SPECS` 20件を、`server/partyRecommendations.ts` の `MANUALLY_CURATED_HIGH_USAGE_CATALOG` へ登録する。

- 登録は他バッチと同じ `datedOptions(date, resetTargetChanges)` を使う。第15バッチ用に `const batch15Options = datedOptions("2026-09-05", true);` を追加する（第12〜14・16と同じく `targetChanges: []` を維持するため `resetTargetChanges = true`）。
- 各キャラクターの3案は `plan(members, ja, en, zh)` で登録する。`members` は `batch15Parties.ts` の `teams` の順序・表記をそのまま使う。`synergy` は現行の第15バッチ共通文（`batch15Parties.ts:41`）を各案へ書き写す。
- `sourceUrl` は `SPECS[].sourceUrl` をそのまま渡す。
- **この移設で V1・V2 の応答変更が発生する**（`id` が `batch15-*` → `curated-*`、`communitySources` が 0件 → 1件、`title` / `role` / `sourceLabel` / `targetSummary` が `manualOptions` の共通文言へ）。members と案の順序は変わらない。

### 4.5 メタデータ日付の移設

`server/characterGuideMetadata.ts` に第15バッチの適用ブロックを追加する（第16バッチのブロックと同じ形）。

```ts
Object.entries(batchNames(15)).forEach(([game, names]) => {
  const gameId = game as CatalogGameId;
  names.forEach((name) => {
    const record = CHARACTER_GUIDE_METADATA[gameId][name];
    if (!record) return;
    record.dataAsOf = "2026-09-05";
    record.updatedAt = "2026-09-05";
    record.sourceLabel = /* batch15History.ts の sourceLabel(game) と同じ3分岐 */;
  });
});
```

- 名前リストは R6 で一元化した `batchNames(15)` を使う（`batch15Guides.ts` の `BATCH15_CHARACTERS` も同じ表を参照している）。
- ブロックの位置は第14バッチの後・第16バッチの前に置き、既存の適用順（後のバッチが勝つ）を崩さない。

### 4.6 履歴イベントの移設

`server/guideUpdateHistory.ts` に第15バッチを登録する。

1. `BATCH_EVENT_TEMPLATES` へ `15:` のエントリを追加する。`date` / `title` / `summary` / `changes` / `rationale` は `batch15History.ts:10-17` の `eventFor` からそのまま移す（`summary` は `(name) => ...` 形式）。
2. サイト全体イベント（`batch15History.ts:20-24` の `batchEvent`）を `SITE_EVENTS` へ追加する。**配列内の位置は日付順で第16バッチのイベントの前**に置く（`applyBatch15History` は先頭に差していたが、`SITE_EVENTS` の既存並びは日付昇順のため、そこへ合わせると `build.guideHistory` の `siteEvents` の並びが変わる）。→ **§10 の要確認事項 Q1**。
3. `characters[].dataAsOf` / `updatedAt` / `sourceLabel` の上書きは §4.5 のメタデータ移設で自動的に満たされる（履歴はメタデータから組み立てるため）。

### 4.7 上書き経路の撤去

§4.2〜4.6 が完了し、応答が一致することを確認してから撤去する。

- `server/routers.ts`: `applyBatch15CuratedOverrides` / `applyBatch15History` の呼び出しと import を削除する。
- `server/characterReference.ts`: `batch15GuideFor` / `batch15PartyFor` / `batch15ConstellationFor` の呼び出しと import を削除し、`baseGuideFor` / `partyRecommendationsFor` / `constellationProfileForCatalogName` の結果をそのまま返す。
- 削除するファイル: `server/batch15Guides.ts` / `batch15Parties.ts` / `batch15Constellations.ts` / `batch15CuratedData.ts` / `batch15History.ts` / `batch15CuratedData.test.ts`。
- **`BATCH15_CHARACTERS` / `BATCH15_DATE` / `batch15ConstellationFor` / `batch15PartyFor` は親リポの監査スクリプトが import している公開シンボル**（リファクタリング計画書 §1.3 #3）。削除する場合は、親リポ `修正設計書_hoyoverseスキル再設計_2026-09-08.md` 側の参照を先に外すか、同時に修正する。→ **§10 の要確認事項 Q2**。

---

## 5. 既存機能の非破壊（チェックリスト）

- [ ] 図鑑（`build.reference`）の20名の `guide` が前後で一致する（`headline` / `relicSet` / `planarSet` / `mainStats` / `targets` / `targetContext` / `dataAsOf` / `updatedAt` / `sourceLabel` / `profileId`）。
- [ ] 図鑑の20名の `constellations` が前後で一致する（`dataStatus: "curated"`、効果6段階、本文）。
- [ ] 図鑑の20名の `partyRecommendations` は、`members` と案の順序・件数が前後で一致する（`id` / `title` / `sourceLabel` / `communitySources` は §2.1 V1・V2 の範囲で変わる）。
- [ ] **`hsr:1224`（三月なのか・巡狩）の UID 照会が、存護のビルド・凸を受け取らない**（現状と同じ `generatedHsrGuide` の結果、凸は `preparing`）。
- [ ] 開拓者（`hsr:8001`〜`8010`）・銀狼Lv.999（`hsr:1506`）など `variantOf` を持つ派生実装に、基礎キャラクターの個別ガイドが適用されない。
- [ ] 第15バッチ以外の228名の図鑑応答・推奨PT・凸・履歴・メタデータが前後でバイト一致する。
- [ ] `build.guideHistory` の20名の `events` に第15バッチのイベントが1件だけ含まれる（重複しない）。
- [ ] `assertPartyCatalogIntegrity()` が `true` を返す（20名を追加した後も）。
- [ ] `PARTY_CATALOG_CHARACTER_COUNT` を参照している既存テストの期待値を、20名増加に合わせて更新する。

---

## 6. 変更ファイルと差分の粒度

1項目 = 1コミット。コミットメッセージは `refactor: B15-<番号> <要約>`。

| # | 変更 | ファイル | 応答変更 |
|---|---|---|---|
| B15-1 | 根拠メモ作成（§4.1） | `docs/batch-15-research-notes.md`（新規） | なし |
| B15-2 | source ID キーのガイド索引を追加（§3.1） | `server/buildAdvisor.ts`、`server/characterReference.ts` | なし（索引は空のまま追加し、既存経路が変わらないことを確認する） |
| B15-3 | ガイド20名を基底へ移設（§4.2） | `server/buildAdvisor.ts`、`server/gameProviders.ts` | なし |
| B15-4 | 凸20名を `CURATED` へ移設（§4.3） | `server/characterConstellations.ts` | なし |
| B15-5 | 推奨PT20名を基底カタログへ移設（§4.4） | `server/partyRecommendations.ts` | **V1・V2** |
| B15-6 | メタデータ日付・履歴イベントを移設（§4.5・§4.6） | `server/characterGuideMetadata.ts`、`server/guideUpdateHistory.ts` | **V3**（応答としては不変） |
| B15-7 | 上書き経路とファイルを撤去（§4.7） | `server/routers.ts`、`server/characterReference.ts`、`server/batch15*.ts` 削除 | なし |

- B15-3〜B15-6 は移設先に値を置く段階で、この時点では**上書きが優先されるため応答は変わらない**（B15-5 の V1・V2 も B15-7 の撤去まで表に出ない）。応答が実際に切り替わるのは B15-7。
- したがって **B15-7 の前後で応答差分を採り、§2.1 の3点だけであることを確認する**のが本設計の中心的な受け入れ判定になる。

---

## 7. リスクと割り切り

- **最大のリスクは `三月なのか` の取り違え**。1224 に存護のデータが漏れると、UID 照会で別実装のビルドを表示する。§5 のチェックリストと §8 の専用テストで固定する。
- 移設は「手で書き写す」作業のため転記ミスが起きうる。ゴールデン比較（`scripts/golden-snapshot.mjs`）で全248名の応答を突き合わせ、差分が §2.1 の範囲に収まることを機械的に確認する。
- 親リポの監査スクリプトが `batch15*` の公開シンボルを import しているため、**本設計の B15-7 は親リポ側の修正と同時に出す必要がある**（Q2）。片方だけ先行させると監査スクリプトが壊れる。
- ロールバック: 各項目は `git revert` で戻せる。B15-7 まで進んだ後に問題が出た場合も、`batch15*.ts` の復活と呼び出し2箇所の復元で元の経路へ戻せる。

---

## 8. テスト・検証

### 8.1 各コミット前に実行する共通検証

リファクタリング計画書 §2 の共通検証ブロックをそのまま使う（`check` / `test` / `build:api` / Pages ビルド / `golden compare`）。B15-5・B15-7 は差分が出るため、下記の手順で確認してから baseline を更新する。

### 8.2 移設の同値確認（B15-3〜B15-6）

- ゴールデン比較が全11セクション一致すること。上書きが優先される段階のため、移設先へ値を置いても応答は変わらない。
- 加えて、移設先の値が上書きの値と一致することを直接確認する: 20名について `batch15GuideFor(game, name, baseGuide)` と移設後の `guideFor` / `genshinGuide` / `zzzGuide` の戻り値が `toEqual` で一致するテストを一時的に追加する（B15-7 で `batch15*.ts` を削除する際にこのテストも削除する）。

### 8.3 撤去後の受け入れ判定（B15-7）

1. `check-r8` が `catalog-id mismatch 0 / not curated 5` のままであること（凸の解決経路が変わっていない）。
2. ゴールデン比較の差分が **`parties` と `referenceCore` の20件のみ**であること。他9セクション（`ledger` / `history` / `referenceCatalog` / `referenceConstellations` / `idConstellations` / `catalogConstellations` / `metadata` / `expectedProfiles` / `guides`）は一致すること。
   - `metadata` は B15-6 で先に差分が出るため、その時点で §2.1 V3 の20名のみであることを確認して baseline を更新しておく。
   - `guides` は B15-3 で差分が出る（`generated*` → 個別ガイド）。20名のみであることを確認して baseline を更新する。
3. 差分20件の内容が §2.1 の V1・V2 に限られることを確認する。`members[].name` と案の順序・件数、`targetChanges` が前後で一致することを機械的に照合する。
4. 新規テスト（恒久）:
   - `server/characterReference.test.ts`: 第15バッチの代表3名（`hsr:御空` / `genshin:ノエル` / `zzz:猫又`）が `guide.dataAsOf === "2026-09-05"`、`constellations.dataStatus === "curated"` かつ効果6件、`partyRecommendations.options` が3案で各案 `communitySources.length > 0`。
   - `server/buildAdvisor.test.ts`: `guideFor("三月なのか", "巡狩", { sourceId: "1224", variantOf: null })` が存護の個別ガイド（`profileId: "curated:batch15:march-preservation"`）**ではない**こと。`sourceId: "1001"` では存護の個別ガイドを返すこと。
   - `server/partyRecommendations.test.ts`: `assertPartyCatalogIntegrity()` が `true`、`PARTY_CATALOG_CHARACTER_COUNT` が20件増えていること。
5. ローカル API（`node dist/index.js`、`API_ONLY=true`、port 3042）で `build.reference` の `hsr:御空` が `"dataStatus":"curated"` と `2026-09-05` を返すこと、`build.guideHistory` が `"result"` を返すこと。
6. 公開後: Railway 反映を待ち、本番 API で同じ3名を確認する。Google Chrome で図鑑の御空・ノエル・猫又の表示が変更前と同じであることを目視確認する（PT の出典表記だけが変わる）。

---

## 9. 受け入れ条件

- [ ] `docs/batch-15-research-notes.md` が存在し、20名の出典と確認日を記録している。
- [ ] `server/batch15*.ts`（5ファイル）と `batch15CuratedData.test.ts` がリポジトリから消え、`routers.ts` / `characterReference.ts` に第15バッチ専用の分岐が無い。
- [ ] ゴールデン比較の差分が §2.1 の V1〜V3 に限られ、それ以外の228名・他セクションはバイト一致。
- [ ] `hsr:1224` に存護のビルド・凸が適用されないことをテストで固定している。
- [ ] `assertPartyCatalogIntegrity()` が `true`。全テスト・型検査・ビルドが green。
- [ ] `docs/実装ログ.md` の Phase 21 を締めチェックリストでクローズし、本設計書と設計書インデックスを「実装済み（反映コミット列挙）」へ更新している。
- [ ] リファクタリング計画書 §4.3 #3 が解消済みであることを、同計画書の変更履歴か本設計書の変更履歴で追える。

---

## 10. 未確定・要確認事項

- **Q1: サイト全体イベントの並び**。`applyBatch15History` は第15バッチのサイトイベントを `siteEvents` の**先頭**に差している。基底の `SITE_EVENTS` は日付昇順で並んでおり、日付順に入れると `build.guideHistory` の `siteEvents` の並びが変わる（＝§2.1 に無い応答変更が増える）。「並びを日付昇順へ統一してよいか」「現状の先頭配置を再現すべきか」を確認する。**回答が出るまで B15-6 に着手しない。**
- **Q2: 親リポ監査スクリプトとの同時修正**。`BATCH15_CHARACTERS` / `BATCH15_DATE` / `batch15ConstellationFor` / `batch15PartyFor` を import している親リポ `hoyoverse-character-audit/scripts/audit-characters.mjs` を、本設計の B15-7 と同時に修正するか、先に `characterBatches.ts` 参照へ切り替えるかを決める。
- **Q3: 実施時期**。親リポ `修正設計書_hoyoverseスキル再設計_2026-09-08.md` は第15バッチ正規化を「スキル側の初回運用タスク」として想定している。本設計書（リポジトリ内のコード変更）とスキル再設計のどちらを先に実施するかを確認する。

---

## 変更履歴

| 日付 | 内容 |
|---|---|
| 2026-09-10 | 新規作成（前提コミット `0822df2`）。リファクタリング計画書 §4.3 #3 の残課題を回収する設計として起票。上書き経路・メタデータ日付・`communitySources` 空・`三月なのか` の同名ID・根拠メモ欠落を前提コミットで実測して記載。 |
