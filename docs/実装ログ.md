# 実装ログ（hoyoverse-builder）

`D:\my-app2\docs\実装フェーズ記録ルール.md` に従い、実装の過程をフェーズ単位で記録する。
**次フェーズ着手前に現フェーズを「締めチェックリスト」で必ずクローズする。**

- 対象アプリ: HoYoverse Builder（`Sitar-sitar/hoyoverse-builder`、公開 https://sitar-sitar.github.io/hoyoverse-builder/ ）
- 現在フェーズ: 第16バッチ キャラクター個別精査
- 現在ステータス: 進行中（ローカル検証済み・公開未実施）

> 本ファイルは 2026-09-07（第16バッチ着手時）に新規作成した。第1〜15バッチは本ログの運用開始前の作業であり、
> 経緯は [`docs/all-character-update-ledger.md`](all-character-update-ledger.md) と `docs/batch-N-research-notes.md`、
> コミット履歴を参照する。ここへ遡って再構成はしない。

---

## 現在進行中フェーズ

### Phase 16: 第16バッチ キャラクター個別精査（HSR 4名・原神 16名）

- 目的 / スコープ: 台帳の未精査20名について、更新日付きの公開ガイドから個別ビルド・全6段階の凸・実名推奨PTを精査し、
  UID照会と図鑑（UID不要）の双方へ同じ内容を反映する。
- 対象バージョン: 採番なし（データ更新フェーズ）
- 関連文書: [batch-16-research-notes.md](batch-16-research-notes.md) / [all-character-update-ledger.md](all-character-update-ledger.md)
- 着手日: 2026-09-07
- ステータス: 進行中

**作業メモ**

- 2026-09-07: 分割後リポジトリ `D:\my-app2\hoyoverse-builder` に `node_modules` が無かったため `corepack pnpm install --frozen-lockfile` を実施。
- 2026-09-07: 作業ブランチ `feat/batch16-character-curation` を `origin/main`（`86eec98`）から作成。
  `chore/repository-split`（`5f94b91`、Pages base 変更などの設定移行）は未マージのため、本バッチはそれに依存しない形で main 基準にした。
- 2026-09-07: 調査は Game8日本語版を主出典、GameWith・崩壊スターレイルWikiを補助として本文取得。定型的なガイド収集は Sonnet、
  単純な星魂表の穴埋めは Haiku、実装・検証・台帳は Opus と、作業ごとにモデルを分けた。
- 2026-09-07: 第15バッチはAPI境界と図鑑で二重に上書きする構成だったが、本バッチは基底ファイル直接更新へ戻した。
  その過程で、図鑑経路がカタログ名を source ID として `resolveCharacterIdentity()` へ渡しており、ID キーの凸データへ到達しない
  既存の欠落を検出。`constellationProfileForCatalogName()` を追加して名前→確認済みID解決を挟んだ（お試し実装の別IDは
  カタログ名を持たないため、名前一致による誤適用は起きない）。
- 2026-09-07: 数値目標は出典の明示値のみ登録。明示が1点しかない項目は水準差を作らず同値、複数出典で異なる場合のみ
  厳選/妥協へ割り当てた。数値の明示がない10名は `targets: []` とし優先度のみ記録した。

**検証（2026-09-07 時点）**

- `node node_modules/typescript/bin/tsc --noEmit`: pass（エラー0）
- `node node_modules/vitest/vitest.mjs run`: pass（33ファイル / 174テスト。うち新規 `server/batch16CuratedGuides.test.ts` 7テスト）
- `corepack pnpm build:api`: pass（`dist/index.js` 1.6MB）
- `corepack pnpm exec vite build --config vite.pages.config.ts`: pass
- 実UIDでの公開画面確認: 未実施（今回のセッションで許可された公開UIDがないため）
- 公開（`main` への push / Pages 反映）: 未実施（ユーザー承認待ち）

**反映コミット（進行中）**

- `309f9e3` feat: 第16バッチ20名のキャラクター情報を更新
- `950bf16` fix: 図鑑（UID不要）でも個別ビルドと凸をUID照会と同じ経路で解決する

**実画面確認（ローカル `http://localhost:3111/characters`、Google Chrome、2026-09-07）**

- 不死途: 個別ビルド・目標値・出典ラベル・星魂6段・推奨PT2案を確認。
- ラウマ: 元素熟知800／元素チャージ効率170%の3水準表示、目標値の前提文、推奨PT3案、命ノ星座を確認。
- リネ: 当初は自動生成ガイドが表示され、上記 `950bf16` の修正後に精査済みビルドが表示されることを確認。

**残作業**

- ローカルビルドでの図鑑（UID不要）実画面確認。
- ユーザー承認後の push・Pages 公開と、公開後の API（Railway）／公開画面の照合。
- Obsidian 側ノートの更新（未実施）。

---

## 完了フェーズ（締め済み）

（本ログ運用開始後の完了フェーズはまだない）
