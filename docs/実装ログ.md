# 実装ログ（hoyoverse-builder）

`D:\my-app2\docs\実装フェーズ記録ルール.md` に従い、実装の過程をフェーズ単位で記録する。
**次フェーズ着手前に現フェーズを「締めチェックリスト」で必ずクローズする。**

- 対象アプリ: HoYoverse Builder（`Sitar-sitar/hoyoverse-builder`、公開 https://sitar-sitar.github.io/hoyoverse-builder/ ）
- 現在フェーズ: なし（Phase 47 完了。公開API保護と外部API耐障害性バッチ Phase 42〜47 のローカル実装が完了。本番・実機確認は未実施）
- 現在ステータス: 完了

> 本ファイルは 2026-09-07（第16バッチ着手時）に新規作成した。第1〜15バッチは本ログの運用開始前の作業であり、
> 経緯は [`docs/all-character-update-ledger.md`](all-character-update-ledger.md) と `docs/batch-N-research-notes.md`、
> コミット履歴を参照する。ここへ遡って再構成はしない。

---

## 現在進行中フェーズ

（なし）

---

## 完了フェーズ（締め済み）

### Phase 47: UID 応答キャッシュの件数上限・LRU 化 — 完了

- 目的 / スコープ: `UidResponseCache` は `CACHE_SWEEP_THRESHOLD = 256` を超えても期限切れしか消さないため、全件有効なら無制限に増える。1キャッシュ 256 件（HSR / 原神 / ZZZ 合計 768 件）の上限と LRU による追い出しを入れる。TTL と in-flight 制御は変えない。
- 対象バージョン: 採番なし（API の運用改善）
- 関連設計書: [修正設計書_公開API保護と外部API耐障害性_2026-09-19.md](修正設計書_公開API保護と外部API耐障害性_2026-09-19.md) §4 Phase 47・§10-1・§11.1 A-05
- 完了日: 2026-09-19
- 着手日: 2026-09-19

**作業メモ（随時追記）**
- 2026-09-19: 実装（`8760e2b`）。`server/buildAdvisor.ts` の `UidResponseCache` に `CACHE_MAX_ENTRIES = 256` と `evict()` を追加。`evict` は「期限切れを全掃除 → なお超える分は `Map.keys()` の先頭（最後に使われたのが最も古い）から削除」。`set` は `Map.set` が既存キーの更新で挿入順を変えないため `delete` → `set` に変更し、`getEntry` も有効エントリを読んだら末尾へ動かす。`size` getter と期限切れ削除の副作用は維持。
- 2026-09-19: 既存の UID キャッシュテスト4件は変更せずに pass することを先に確認した（TTL 境界、閾値超過時の期限切れ掃除、閾値以下で保持、`expiresAt <= now` の境界）。LRU の追加で挿入順が変わるが、いずれも最終的な `size` と値の有無しか見ていないため影響しない。
- 2026-09-19: 回帰テスト5件を `server/buildAdvisor.test.ts` の「UIDキャッシュ」へ追加（全件有効でも 256 を超えない / 257件目で最古1件だけ追い出す / `get` したエントリが追い出されない / 既存キーの `set` も recency を更新する / 期限切れを LRU より先に捨てる）。
- 2026-09-19: 変異チェックを2つ実施。(1) LRU の追い出しループを外す → 4件 fail。(2) `getEntry` の recency 更新を外す → 1件 fail。いずれも編集で元に戻した。

---
- 反映コミット: `8760e2b`（実装・テスト）／実装ログの締めは本コミット
- 検証結果: すべて pass。変異チェック2種（追い出し除去で 4件 fail、recency 更新除去で 1件 fail → 復元後 pass）/ `vitest run` 63ファイル・443テスト pass（Phase 46 の 63/438 から +5テスト）/ `tsc --noEmit` pass / `build:api` pass / Pages ビルド pass（entry 448.96kB・gzip 140.40kB、変化なし）/ ゴールデン比較 `compare` 全11セクション一致。**本番・実機確認は未実施**。
- 残課題・既知の問題: 256 件/キャッシュは保守的な初期値。設計 §10-1 のとおり、引き上げる場合は1エントリの実メモリ実測とヒット率を根拠に別変更とする。
- 次フェーズへの申し送り: 本バッチ（Phase 42〜47）のローカル実装は完了。残るのは (1) PR 作成とマージ、(2) 本番（Railway 反映後）での確認、(3) 設計書とインデックスの「実装済み」化。**設計 §9 の受け入れ条件のうち、`X-Real-IP` による複数クライアントの識別（Phase 43）と production の replica 数が 1 であることは本番でしか確認できない**。複数クライアントを識別できない場合は Phase 43 を完了扱いにせず、設計 §9 に従って対応する。Pages の `/admin/display` 直接アクセスと CI のルート検査も、GitHub Actions 上での実行が未確認。
- 関連ドキュメント: [修正設計書_公開API保護と外部API耐障害性_2026-09-19.md](修正設計書_公開API保護と外部API耐障害性_2026-09-19.md) §4 Phase 47・§10-1・§11.1 A-05 / [設計レビュー_公開API保護と外部API耐障害性_2026-09-19.md](設計レビュー_公開API保護と外部API耐障害性_2026-09-19.md) R-11・R-12


### Phase 46: Analytics の応答分離と `/admin/display` の Pages 対応 — 完了

- 目的 / スコープ: Analytics の DB 書込みを `await` しているため、照会自体は成功していても DB が遅いと応答が遅れる。ベストエフォートの記録を応答から切り離す。あわせて `deploy-pages.yml` のルート列挙に `admin/display` を追加し、ルートの取りこぼしを CI で検出する。
- 対象バージョン: 採番なし（API の運用改善）
- 関連設計書: [修正設計書_公開API保護と外部API耐障害性_2026-09-19.md](修正設計書_公開API保護と外部API耐障害性_2026-09-19.md) §4 Phase 46
- 完了日: 2026-09-19
- 着手日: 2026-09-19

**作業メモ（随時追記）**
- 2026-09-19: 実装（`129d71e`）。`server/routers.ts` の `build.lookup` で Analytics を `void recordLookupAnalyticsEvent(...).catch(...)` に変更。timeout は付けない（設計どおり。待たない以上は無意味で、DB 側の接続・トランザクションも解放されない）。引数は `(game, cached)` のままで UID を渡さない。
- 2026-09-19: `.github/workflows/deploy-pages.yml` のルート列挙へ `admin/display` を追加。あわせて `Prepare Pages artifact` の直後に検査ステップを追加し、`client/src/App.tsx` の `Route path` 一覧（`/` と `/404` と catch-all を除く）に対応するディレクトリが生成済みかを確認して、無ければ失敗させる。列挙の更新忘れという再発構造そのものを塞ぐ。
- 2026-09-19: 検査ステップの抽出コマンドをローカルで実行して確認した。`App.tsx` から8ルート（`/` と `/404` を含む）を正しく取得し、`admin/display` がある状態では `missing=0`、意図的にそのディレクトリを消すと `missing: /admin/display` で失敗する。作成過程で `sed` の後方参照 `` が壊れていたのをこの実行で検出して直した。
- 2026-09-19: 回帰テスト `server/lookupAnalyticsAsync.test.ts`（4件）。記録の成功・失敗・**解決も棄却もしない Promise**（`await` していたら止まる）のいずれでも照会結果が即座に返ること、`unhandledRejection` を出さないこと、UID を渡していないことを確認する。
- 2026-09-19: 変異チェック（`void ... .catch()` を `await` に戻す）で 4件中3件が fail することを確認（無応答のケースは 5015ms でタイムアウト）。編集で元に戻した。

---
- 反映コミット: `129d71e`（実装・テスト・ワークフロー）／実装ログの締めは本コミット
- 検証結果: すべて pass。変異チェック（`await` へ戻して 3件 fail → 復元後 pass）/ `vitest run` 63ファイル・438テスト pass（Phase 45 の 62/434 から +1ファイル +4テスト）/ `tsc --noEmit` pass / `build:api` pass / Pages ビルド pass（entry 448.96kB・gzip 140.40kB、変化なし）/ ゴールデン比較 `compare` 全11セクション一致。Pages の検査ステップはローカルで正常系・異常系の両方を実行して確認。**GitHub Actions 上での実行と本番・実機確認は未実施**。
- 残課題・既知の問題: Analytics はプロセス終了時に取りこぼす（設計 §7 で許容済み。課金・監査用途ではない）。
- 次フェーズへの申し送り: Phase 47 の `UidResponseCache` は3インスタンス（`lookupCache` / `genshinCache` / `zzzCache`）で共有されるクラス。`Map.set` は既存キーの更新では挿入順を変えないため、LRU にするには `set` でも `delete` → `set` が要る（R-12）。`getEntry` の期限切れ削除の副作用と `size` の既存テストを壊さないこと。
- 関連ドキュメント: [修正設計書_公開API保護と外部API耐障害性_2026-09-19.md](修正設計書_公開API保護と外部API耐障害性_2026-09-19.md) §4 Phase 46 / [設計レビュー_公開API保護と外部API耐障害性_2026-09-19.md](設計レビュー_公開API保護と外部API耐障害性_2026-09-19.md) R-16・R-17


### Phase 45: Enka フォールバックのエラー分類と `safeTrpcFetch` — 完了

- 目的 / スコープ: `fetchEnkaPayload` が静的カタログ取得の後に HTTP status を見るため、429 / 500 と分かっているのに最大5秒待つ。404 以外も「公開中のキャラクターが見つかりません」になる。あわせて `safeTrpcFetch` の擬似 tRPC レスポンス生成（常に1件・`path` が `build.lookup` 固定）を廃止し、バッチで破綻しない形にする。
- 対象バージョン: 採番なし（API の運用改善）
- 関連設計書: [修正設計書_公開API保護と外部API耐障害性_2026-09-19.md](修正設計書_公開API保護と外部API耐障害性_2026-09-19.md) §4 Phase 45・§10-7・§11.1 A-04
- 完了日: 2026-09-19
- 着手日: 2026-09-19

**作業メモ（随時追記）**
- 2026-09-19: 実装（`d24f94d`）。`server/enkaFallback.ts` の `fetchEnkaPayload` を「fetch → status 判定 → 本文 JSON 化 → `getStaticIndex()` → normalize → 0件判定」の順へ。分類は `enkaStatusError()` に集約し、404=`NOT_FOUND` / 429=`TOO_MANY_REQUESTS` / 5xx=`BAD_GATEWAY`（外部障害の文言）/ その他の非2xx=`BAD_GATEWAY`（正常な応答を取得できない文言）。設計 A-04 のとおり Content-Type は必須条件にせず、200 の本文が JSON なら従来どおり処理する。R-14 の0件判定は normalize の後に維持。
- 2026-09-19: `client/src/lib/safeTrpcFetch.ts` の `trpcErrorResponse`（擬似 tRPC エンベロープ）を廃止し、非 JSON 応答では `Error` を throw する形へ。`httpBatchLink` が fetch の reject を `TRPCClientError.from(cause)` で包み、バッチ内の全 operation へ同じエラーを配るため、件数不一致も `path` の `build.lookup` 固定も起きなくなる。メッセージ文言は現行のまま流用（表示の変更なし）。
- 2026-09-19: 回帰テスト。`server/enkaErrorClassification.test.ts`（10件）は global fetch を差し替え、**静的データ側の URL が呼ばれた回数**を数えて「失敗応答で静的カタログを取りに行かない」ことを直接確認する。`client/src/lib/safeTrpcFetch.test.ts`（9件・全面改訂）は実際の `httpBatchLink` と組み合わせ、3件バッチが1リクエストにまとまり全件が同じエラーで失敗することを確認する。
- 2026-09-19: 全体実行で `server/_core/compression.test.ts` が一度だけ suite ごと fail（単独実行・再実行では pass）。原因は**実サーバーを起動するテストが4本に増え、各ファイルが 40000〜50000 のランダムなポートを取り合っていた**こと。`findAvailablePort` は空きを +20 まで走査するが帯域が重なると起動待ちがタイムアウトしうる。4ファイルへ重ならない固定の基点（41000 / 41100 / 41200 / 41300）を割り当てて解消し、全体実行を3回連続で pass させて確認した。既存の `corsVary.test.ts` と `compression.test.ts` も同じ理由で修正している（挙動の変更ではなくテストの安定化）。
- 2026-09-19: 変異チェック（status の早期判定 `if (!response.ok) throw enkaStatusError(...)` を外す）で `enkaErrorClassification.test.ts` が 10件中5件 fail することを確認し、編集で元に戻した。

---
- 反映コミット: `d24f94d`（実装・テスト・テスト安定化）／実装ログの締めは本コミット
- 検証結果: すべて pass。変異チェック（早期 status 判定の除去で 5件 fail → 復元後 pass）/ `vitest run` 62ファイル・434テスト pass を3回連続（Phase 44 の 61/416 から +1ファイル +18テスト。`safeTrpcFetch.test.ts` は 1件→9件）/ `tsc --noEmit` pass / `build:api` pass / Pages ビルド pass（entry 448.96kB・gzip 140.40kB。`safeTrpcFetch` の簡素化で Phase 44 の 449.18kB から微減）/ ゴールデン比較 `compare` 全11セクション一致。本番・実機確認は未実施。
- 残課題・既知の問題: Enka が 404 を HTML で返した場合の分類が `BAD_GATEWAY` から `NOT_FOUND` へ変わる（設計 §7 で意図した変更として許容済み）。利用者向けには「接続できませんでした」より正確な案内になる。
- 次フェーズへの申し送り: (1) 実サーバーを起動するテストを新規に追加するときは、`PORT_BASE` を 41400 以降の未使用帯から割り当てる。ランダムなポートにしない。(2) Phase 46 は `void ... .catch(...)` の形を必ず守る（`await` を外すだけだと `unhandledRejection` になる）。timeout は付けない。
- 関連ドキュメント: [修正設計書_公開API保護と外部API耐障害性_2026-09-19.md](修正設計書_公開API保護と外部API耐障害性_2026-09-19.md) §4 Phase 45・§10-7・§11.1 A-04 / [設計レビュー_公開API保護と外部API耐障害性_2026-09-19.md](設計レビュー_公開API保護と外部API耐障害性_2026-09-19.md) R-13・R-14・R-15


### Phase 44: 原神・ZZZ 静的カタログの Last Known Good と再試行抑止 — 完了

- 目的 / スコープ: `createTimedLoader` を Last Known Good ＋ negative caching の状態機械へ置き換える。TTL 切れ後の再取得に1回失敗しただけで原神・ZZZ の UID 照会が全面的に失敗する状態を解消し、外部障害中に毎リクエストが外部タイムアウトを待つことも防ぐ。HSR 側（`getStaticIndex`）にも60秒の再試行抑止を揃える。
- 対象バージョン: 採番なし（API の運用改善）
- 関連設計書: [修正設計書_公開API保護と外部API耐障害性_2026-09-19.md](修正設計書_公開API保護と外部API耐障害性_2026-09-19.md) §4 Phase 44・§10-6・§11.1 A-03
- 完了日: 2026-09-19
- 着手日: 2026-09-19

**作業メモ（随時追記）**
- 2026-09-19: 実装（`dd15453`）。`server/gameProviders.ts` の `createTimedLoader` を「TTL キャッシュ ＋ Last Known Good（7日）＋ negative caching（60秒）」の状態機械へ置き換え、ログ識別用の `label` を第1引数に足した（`genshin-catalog` / `zzz-catalog`）。`server/enkaFallback.ts` の `getStaticIndex` にも同じ60秒の再試行抑止を追加し、LKG 無しのときに `EMPTY_STATIC_INDEX` を返す既存仕様は変えていない。定数は両ファイルで同値（7日 / 60秒）。
- 2026-09-19: テスト都合の最小限の公開。`createTimedLoader`（`gameProviders.ts`）と `getStaticIndex` / `resetStaticIndexForTests`（`enkaFallback.ts`）を export した。どちらも外部から呼ぶための公開ではなく、外部 API を叩かずに再試行抑止を観測するため。
- 2026-09-19: 回帰テスト `server/staticCatalogResilience.test.ts`（10件）を追加。偽タイマーで時刻を進め、**取得関数（および HSR は global fetch）の呼び出し回数**を数えて「障害中に外部を叩かない」ことを直接確認している。LKG 有り／無しの両方、7日超過、60秒経過後の再試行、同時アクセスの重複排除を含む。
- 2026-09-19: 変異チェック（`CATALOG_RETRY_SUPPRESSION_MS` と `STATIC_RETRY_SUPPRESSION_MS` を 0 にする）で 10件中4件が fail することを確認し、編集で元に戻した。

---
- 反映コミット: `dd15453`（実装・テスト）／実装ログの締めは本コミット
- 検証結果: すべて pass。変異チェック（再試行抑止を 0 にして 4件 fail → 復元後 pass）/ `vitest run` 61ファイル・416テスト pass（Phase 43 の 60/406 から +1ファイル +10テスト）/ `tsc --noEmit` pass / `build:api` pass / Pages ビルド pass（entry 449.18kB・gzip 140.50kB、変化なし）/ ゴールデン比較 `compare` 全11セクション一致。本番・実機確認は未実施。
- 残課題・既知の問題: negative caching 中は外部が復旧していても最大60秒は失敗が続く（設計 §7 で許容済みのトレードオフ）。`fresh` / `stale` の区別は内部状態として持つが UI 表示はしない（設計どおりスコープ外）。
- 次フェーズへの申し送り: (1) `createTimedLoader` は第1引数が `label` になった。新しいカタログを足すときはラベルを渡す。(2) Phase 45 では `fetchEnkaPayload` の処理順を「fetch → status 判定 → 本文 JSON 化 → `getStaticIndex()` → normalize → 0件判定」に変える。**Content-Type は新たな必須条件にしない**（設計 A-04）。**`!data.characters.length` の 0 件判定を normalize の後に必ず残す**（R-14）。
- 関連ドキュメント: [修正設計書_公開API保護と外部API耐障害性_2026-09-19.md](修正設計書_公開API保護と外部API耐障害性_2026-09-19.md) §4 Phase 44・§10-6・§11.1 A-03 / [設計レビュー_公開API保護と外部API耐障害性_2026-09-19.md](設計レビュー_公開API保護と外部API耐障害性_2026-09-19.md) R-04


### Phase 43: 公開 API のレート制限 — 完了

- 目的 / スコープ: 認証不要の公開 API が無制限に実行できる状態を解消する。`/api/trpc` は Express 層から除外し tRPC middleware 内で全体上限＋procedure 別上限を適用、非 tRPC `/api` は Express 層で粗い IP 上限。クライアント IP は Railway の `X-Real-IP` を Railway 環境限定で利用する（`trust proxy` は追加しない）。
- 対象バージョン: 採番なし（API の運用改善）
- 関連設計書: [修正設計書_公開API保護と外部API耐障害性_2026-09-19.md](修正設計書_公開API保護と外部API耐障害性_2026-09-19.md) §4 Phase 43・§10-2・§10-4・§10-5
- 完了日: 2026-09-19
- 着手日: 2026-09-19

**作業メモ（随時追記）**
- 2026-09-19: 実装（ブランチ `fix/phase42-trpc-post` を継続使用）。`server/_core/rateLimit.ts` を新規追加（固定ウィンドウ計数・クライアント IP 確定・UID の HMAC キー・`Retry-After` 付与）。`server/_core/trpc.ts` で全 procedure 共通の土台 `baseProcedure` を作り、`publicProcedure` / `protectedProcedure` / `adminProcedure` をそこから派生させた（`adminProcedure` のインライン middleware は `requireAdmin` へ切り出し）。`server/routers.ts` の `build.lookup` / `feedback.submit` は `.input(...)` の後に `.use(...)` で個別上限。`server/_core/index.ts` に非 tRPC `/api` 用の Express ミドルウェア（CORS の後・`compression` の前）。`server/_core/githubAdminAuth.ts` の交換 API に個別上限。
- 2026-09-19: 実装判断（設計が文言まで定めていなかった箇所）。tRPC 429 のユーザー向け文言は、全体上限「リクエストが集中しています。しばらく待ってから再度お試しください。」、`build.lookup`「照会が集中しています。数分後に再度お試しください。」（MiHoMo / Enka の 429 文言と統一）、`feedback.submit`「送信が集中しています。しばらく待ってから再度お試しください。」とした。挙動は設計どおりで、変更したのは文言のみ。
- 2026-09-19: 設計どおりの帰結として記録。**UID 単位の 6 回/分は IP をまたいで共有する**（キーが `lookup:uid:<game>:<HMAC>` で IP を含まないため）。同一 UID へ 1 分間に 7 人以上が同時に照会すると、7 人目以降が 429 になる。同一 UID への集中を止めるという設計意図どおりだが、公開 UID をデモに使う場合は体感しうる。回帰テスト「UID の上限は IP をまたいで共有する」で固定した。
- 2026-09-19: 回帰テスト3本を追加（計27件）。`server/_core/rateLimit.test.ts`（13件・単体）、`server/rateLimitProcedures.test.ts`（8件・`appRouter.createCaller` で外部 API と DB をモックし middleware の判定だけを見る）、`server/_core/rateLimitHttp.test.ts`（6件・実サーバー起動）。HTTP 側は全ケースが 127.0.0.1 から出て同じバケットを共有するため、使い切る検査をファイル末尾に置いた。
- 2026-09-19: 変異チェック（`/api/health` と `/api/trpc` の除外を外す）で `rateLimitHttp.test.ts` が 6件中4件 fail することを確認し、編集で元に戻した（`git checkout` は使わない）。

---
- 反映コミット: `66cce41`（実装・テスト）／実装ログの締めは本コミット
- 検証結果: すべて pass。変異チェック（除外の除去で 4件 fail → 復元後 pass）/ `vitest run` 60ファイル・406テスト pass（Phase 42 の 57/379 から +3ファイル +27テスト）/ `tsc --noEmit` pass / `build:api` pass / Pages ビルド pass（entry 449.18kB・gzip 140.50kB、Phase 42 から変化なし）/ ゴールデン比較 `compare` 全11セクション一致。本番・実機確認は未実施。
- 残課題・既知の問題: (1) 上限値は初期運用値であり、正規利用が 429 になる実測が出たら別変更で調整する。(2) インメモリ方式は単一インスタンス前提。Railway を 2 replica 以上にする前に共有ストアへ移行する（設計 §7・§10-3）。(3) `X-Real-IP` によるクライアント IP 識別は本番でしか検証できない。production 結合確認で複数クライアントを識別できない場合、設計 §9 に従い Phase 43 を完了扱いにしない（現時点はローカル検証のみで、本番確認は未実施）。
- 次フェーズへの申し送り: (1) tRPC の全体上限（120 operation/分/IP）は `ctx.req.socket.remoteAddress` が取れないコンテキストでは全て `unknown` キーに集まる。既存テストの `{ req: {}, res: {} }` 形式のコンテキストはこれに該当するが、vitest はファイル単位でモジュールを分離するため、1ファイルで 120 回を超える tRPC 呼び出しをしない限り影響しない。新しいサーバーテストを書くときは注意する。(2) Phase 44 は `createTimedLoader` を LKG ＋ negative caching へ置き換える。LKG が無い場合も 60 秒は外部へ行かず直前エラーを再送出する（設計 §4 Phase 44・A-03）。
- 関連ドキュメント: [修正設計書_公開API保護と外部API耐障害性_2026-09-19.md](修正設計書_公開API保護と外部API耐障害性_2026-09-19.md) §4 Phase 43・§10-2・§10-4・§10-5 / [設計レビュー_公開API保護と外部API耐障害性_2026-09-19.md](設計レビュー_公開API保護と外部API耐障害性_2026-09-19.md) R-01・R-02・R-06〜R-10


### Phase 42: tRPC query の POST 化（UID を GET URL へ載せない） — 完了

- 目的 / スコープ: `httpBatchLink` の query が GET で送られ、UID が URL のクエリ文字列に載っている。CDN・プロキシ・アクセスログ・ブラウザ履歴へ残る経路を断つため、router とクライアント呼出を変えずに全 query を POST 送信へ切り替える。
- 対象バージョン: 採番なし（API の運用改善）
- 関連設計書: [修正設計書_公開API保護と外部API耐障害性_2026-09-19.md](修正設計書_公開API保護と外部API耐障害性_2026-09-19.md) §4 Phase 42 / [設計レビュー_公開API保護と外部API耐障害性_2026-09-19.md](設計レビュー_公開API保護と外部API耐障害性_2026-09-19.md) R-05
- 完了日: 2026-09-19
- 着手日: 2026-09-19

**作業メモ（随時追記）**

- 2026-09-19: 設計書をユーザー提供の完成版（Phase 42〜47、ステータス「実装着手可（正本・確定版）」）へ差し替え、設計書インデックスへ反映。本バッチは Phase 42〜47 の6フェーズ・6PR構成で、各フェーズを締めてから次へ進む。
- 方針（設計書 §3-3・R-05）: `build.lookup` の mutation 化は行わない。tRPC 11.18.0 の `httpBatchLink({ methodOverride: "POST" })` ＋ `createExpressMiddleware({ allowMethodOverride: true })` で、React Query の query キャッシュと同時照会抑止を維持したまま POST 送信にする。
- 2026-09-19: 実装（ブランチ `fix/phase42-trpc-post`）。`server/_core/index.ts` に `allowMethodOverride: true`、`client/src/main.tsx` の `httpBatchLink` に `methodOverride: "POST"`。`server/routers.ts` と `client/src/pages/Home.tsx` は設計どおり未変更。
- 2026-09-19: 回帰テスト2本を追加。`server/_core/methodOverride.test.ts`（実サーバー起動・5件。単一/バッチ query の POST 受理、UID を本文から読むこと、既存 GET の維持、mutation は GET 不可）、`client/src/lib/trpcMethodOverride.test.ts`（2件。`methodOverride` 有りで POST・URL に UID なし、無しで GET・URL に UID ありという対照）。
- 2026-09-19: `build.lookup` の POST 受理確認は、当初 存在しない UID を送って外部 API まで到達させていたが、ネットワーク依存で不安定（502）だったため、桁数不正の UID で Zod 検証エラー（400）に到達することを見る形へ変更した。外部 API を叩かない。
- 2026-09-19: 変異チェック（`allowMethodOverride` を外す）で `methodOverride.test.ts` が 5件中3件 fail（405）になることを確認し、元に戻した。この際 `git checkout` で実装差分も消えたため再適用済み。

---
- 反映コミット: `1c80de0`（実装・テスト・設計書・レビュー・インデックス）／実装ログの締めは本コミット（`docs: Phase 42 …を締める`）
- 検証結果: すべて pass。再現テスト（変異チェック `allowMethodOverride` 除去で 5件中3件 fail → 修正後 5件 pass）/ `vitest run` 57ファイル・379テスト pass（Phase 41 の 55/372 から +2ファイル +7テスト）/ `tsc --noEmit` pass / `build:api` pass（`dist/index.js` 2.2MB）/ Pages ビルド pass（entry 449.18kB・gzip 140.50kB）/ ゴールデン比較 `compare` 全11セクション一致。本番・実機確認は未実施（PR マージ後に Phase 46 までまとめて実施する）。
- 残課題・既知の問題: なし。`methodOverride` はリンクを通る全 query に効くため `build.guideHistory` / `display.settings` も POST になるが、いずれも動的応答で HTTP キャッシュを前提にしていない。
- 次フェーズへの申し送り: (1) ゴールデン比較は `node --import tsx scripts/golden-snapshot.mjs compare` で実行する（`node scripts/...` だけでは `ERR_MODULE_NOT_FOUND`、サブコマンド省略では usage 終了）。(2) この環境に `pnpm` は PATH に無く、`node_modules\.bin\` の vitest / tsc / esbuild / vite を直接叩いている。(3) 変異チェックで `git checkout -- <file>` を使うと未コミットの実装差分ごと消える。以後は編集で戻す。(4) Phase 43 のレート制限は設計どおり `/api/trpc` を Express 層から除外し、tRPC middleware 内で全体上限＋個別上限を適用する（Express から通常 JSON の 429 を tRPC endpoint へ返さない）。
- 関連ドキュメント: [修正設計書_公開API保護と外部API耐障害性_2026-09-19.md](修正設計書_公開API保護と外部API耐障害性_2026-09-19.md) §4 Phase 42 / [設計レビュー_公開API保護と外部API耐障害性_2026-09-19.md](設計レビュー_公開API保護と外部API耐障害性_2026-09-19.md) R-05


### Phase 41: tRPC 応答の `Vary: Origin` 欠落の修正 — 完了

- 完了日: 2026-09-15
- 着手日: 2026-09-15
- 目的・スコープ: `/api/trpc/*` が要求元を反射した ACAO を返すのに `Vary` に `Origin` が無い既存不具合を直す。応答本文は不変。
- 対象バージョン: 採番なし（API の運用改善）
- 正本: [修正設計書_tRPC応答のVary_Origin欠落_Phase41_2026-09-15.md](修正設計書_tRPC応答のVary_Origin欠落_Phase41_2026-09-15.md)（PR #70 → `b3adcf6`）
- 経緯: 2026-09-11 に Phase 28（PR #37）として実装したが未マージのまま main が Phase 40 まで進み衝突したため、tRPC 11.18.0 前提で設計から作り直した。
- 2026-09-15: 実装（`d03fff2`、ブランチ `fix/phase41-vary-origin`）。`corsVary.test.ts` 新規（5件）、`index.ts` に `responseMeta`（D1）と CORS 冒頭の `res.vary("Origin")`（D2）、`compression.test.ts` の注記更新。
- 2026-09-15: 検証（ローカル）。
  - 再現テスト: 修正前に 1〜4 fail / 5 pass → 修正後 5件 pass。変異スポットチェック（`Headers` → 素のオブジェクト）で 1・2 が fail することを確認し元に戻した。
  - `pnpm check` pass / `pnpm test` pass（55ファイル・372テスト）/ `build:api` pass / Pages ビルド pass / ゴールデン比較 全11セクション一致（main の状態で baseline 取得）。
  - ビルド済み `dist/index.js`（API_ONLY）: `build.guideHistory` → `Vary: trpc-accept, accept, Origin, Accept-Encoding`・ACAO 維持・`br`、`/api/health` → `Vary: Origin, Accept-Encoding`。
  - 実装前に `pnpm install --frozen-lockfile` でローカルの `@trpc/server` を 11.6.0 → 11.18.0 に揃えた。
- 2026-09-15: PR #71 → main `74be69e`。CI（validate）pass。PR #37 は「Phase 41 で作り直し」とコメントしてクローズ。
- 2026-09-15: 本番確認（Railway 反映後、HTTP）。`build.guideHistory`（Pages の Origin・ブラウザ同等の Accept-Encoding）→ `Vary: trpc-accept, accept, Origin, Accept-Encoding`、ACAO と Allow-Credentials 維持、`br` 15,771 バイト。未許可の Origin → ACAO なし・`Vary` に `Origin`。`/api/health` → `Vary: Origin, Accept-Encoding`。preflight → 204・ACAO・`Vary: Origin`。Chrome で Pages の更新履歴と図鑑（推奨PT表示）が本番 API から表示され、コンソールエラーなし。

---

- 反映コミット: `d03fff2`（修正）/ `5dd729c`（実装ログ）、PR #71 → main `74be69e`。設計書は PR #70 → `b3adcf6`
- 検証結果: ローカル（再現テスト・変異スポットチェック・`pnpm check`・`pnpm test` 55ファイル372テスト・`build:api`・Pages ビルド・ゴールデン比較 全11セクション一致・`dist/index.js` の HTTP 確認）すべて pass。CI pass。本番 HTTP 確認 pass、Chrome 画面確認 pass。
- 残課題・既知の問題: なし（`br` 転送量は Phase 26 の 13,902 → 15,771 バイト。データ増加分で、本修正は本文を変えていない）。
- 次フェーズへの申し送り: tRPC の応答ヘッダーは `setHeader` で書かれるため、Express 側で付けたヘッダーは tRPC 経路で消える。tRPC 経路にヘッダーを足すときは `responseMeta` で `Headers` インスタンスを返す（素のオブジェクトは既定値を置き換える）。tRPC 更新時は `corsVary.test.ts` が検出する。長く未マージの PR は main の Phase 進行と実装ログで衝突するため、衝突したら作り直しを検討する。
- 関連ドキュメント: [修正設計書_tRPC応答のVary_Origin欠落_Phase41_2026-09-15.md](修正設計書_tRPC応答のVary_Origin欠落_Phase41_2026-09-15.md)

### Phase 40: 達成ゲージの目盛りラベル修正（案A の不具合報告） — 完了

- 完了日: 2026-09-15
- 着手日: 2026-09-15
- 経緯: ユーザーが本番（達成ゲージ公開中）で「緋英の会心率が、現在値とバーの数値が異なっている」と報告。調査の結果、計算は正しく（右端は最大目標×1.1、会心率 61.3% は約70%の位置、妥協60%は約68%）、妥協・目標・厳選の数値が目盛りの真下ではなく左・中央・右の固定列に並ぶため、どの線が何%かを読み取れないことが原因と判明。
- ユーザー決定（2026-09-15）: 候補 A「目盛りの真下に数値を置く（0 起点の目盛りは変えない）」で修正する。
- 正本: [実装設計書_表示デザインの管理画面切替_2026-09-13.md](実装設計書_表示デザインの管理画面切替_2026-09-13.md) の §4.4 A（本修正で追記）
- 2026-09-15: 実装（`ac0c472`、ブランチ `fix/stat-gauge-tick-labels`、未 push）。`StatGauge.tsx` のバーの下に、各目盛りと同じ left の位置へ値のラベルを追加（目標は1段下げて隣と重ならないようにする、左右端はバーからはみ出さないよう寄せる、色は目盛りと同じ）。既存の達成状態の列と算出は変更なし。
- 2026-09-15: 検証。全回帰 pass（54ファイル・367テスト、再現テスト1件追加）/ 型検査 pass / Pages ビルド pass（entry 454.51kB）/ ゴールデン比較 全11セクション一致。変異チェック（ラベルの位置を目盛りとずらす）で再現テストが落ちることを確認。ローカル Google Chrome で、緋英と同じ数値の検証用データ（会心率 61.3% / 60・70・80 など4行）を照会し、各値が目盛りの真下に並び、目標が1段下で重ならないことを目視。
---
- 反映コミット: `ac0c472`（修正）/ `c65fdbe`（実装ログ・設計書）、PR #67 → main `fd0a6ba`
- 検証結果: 上記のローカル検証に加え、CI（PR #67 の validate）pass、Pages デプロイ success、公開中の Home チャンクに目盛りラベルが含まれることを確認。実 UID（緋英）での本番表示は、2026-09-15 にユーザーが確認し問題なし。
- 残課題・既知の問題: 狭い画面で目盛り同士が近い場合（例: 妥協と目標の差が小さいステータス）はラベルが近接する可能性がある（目標を1段下げて緩和済み）。
- 次フェーズへの申し送り: バーやグラフに数値を添えるときは、固定列ではなく図形と同じ位置に置く。
- 関連ドキュメント: [実装設計書_表示デザインの管理画面切替_2026-09-13.md](実装設計書_表示デザインの管理画面切替_2026-09-13.md) §4.4 A

### Phase 39: 表示デザインの管理画面切替 設計内 Phase 7（案 E-2 ダークテーマの切替） — 完了

- 完了日: 2026-09-15
- 目的: 表示キー `colorTheme` のバリアント `switchable` を追加し、公開4画面で利用者がライト／ダークを切り替えられるようにする。legacy と管理画面・404 はライト固定。
- 正本: [実装設計書_表示デザインの管理画面切替_2026-09-13.md](実装設計書_表示デザインの管理画面切替_2026-09-13.md) の §4.4 E-2・§4.5 G-E2・§6.3 手順7・§8.4 E2a・E2b・§9.9
- 2026-09-15 ユーザー決定: ダーク配色は「既存の色クラスに dark: を追加」ではなく、`index.css` の `.dark` で色の定義（stone / amber / emerald / rose / white と shadcn トークン）を置き換え、もともと暗い面には印（`data-surface="dark"`）を付けて元の色に戻す方式で進める（約780か所への dark: 追加を避ける）。個別の微調整が必要な箇所だけ追加する。設計書 §4.4 E-2 の実装方針を締め時に更新する。
- 実施済み:
  - `ThemeContext.tsx`（好みと有効テーマの分離、保存値の検証と例外吸収、切り替え時だけ保存）、`App.tsx` の ThemeGate と Provider 順、`ThemeToggle.tsx`（言語切替の隣）。
  - `index.css` の `.dark`（G-E2 トークンと色の置き換え）、濃色面4か所の `data-surface="dark"`、ダーク時のゲーム別アクセント。登録表 `colorTheme`、3言語の文言3件。
- 反映コミット: `62e9bf5`（実装）/ `56866d8`（実装ログ）、PR #65 → main `1557a9d`
- 検証結果:
  - 全回帰 pass（54ファイル・366テスト）/ 型検査 pass / `build:api` pass / Pages ビルド pass。CI（PR #65 の validate）pass、Pages デプロイ success。
  - M1: entry 454.51kB。ゴールデン比較 全11セクション一致（baseline `b8d7057`）。
  - 受入テスト E2a・E2b（トークンからのコントラスト計算を含む）・R2 pass、変異チェック5件すべて検出。
  - ローカル Google Chrome: 図鑑（カード棚・シート・隊列・凸）、3ゲーム入口、更新履歴、翻訳フィードバックをダークで目視、管理画面へ移るとライトで戻るとダーク、コンソールエラーなし。
  - **本番**: 公開中の entry に `colorTheme` を確認。本番管理画面（管理者ログイン済み）に案E-2「ダークテーマの切替」（現行／切替を開放、影響4画面、公開中: 現行、最終更新: 未保存）が表示され、管理画面はライトであることを確認。公開操作はしていない。
  - UID 照会結果（濃色面・達成ゲージ）と 360/768px のダーク表示は、公開後の 2026-09-15 にユーザーが確認し問題なし。
  - **未実施**: 本番での公開・プレビュー操作、M3、隔離 DB。
- 残課題・既知の問題:
  - 締め時点の公開値は、凸 = `stepper`、達成ゲージ = `gauge`、トップの3ゲーム入口 = `portal`、共通ヘッダー = `unified`（いずれも管理画面から公開）、推奨PTの隊列・カード棚・ダークテーマ = 現行。
  - Phase 32 から継続の M3 と隔離 DB は未検証のまま。
- 次フェーズへの申し送り:
  - 表示デザインの管理画面切替は設計内 Phase 1〜7 をすべて実装済み。今後の新しい見せ方は別設計とする。
  - 新しい部品に色を足すときは、ライトの色クラスだけで書き、もともと暗い面には `data-surface="dark"` を付ける（ダークは `.dark` の置き換えで追従する）。inline style で色の値を持つ場合は、ダーク用の上書きを `index.css` に足す。
- 関連ドキュメント: [実装設計書_表示デザインの管理画面切替_2026-09-13.md](実装設計書_表示デザインの管理画面切替_2026-09-13.md) / [設計書インデックス.md](設計書インデックス.md)
- 実装メモ（時系列）:
- 2026-09-15: 実装（`62e9bf5`、ブランチ `feat/display-variants-phase7-theme`、未 push）。
  - `ThemeContext.tsx`: 保存済みの好み（キー `theme`、light / dark 以外と storage 例外は light）と有効テーマ（switchable のときだけ好み、それ以外は light）を分離。好みは切り替えたときだけ保存。`documentElement` の `dark` クラスを有効テーマに同期。Provider 外用の `useOptionalTheme`。
  - `App.tsx`: ErrorBoundary → LanguageProvider → DisplaySettingsProvider → WouterRouter → ThemeGate → ThemeProvider → TooltipProvider / Toaster / 帯 / Router / ショートカット。ThemeGate は base 除去後の location が公開4ルートかつ `colorTheme = switchable` のときだけ切替を許可（管理画面・404 はライト）。Provider に key を付けない。
  - `ThemeToggle.tsx` 新規（切り替えられない画面では描かない）。`LanguageSwitcher` の隣に描くことで、legacy / unified 両ヘッダーに1か所の変更で並べる（管理画面では出ない）。3言語の文言3件。
  - `index.css`: `.dark` に G-E2 のトークンと、stone / amber / emerald / rose / white の置き換え色。`[data-surface="dark"]`（UID照会の人物カード・推奨ビルド・凸・推奨PTの濃色面、計4か所）では Tailwind の既定値へ戻す。body の背景模様をダーク時は消す。ゲーム別アクセントはダーク時に濃色面用の値へ切り替え（統一ヘッダーのスコープ、案F のパネル、案B のカード帯、凸の明るい面、共通ヘッダーの既定色）。
  - 登録表に `colorTheme`（プレビュー先 UID照会）。
- 2026-09-15: ローカル検証の結果。
  - 全回帰 pass（54ファイル・**366テスト**）/ 型検査 pass / `build:api` pass / Pages ビルド pass（500kB 警告なし）。
  - M1: entry 453.40kB → **454.51kB**、CSS 137.85kB。
  - ゴールデン比較: `b8d7057` の baseline に対し、`--allow` / `--refresh` なしで全11セクション一致。
  - 受入テスト E2a（legacy は light で好みを上書きしない、保存値の検証と storage 例外、切り替え時だけ保存、公開4画面で保持、管理画面・404 は light で公開画面へ戻ると dark、公開値の legacy 化・再 switchable で再マウントなし）と E2b（`.dark` の本文 4.5:1・UI 境界 3:1 をトークンから計算）、R2 pass。変異チェック5件（有効テーマ＝好み・保存値の検証・管理画面の固定・コントラスト・切替時の保存）すべて検出。
  - ローカル Google Chrome（表示設定を全案の新表示＋switchable に差し替え、実データ）: 図鑑のカード棚とシート（推奨ビルド・目標表・隊列・凸タイムライン）、UID照会の3ゲーム入口、更新履歴、翻訳フィードバックをダークで目視し、文字の読みにくさ・横はみ出し・コンソールエラーなし。管理者ポータルへ移るとライトで切替なし、戻るとダークに戻り、保存値は dark のまま。
  - **未実施**: UID 照会結果（人物カード・推奨ビルド・凸・推奨PTの濃色面、達成ゲージ）のダーク目視（実 UID が必要）、360/768px、本番、M3、隔離 DB。
- ステータス: 進行中

---

### Phase 38: 表示デザインの管理画面切替 設計内 Phase 6（案 B 図鑑カード棚・詳細シート） — 完了

- 完了日: 2026-09-15
- 目的: キャラ図鑑の一覧と詳細に、表示キー `catalogShelf` のバリアント `shelf` を追加する。図鑑データ・API・URL パラメータの意味は変えない。
- 正本: [実装設計書_表示デザインの管理画面切替_2026-09-13.md](実装設計書_表示デザインの管理画面切替_2026-09-13.md) の §4.4 B・§4.5 G-B・§6.3 手順6・§8.4 B1〜B3・§9.8
- 実施済み:
  - `CatalogShelf.tsx` 新規（カード棚、AND 絞り込みと並び順、右シート／狭幅全画面、sticky のジャンプ列、各状態の案内と再試行）。
  - `CharacterCatalog.tsx`: 詳細 JSX の変数化（D6 例外1）、shelf 用の選択・URL effect、フィルター state。登録表 `catalogShelf`、3言語の文言18件。
- 反映コミット: `f6a7df3`（実装）/ `85f8655`（実装ログ）、PR #63 → main `82ddb03`
- 検証結果:
  - 全回帰 pass（53ファイル・360テスト）/ 型検査 pass / `build:api` pass / Pages ビルド pass。CI（PR #63 の validate）pass、Pages デプロイ success。
  - M1: entry 453.40kB。ゴールデン比較 全11セクション一致（baseline `d506204`）。
  - 受入テスト B1〜B3・R2 pass、変異チェック6件すべて検出。
  - ローカル Google Chrome: カード3列、legacy からの選択引き継ぎ、ジャンプと sticky、閉じたときの URL とフォーカス、横はみ出し0、コンソールエラーなし。
  - **本番**: 公開中の entry に `catalogShelf` を確認。本番管理画面（管理者ログイン済み）に案B「図鑑カード棚・詳細シート」（現行／カード棚、影響画面 キャラ図鑑、公開中: 現行、最終更新: 未保存）が表示されることを確認。公開操作はしていない。
  - 360/768px（全画面シート）は、公開後の 2026-09-15 にユーザーが確認し問題なし。
  - **未実施**: 本番での公開・プレビュー操作、M3、隔離 DB。
- 残課題・既知の問題:
  - 締め時点の公開値は、凸 = `stepper`、達成ゲージ = `gauge`、トップの3ゲーム入口 = `portal`、共通ヘッダー = `unified`（いずれも管理画面から公開）、推奨PTの隊列とカード棚 = 現行。
  - Phase 32 から継続の M3 と隔離 DB は未検証のまま。
- 次フェーズへの申し送り:
  - 次は設計内 Phase 7（案E-2 ダークテーマの切替、§4.4 E-2・§6.3 手順7・§8.4 E2a・E2b）。公開4画面と全バリアント（シートの portal、凸の tone、濃色の PT 面を含む）へ dark 配色を足すため、変更範囲が最も広い。
  - Radix のシート（Dialog）で閉じたあとのフォーカスを移すときは、`onCloseAutoFocus` で既定を止め、次のタスクで focus する。自動操作の Chrome タブが裏にある間は閉じるアニメーションが終わらないので、前面に出してから確認する。
- 関連ドキュメント: [実装設計書_表示デザインの管理画面切替_2026-09-13.md](実装設計書_表示デザインの管理画面切替_2026-09-13.md) / [設計書インデックス.md](設計書インデックス.md)
- 実装メモ（時系列）:
- 2026-09-15: 実装（`f6a7df3`、ブランチ `feat/display-variants-phase6-shelf`、未 push）。
  - `CatalogShelf.tsx`: ゲーム切替、名前検索・精査状態・バッチ（選択ゲームの entries から重複除去の数値降順）・並び順（図鑑の順／バッチの新しい順、同値は元順、未設定は末尾）、カードは1/2/3列でゲーム色の細帯と精査状態。詳細は既存 `ui/sheet` の右シート（640px 未満は 100vw×100dvh、以上は min(56rem, 90vw)）、内部だけ縦スクロール、4項目のジャンプ列を sticky、閉じるボタン。一覧の取得中・失敗（再試行）、詳細の読込中・失敗（同じ query を再試行）・対象なし、不正な直リンクの案内。純関数 `shelfBatchOptions` / `shelfEntries`。
  - `CharacterCatalog.tsx`: C/D の分岐を解決した詳細 JSX を変数 `detail` にまとめ、legacy の外枠とカード棚の両方へ渡す（D6 例外1、4セクションに id と scroll-margin を追加）。shelf では自動選択をせず、取得済みの一覧に無い名前だけを不正として URL から消す。閉じたら character だけを消し game・他のパラメータ・hash を保つ。ゲーム変更で選択・検索・精査状態・バッチをリセットし並び順は保持。legacy↔shelf の切替は同じ state のまま（再マウントなし）。
  - 登録表に `catalogShelf`（プレビュー先 図鑑）、図鑑の `uiText` に3言語の文言18件。
  - 設計から細部を決めた点: 閉じたあとのフォーカス移動は Radix の閉じる処理の後（`setTimeout 0`）に行う（同時に行うと body へ戻される）。shelf で未選択のときも URL の game を現在のゲームに合わせる。
- 2026-09-15: ローカル検証の結果。
  - 全回帰 pass（53ファイル・**360テスト**）/ 型検査 pass / `build:api` pass / Pages ビルド pass（500kB 警告なし）。
  - M1: entry 452.81kB → **453.40kB**。CharacterCatalog 17.08 → 30.05kB（シートは既存の dialog チャンクを共有）。
  - ゴールデン比較: `d506204` の baseline に対し、`--allow` / `--refresh` なしで全11セクション一致。
  - 受入テスト B1〜B3（B3 は shelf × 隊列 / 凸の4構成）と R2 pass。変異チェック6件（一覧取得中の不正判定・URL の character 削除・ゲーム変更の絞り込みリセット・未設定を末尾・開いたカードへのフォーカス・legacy 分岐）すべて検出。
  - ローカル Google Chrome（表示設定だけ差し替え、図鑑は実データ）: ZZZ 59件のカード3列、legacy で選ばれていた「11号」を引き継いでシートが開く、ジャンプ「凸」で内部スクロールしジャンプ列は上端に固定、閉じると URL から character だけ消え、開いたカードへフォーカスが戻る、横はみ出し0、コンソールエラーなし。自動操作のタブが裏にある間は閉じるアニメーションが止まり、前面表示で完了することを確認（実装の問題ではない）。
  - **未実施**: 360/768px（全画面シート）の実機幅、本番、M3、隔離 DB。
- ステータス: 進行中

---

### Phase 37: 表示デザインの管理画面切替 設計内 Phase 5（案 E-1 共通ヘッダーとゲーム別アクセント） — 完了

- 完了日: 2026-09-15
- 目的: 公開4画面のヘッダーに、表示キー `siteChrome` のバリアント `unified` を追加する。unified のときだけ固定の図鑑ショートカットを隠し、UID照会と図鑑の外枠でゲーム別の色を定義する。
- 正本: [実装設計書_表示デザインの管理画面切替_2026-09-13.md](実装設計書_表示デザインの管理画面切替_2026-09-13.md) の §4.4 E-1・§4.5 G-E1・§6.3 手順5・§8.4 E1a・E1b・§9.7
- 実施済み:
  - `SiteHeader.tsx` 新規（現在地、`/admin`、1024px 未満のメニューと Escape・フォーカス復帰）、公開4画面のヘッダー分岐、固定ショートカットの出し分け。
  - UID照会・図鑑の外枠だけに `data-game` と `--game-accent` / `--game-accent-on-dark`。凸のタイムライン（濃色面）は濃色面用の変数を参照。
  - 登録表 `siteChrome`、プレビュー先 `/feedback`、3言語の文言5件。
- 反映コミット: `0bbdfed`（実装）/ `3119ce1`（実装ログ）、PR #61 → main `f26f724`
- 検証結果:
  - 全回帰 pass（51ファイル・344テスト）/ 型検査 pass / `build:api` pass / Pages ビルド pass。CI（PR #61 の validate）pass、Pages デプロイ success。
  - M1: entry 452.81kB。ゴールデン比較 全11セクション一致（baseline `a8a8b1c`）。
  - 受入テスト E1a・E1b・R2 pass、変異チェック4件すべて検出。
  - ローカル Google Chrome: 図鑑（原神）で共通ヘッダー・現在地・ゲーム色の範囲・横はみ出し0・コンソールエラーなし。
  - **本番**: 公開中の entry に `siteChrome` を確認。本番管理画面（管理者ログイン済み）に案E-1「共通ヘッダーとゲーム別アクセント」（現行／共通ヘッダー、影響4画面、公開中: 現行、最終更新: 未保存）とプレビュー先「翻訳フィードバック」が表示されることを確認。公開操作はしていない。
  - 360/768px の表示とメニュー開閉は、公開後の 2026-09-15 にユーザーが確認し「表示は問題なし」と回答。
  - **未実施**: 更新履歴・翻訳フィードバックの目視、本番での公開・プレビュー操作、M3、隔離 DB。
- 残課題・既知の問題:
  - 締め時点の公開値は、凸 = `stepper`、達成ゲージ = `gauge`、トップの3ゲーム入口 = `portal`、推奨PTの隊列と共通ヘッダー = 現行。案F の公開はこのセッションの操作ではなく管理画面から行われた。
  - Phase 32 から継続の M3 と隔離 DB は未検証のまま。
- 次フェーズへの申し送り:
  - 次は設計内 Phase 6（案B 図鑑カード棚・詳細シート、§4.4 B・§6.3 手順6・§8.4 B1〜B3）。B×C/D の組み合わせ検証を必ず行う。
  - ページが `useDisplayVariant` を新たに使うときは、既存テストの `DisplaySettingsContext` のモック有無を確認する（今回は Provider 外でも legacy に倒れて既存テストは通った）。
- 関連ドキュメント: [実装設計書_表示デザインの管理画面切替_2026-09-13.md](実装設計書_表示デザインの管理画面切替_2026-09-13.md) / [設計書インデックス.md](設計書インデックス.md)
- 実装メモ（時系列）:
- 2026-09-15: 実装（`0bbdfed`、ブランチ `feat/display-variants-phase5-chrome`、未 push）。
  - `SiteHeader.tsx`: サイト名、ゲーム記号と名称（UID照会・図鑑のみ）、UID照会 / キャラ図鑑 / 更新履歴 / 翻訳フィードバック / 管理者（`/admin`）、現在地は `aria-current="page"`。1024px 未満はメニューボタン（aria-expanded / aria-controls）、Escape で閉じてボタンへフォーカス、項目選択で閉じる。ラベルは11px。
  - 公開4画面のヘッダーを `siteChrome` で分岐（legacy の JSX は移動なし）。図鑑の「UID不要 / 収録数」は共通ヘッダー右側へ残す。UID照会と図鑑の外枠には unified のときだけ `data-site-chrome` と `data-game` を付け、`index.css` でゲーム色を定義（body には付けない）。
  - `PublicCatalogShortcut.tsx`: unified のときは出さない（設計の「App のショートカット条件」を部品側で判定。表示位置は `/` だけのまま）。
  - 登録表に `siteChrome`（プレビュー先 図鑑）、プレビュー先の固定候補に `/feedback`、3言語の文言5件。
  - 設計から細部を決めた点: G-E1 の濃色面用の色を `--game-accent-on-dark` として同時に定義し、凸のタイムライン（濃色面）はこちらを参照するように変更（明るい面は従来どおり `--game-accent`）。濃色面に明るい面用の濃い色が載るのを防ぐため。
- 2026-09-15: ローカル検証の結果。
  - 全回帰 pass（51ファイル・**344テスト**）/ 型検査 pass / `build:api` pass / Pages ビルド pass（500kB 警告なし）。
  - M1: entry 451.59kB → **452.81kB**。共通ヘッダーは独立チャンク（SiteHeader 3.05kB）。
  - ゴールデン比較: `a8a8b1c` の baseline に対し、`--allow` / `--refresh` なしで全11セクション一致。
  - 受入テスト E1a・E1b（4画面の現在地・Pages base 付きリンク・`/admin`、legacy は従来ヘッダー、メニューの Escape とフォーカス復帰、固定ショートカット、ゲーム変更と中立ページ）と R2（プレビュー先 `/feedback`）pass。変異チェック4件（Escape のフォーカス復帰・ショートカット判定・図鑑の legacy に data-game・現在地の反転）すべて検出。
  - ローカル Google Chrome（表示設定だけ差し替え、図鑑は実データ）: 図鑑（原神）で共通ヘッダー・現在地「キャラ図鑑」・外枠の `--game-accent` #115e59 と凸タイムラインへの反映、body に色が残らない、横はみ出し0、コンソールエラーなし。
  - **未実施**: 360/768px の実機幅（メニュー開閉の目視）、更新履歴・翻訳フィードバックの目視、本番、M3、隔離 DB。
- ステータス: 進行中

---

### Phase 36: 表示デザインの管理画面切替 設計内 Phase 4（案 F トップの3ゲーム入口） — 完了

- 完了日: 2026-09-15
- 目的: Home 先頭（見出し・UID照会フォーム・利用手順）に、表示キー `topPortal` のバリアント `portal` を追加する。照会ロジック・URL の意味・送信前に照会しない規約は変えない。
- 正本: [実装設計書_表示デザインの管理画面切替_2026-09-13.md](実装設計書_表示デザインの管理画面切替_2026-09-13.md) の §4.4 F・§4.5 G-F・§6.3 手順4・§8.4 F1・F2・§9.6
- 実施済み:
  - `TopPortal.tsx` 新規（3ゲームパネル、選択ゲーム内だけの入力欄、成功後の帯とほかのゲーム、UIDを変更、部品内の収録数取得と再取得、注記・利用手順の保持）。
  - `index.css` の CSS だけの装飾、`Home.tsx` 先頭の分岐（legacy JSX は移動なし）、登録表 `topPortal`、3言語の文言11件。
- 反映コミット: `b9d9ed8`（実装）/ `04530c8`（実装ログ）、PR #57 → main `dd45a52`
- 検証結果:
  - 全回帰 pass（50ファイル・337テスト）/ 型検査 pass / `build:api` pass / Pages ビルド pass。CI（PR #57 の validate）pass、Pages デプロイ success。
  - M1: entry 449.89kB → 451.59kB。ゴールデン比較 全11セクション一致（baseline `6264ff1`）。
  - 受入テスト F1・F2 pass、変異チェック4件すべて検出。
  - ローカル Google Chrome: 3列パネル・実データの収録数・ゲーム選択とフォーカス・横はみ出し0・コンソールエラーなし。
  - 360px / 768px の表示は、2026-09-15 にユーザーが確認し「問題ない」と回答（こちらの Chrome では幅を縮められず未実施だった分）。
  - **本番**: 公開中の entry に `topPortal` が含まれることを確認。公開値は `{progressionStepper: stepper, statGauge: gauge}` のままで、トップは現行表示（未保存キーは legacy）。
  - 締め時点では管理者セッション切れで未確認だったが、2026-09-15 にユーザーの再ログイン後、本番管理画面（Google Chrome）で案F「トップの3ゲーム入口」（現行／3ゲーム入口、影響画面 UID照会、公開中: 現行、最終更新: 未保存）が表示されることを確認。管理用 API が4キーを返すことも確認済み。公開操作はしていない。
  - **未実施**: 実 UID 照会後の帯表示の目視、本番での公開・プレビュー操作、M3、隔離 DB。
- 残課題・既知の問題:
  - `compression.test.ts` は全回帰の負荷時に API 起動待ち60秒を超えて失敗することがある（再実行で pass）。
  - Phase 32 から継続の M3 と隔離 DB は未検証のまま。
- 次フェーズへの申し送り:
  - 次は設計内 Phase 5（案 E-1 共通ヘッダーとゲーム別アクセント、§4.4 E-1・§6.3 手順5・§8.4 E1a・E1b）。`--game-accent` は E-1 だけが定義し、案F の部品内の色は既定値として残す。
  - ローカル Chrome の表示設定の差し替えは、fetch 差し替え後に refetch の間隔（約60秒）を待ってから visibilitychange を送る。1回の実行で60秒以上待つと CDP が45秒で時間切れになるため、30秒ずつに分ける。
- 関連ドキュメント: [実装設計書_表示デザインの管理画面切替_2026-09-13.md](実装設計書_表示デザインの管理画面切替_2026-09-13.md) / [設計書インデックス.md](設計書インデックス.md)
- 実装メモ（時系列）:
- 2026-09-15: 実装（`b9d9ed8`、ブランチ `feat/display-variants-phase4-portal`、未 push）。
  - `TopPortal.tsx`: 見出し「HoYoverse Builder」＋3ゲーム名の副題、HSR→原神→ZZZ のパネル（1024px 以上3列・未満は縦積み）に装備の呼び名・図鑑の収録数・「UIDで照会」「図鑑を見る」。入力欄は選択中ゲームのパネル内だけ。照会成功かつ1件以上で帯に縮め、ほか2ゲームをボタンで並べる。「UIDを変更」で入力を開き、編集だけでは再照会しない。収録数は部品内で `referenceCatalog`（staleTime 10分）、取得中・失敗は「—」、失敗時は再取得ボタン。注記3行は文言そのまま、利用手順はパネルの下（Home の現行条件を渡す）。色は G-E1 の確定値を部品内に持つ。
  - `index.css`: CSS だけの装飾（HSR 同心の細線、原神7色の円環、ZZZ 黒黄ストライプ）。画像・ロゴなし。
  - `Home.tsx`: 先頭の section と利用手順を `topPortal` で分岐（legacy の JSX は移動しない）。state と `handleSubmit` / `selectGame` は既存のまま渡す。
  - 登録表に `topPortal`（プレビュー先 UID照会）、3言語の文言11件。既存テストは4キー目を明示した期待値へ更新（別キーのバリアント名 `formation` の拒否を追加）。
- 2026-09-15: ローカル検証の結果。
  - 全回帰 pass（50ファイル・**337テスト**）/ 型検査 pass / `build:api` pass / Pages ビルド pass（500kB 警告なし）。途中の全回帰で `compression.test.ts` が API 起動待ち60秒を超えて1回失敗、単独再実行と全回帰の再実行で pass（負荷時の一時的な失敗）。
  - M1: entry 449.89kB → **451.59kB**（gzip 142.48kB、登録表と文言の分）。Home 48.18 → 54.84kB。
  - ゴールデン比較: `6264ff1` の baseline に対し、`--allow` / `--refresh` なしで全11セクション一致。
  - 受入テスト F1・F2 pass。変異チェック: 送信時に帯へ戻さない / Home で収録数を常に取得 / 帯に縮めない / 成功後も利用手順を出す の4件すべてで対応テストが落ちることを確認（最後の1件は検証を追加してから）。
  - ローカル Google Chrome（表示設定だけ差し替え、図鑑は実データ）: 3列パネルと収録数 84 / 109 / 59、見出し、注記、利用手順、原神の「UIDで照会」で入力が原神パネルへ移りフォーカス、ページの横はみ出し0、コンソールエラーなし。
  - **未実施**: 360/768px の実機幅確認（ウィンドウが最大化で縮められず、iframe は表示設定の差し替えが効かなかった）、実 UID 照会後の帯表示の目視、本番、M3、隔離 DB。
- ステータス: 進行中

---

### Phase 35: 表示デザインの管理画面切替 設計内 Phase 3（案 C 推奨PTの隊列） — 完了

- 完了日: 2026-09-14
- 目的: UID照会と図鑑の推奨PTに、表示キー `partyFormation` のバリアント `formation` を追加する。推奨PTデータと Home の目標補正順（PT → 凸）は変えない。
- 正本: [実装設計書_表示デザインの管理画面切替_2026-09-13.md](実装設計書_表示デザインの管理画面切替_2026-09-13.md) の §4.4 C・§4.5 G-C・§6.3 手順3・§8.4 C1〜C3・§9.5
- 実施済み:
  - 登録表に `partyFormation` を追加（プレビュー先はキャラ図鑑）。
  - `client/src/components/variants/PartyFormation.tsx`: PLAN の横タブ（左右は端で折り返し・Home・End、roving tabindex）、役割の色帯付きメンバーカード（回復→耐久→弱体→支援→主力、未知語は無彩色、原文の役割も表示）、表示中キャラの完全一致だけを強調、目標変更を「基礎目標 → 編成目標」の初期閉 details、シナジー・要約・版・日付・出典・コミュニティ照合を保持。選択状態はページが持つ。
  - `Home.tsx` は既存の PT 選択 state を渡すだけ。`CharacterCatalog.tsx` は formation 専用の PT 選択 state（ゲーム・キャラ変更で先頭へ）を追加し、legacy の全案一覧は変更なし。3言語の文言を追加。
  - 設計から細部を決めた点: 目標変更が無い案の注記は Home だけに出す（図鑑の情報量を変えない）。
- 反映コミット: `2d7d243`（実装）/ `c5997c3`（実装ログ）、PR #54 → main `7c925dc`
- 検証結果:
  - 全回帰 pass（49ファイル・**330テスト**。+2ファイル・+11テスト）/ 型検査 pass / `build:api` pass / Pages ビルド pass（500kB 警告なし）。CI（PR #54 の validate）pass、Pages デプロイ success。
  - M1: entry 449.00kB → **449.89kB**。隊列部品は Home と図鑑の共有チャンク（8.13 → 14.43kB）に入り、entry には入らない。
  - ゴールデン比較: `86c084c` の baseline に対し、`--allow` / `--refresh` なしで**全11セクション一致**。
  - 受入テスト C1〜C3 pass。変異チェックでテストの抜け（役割の優先順位の入れ替えを検出しない）を見つけて補い、優先順位・名前の完全一致・図鑑の選択リセット・legacy 分岐の**すべて**で対応テストが落ちることを確認。
  - ローカル Google Chrome: UID照会で PT 切替と達成ゲージの連動（同じキーは凸の 70% が優先）、図鑑でクラレッタ（3人・358px）とホタル（4人・766px）、キー操作とフォーカス、ページの横はみ出し0、3言語、コンソールエラーなし。
  - **本番**: Pages のデプロイと公開中ファイルへの反映を確認。公開中の図鑑（ホタル）は、その時点の公開値どおり凸が縦のタイムライン、推奨PTは現行の3案カードで表示され、横はみ出し・コンソールエラーなし。
  - 締め時点では管理者セッション切れで未確認だったが、2026-09-14 にユーザーの再ログイン後、本番の管理画面（Google Chrome）で案C「推奨PTの隊列」（現行／隊列、影響画面 UID照会・キャラ図鑑、公開中: 現行、最終更新: 未保存）が表示されることを確認。管理画面は管理用 API の応答を表示するため、API が3キーを返すことも確認済み。公開操作はしていない。
  - **未実施**: 実 UID での本番描画、本番での公開・プレビュー操作、M3、隔離 DB。
- 残課題・既知の問題:
  - 締め時点の公開値は、凸の6段表示 = `stepper`、目標の達成ゲージ = `gauge`、推奨PTの隊列 = 現行。凸と達成ゲージの公開は、このセッションの操作ではなく管理画面から行われた。
  - 766px 幅でも4人のメンバー欄は横スクロールになる（長い名前でカードが広がる）。設計の許容範囲として変更していない。
  - Phase 32 から継続の M3 と隔離 DB は未検証のまま。
- 次フェーズへの申し送り:
  - 次は設計内 Phase 4（案F トップの3ゲーム入口、§4.4 F・§6.3 手順4・§8.4 F1・F2）。記憶済み UID で送信前に照会しないこと、legacy で図鑑の収録数の取得が0件であることを必ず確認する。
  - 本番の管理画面を確認するときは、管理者セッション（12時間）が有効かを先に確かめる。
  - 置換スクリプトでテンプレート文字列に正規表現を書くと `\/` のエスケープが消える。テストに正規表現を入れるときは文字列一致に寄せる。
- 関連ドキュメント: [実装設計書_表示デザインの管理画面切替_2026-09-13.md](実装設計書_表示デザインの管理画面切替_2026-09-13.md) / [設計書インデックス.md](設計書インデックス.md)

### Phase 34: 表示デザインの管理画面切替 設計内 Phase 2（案 A 目標の達成ゲージ） — 完了

- 完了日: 2026-09-13
- 目的: UID照会の目標マトリクスと次の強化に、表示キー `statGauge` のバリアント `gauge` を追加する。比較・目標・優先順の算出とキャラクターデータは変えない。
- 正本: [実装設計書_表示デザインの管理画面切替_2026-09-13.md](実装設計書_表示デザインの管理画面切替_2026-09-13.md) の §4.4 A・§4.5 G-A・§6.3 手順2・§8.4 A1〜A3・R1・R2・§9.4
- 実施済み:
  - 登録表に `statGauge` を追加（プレビュー先は UID照会）。
  - `client/src/components/variants/StatGauge.tsx`: `StatGauge`（紙面調の1本バー、3ティアの名称・値・達成状態を常時表示、全体の到達は全件が満たす最上位ティア・未取得を含むと判定保留・どれも満たさなければ妥協未満、右端は現在値と旧新の全目標の最大×1.1、変更前と差があるキーだけ点線と「変更前」の文字）と `UpgradePriorities`（既存の順位・不足量・理由、装備提案は初期閉の details）。
  - `Home.tsx`: 既存 JSX を移動せず2箇所に分岐を追加。算出関数は変更なし。
  - `DisplaySettingsAdmin.tsx`: 応答に無い登録キーを現行として扱う（R2）。3言語の文言10件を追加。
  - 既存テストは `statGauge` を明示した期待値へ更新（緩和はしていない）。
- 反映コミット: `146cb2b`（設計書改訂）/ `fde8ae8`（実装）/ `7755073`（実装ログ）、PR #52 → main `9587a13`
- 検証結果:
  - 全回帰 pass（47ファイル・**319テスト**。+2ファイル・+16テスト）/ 型検査 pass / `build:api` pass / Pages ビルド pass（500kB 警告なし）。CI（PR #52 の validate）pass、Pages デプロイ success。
  - M1: entry 447.64kB → **449.00kB**（gzip 141.16 → 141.63kB）。Home 41.15 → 47.96kB。
  - ゴールデン比較: `0808ab1` の baseline に対し、`--allow` / `--refresh` なしで**全11セクション一致**。
  - 受入テスト A1〜A3・R2 pass。変異チェック（判定保留、変更前の目標の検出、UID照会の legacy 分岐、0件の文言）の**4件すべて**で対応テストが落ちることを確認。
  - ローカル Google Chrome（応答差し替え）: 判定保留・1 / 5・変更前の点線3本・追加キーに点線なし・装備提案の初期閉。358px / 766px の iframe でページの横はみ出し0、3言語で文言がそろう。コンソールエラーなし。
  - **本番**: Pages と Railway の反映を確認。管理用 API が `progressionStepper` と `statGauge` の2キー（どちらも legacy）を返し、管理画面に「案 A 目標の達成ゲージ」が「公開中: 現行」で表示される。公開サイトのトップは現行表示、自動照会0件、コンソールエラーなし。**公開値は変えていない**。
  - **未実施**: 実 UID での本番描画、管理画面で達成ゲージを公開・プレビューする本番操作（公開値を変えない方針）、M3、隔離 DB。
- 残課題・既知の問題:
  - Phase 32 から継続の M3 と隔離 DB は未検証のまま。
  - 装備提案の希望ステータス（例「会心ダメ」）はサーバーのデータで、英語・中国語でも日本語のまま（現行カードと同じ、範囲外）。
  - 達成ゲージを本番で公開するかはユーザー判断（管理画面から切り替えられる）。
- 次フェーズへの申し送り:
  - 次は設計内 Phase 3（案C 推奨PTの隊列、§4.4 C・§6.3 手順3・§8.4 C1〜C3）。図鑑には formation 専用の PT 選択 state を追加し、legacy の全案一覧は保持する。Home の PT 変更が達成ゲージにも反映されることを C2 で確認する。
  - 登録表にキーを足すと、`server/displaySettings.test.ts` と `DisplaySettingsAdmin.test.tsx` の全キーを検証する期待値が落ちる。緩めずに新しいキーを明示して更新する。
  - 部品の受け渡し型は、ページの型を import せず構造的な型と総称型で受ける（ページの関数を props で渡すと型が合わないため）。
  - 自動操作の Chrome はウィンドウ幅を変えても表示幅が変わらない。レスポンシブの確認は同一ページ内の iframe（同一オリジン）に応答差し替えを入れて行う。表示設定の再取得は、取得後60秒たってから `window` へ `visibilitychange` を送ると起きる。
- 関連ドキュメント: [実装設計書_表示デザインの管理画面切替_2026-09-13.md](実装設計書_表示デザインの管理画面切替_2026-09-13.md) / [設計書インデックス.md](設計書インデックス.md)

### Phase 33: 翻訳フィードバックの管理画面から表示デザインへの導線を追加 — 完了

- 完了日: 2026-09-13
- 目的: Phase 32 の公開後、ユーザーから「管理者画面に表示デザインが表示されない」と報告があった。トップの「管理者」リンクは `/admin/feedback` へ移動し、この画面から `/admin`（入口カードあり）と `/admin/display` へ辿れなかったため、導線を追加する。
- 正本: [実装設計書_表示デザインの管理画面切替_2026-09-13.md](実装設計書_表示デザインの管理画面切替_2026-09-13.md) §4.2.5（本修正で追記）
- 実施済み: `FeedbackAdmin.tsx` のヘッダーに、管理者にだけ「管理者ポータル」「表示デザイン」へのリンクを追加（3言語の文言を `LanguageContext.tsx` に追加）。
- 反映コミット: `6062f5c`
- 検証結果: 再現テスト（管理者にリンクが出る / 非管理者に出ない）を追加し、修正前は失敗・修正後は pass。全回帰 pass（45ファイル・303テスト）/ 型検査 pass / Pages ビルド pass（entry 447.64kB）。本番の確認はマージ後に実施する。
- 残課題・既知の問題: Phase 32 の M3（本番 LCP）未計測は継続。
- 次フェーズへの申し送り: 管理画面を増やすときは、トップの「管理者」リンク先（`/admin/feedback`）から辿れるかを確認する。
- 関連ドキュメント: [実装設計書_表示デザインの管理画面切替_2026-09-13.md](実装設計書_表示デザインの管理画面切替_2026-09-13.md)

### Phase 32: 表示デザインの管理画面切替 Phase 1（切替基盤と凸の6段ステッパー） — 完了

- 完了日: 2026-09-13
- 目的: 見せ方改修6案を管理画面で切り替える基盤と、最初のバリアント D（凸の縦タイムライン・ゲーム別の記号）を実装する。キャラクターデータと `build.*` の応答は変えない。
- 正本: [実装設計書_表示デザインの管理画面切替_2026-09-13.md](実装設計書_表示デザインの管理画面切替_2026-09-13.md) の §6.1・§9.1・§9.3 / 設計レビュー [設計レビュー_表示デザインの管理画面切替_2026-09-13.md](設計レビュー_表示デザインの管理画面切替_2026-09-13.md)（No-Go。着手前に設計書 §11 で反映）
- 実施済み:
  - 基盤: `shared/displayVariants.ts`（登録表は `progressionStepper` のみ、取得状態で分ける解決規則、キャッシュ・プレビューの検証）、`site_display_settings` と `drizzle/0006_jazzy_lightspeed.sql`、`server/db.ts` の読み書き（複数キーは1トランザクション）、`server/displaySettings.ts`（60秒キャッシュ・世代番号・fail-safe）、`display` ルーター（settings / adminSettings / publish）。
  - クライアント: `DisplaySettingsProvider`（表示中タブは60秒ごと・表示に戻ったときに再取得、キャッシュが無い初回だけ最大600ms待機、`setPreview` / `clearPreview`、非管理者と確定したらプレビュー破棄）、プレビュー帯、`/admin/display`（公開直前の再取得による差分確認、保存失敗と再取得失敗の区別）、管理者ポータルの入口、`useAuth` の補助保存の例外吸収（DR-04）。
  - 案 D: `client/src/components/variants/ProgressionStepper.tsx`（縦のタイムライン、HSR=4本光の星・原神=点と線の星座・ZZZ=フィルム、上下キー・Home・End、準備中は操作不可）。UID照会と図鑑の凸セクションは既存 JSX の先頭に分岐を足すだけにした（D6）。
  - 設計から細部を変えた点（設計書へ反映済み）: 管理画面の結果は画面内のメッセージ、キャッシュは DB から読めた応答だけ保存、ログイン後の戻り先に `/admin/display` を許可、図鑑に `React` import。
- 反映コミット: `1c3ead3`（設計書・レビュー反映・着手）/ `be95217`（実装）、PR #49 → main `87fffbc`
- 検証結果:
  - 全回帰 pass（45ファイル・**301テスト**。Phase 31 から +5ファイル・+53テスト）/ 型検査 pass / `build:api` pass / Pages ビルド pass（500kB 警告なし）。CI（PR #49 の validate）pass、Pages デプロイ success。
  - M1: entry 440.60kB → **447.49kB**（gzip 138.82 → 141.11kB）。`ProgressionStepper` は Home と図鑑で共有する 8.13kB の独立チャンク、`DisplaySettingsAdmin` は 37.07kB。
  - ゴールデン比較: `699fb90` の baseline に対し、`--allow` / `--refresh` なしで**全11セクション一致**。
  - マイグレーション: 生成 SQL は `site_display_settings` の CREATE TABLE だけ。0005 の SQL と journal 既存行は無変更。
  - 変異チェック（KN-009）: 非管理者のプレビュー無視、公開取得の fail-safe、UID照会の legacy 分岐、取得成功時にキャッシュへ戻らない、の**4件すべて**で対応テストが落ちることを確認。
  - ローカル Google Chrome（DB なし）: 表示設定 API が `source: "default"`、図鑑・トップ・更新履歴が現行表示、コンソールエラーなし、トップの自動照会0件。応答の差し替えで、非表示タブでは再取得せず、表示に戻った時点で縦タイムラインへ切り替わることを確認。3ゲームの記号、原神アーロイの準備中表示、古い stepper キャッシュが残っても空の公開値で現行に戻ることを確認。
  - **本番**: Railway で `display.settings` が `source: "database"`（マイグレーション適用・読み取り成功）。管理者ログイン済みの Chrome で、プレビュー（帯・差分1件・星記号の縦タイムライン。プレビュー中も公開 API は現行）、公開（公開 API が `stepper` を返し、プレビューなしの図鑑で原神の星座記号）、すべて現行に戻す（公開 API が空、図鑑が現行表示）を確認。コンソールエラーなし。図鑑 API は total 252（HSR 84・原神 109・ZZZ 59）で不変。**締め時点の公開状態は全キー現行**。
  - **未実施**: M3（本番の LCP。自動操作の Chrome タブが `visibilityState: hidden` になり LCP を記録できず、タイマーも間引かれる）、隔離 MySQL での upgrade とトランザクション取り消し（U27。本番の適用と読み書きは成功）、UID照会（濃色背景）での縦タイムラインの実描画（実 UID 未使用。部品テストと分岐テストで代替）、モバイル幅の目視。
- 残課題・既知の問題:
  - M3 未計測。前提コミットの基準値も取れていないため、前面に表示した Chrome タブで、表示設定キャッシュ有り（待機なし）と無し（最大600ms待機）のトップの LCP を各3回測り、差が 600ms 以下かで判断する。
  - 縦タイムラインを本番で常時公開するかは未決定（ユーザー判断。管理画面から切り替えられる）。
  - HSR Dr.レイシオの凸効果名が API データ上「星魂1〜6」のまま（既存データ、本フェーズの範囲外）。
- 次フェーズへの申し送り:
  - Phase 2（案 A 達成ゲージ）は、G-A と U15〜U17 の決定が着手条件。登録表に1行足し、`Home.tsx` の既存 JSX は移動せずに分岐を足す。
  - 新しい UI 部品（radix など）を初めて使うと、開発サーバーの初回だけ Vite が依存を再最適化し、React の二重読み込みエラーが出る。再読み込みで解消し、本番ビルドには影響しない。
  - Windows の開発起動は `NODE_ENV=development PORT=3042 corepack pnpm exec tsx server/_core/index.ts`（`pnpm dev` は cmd 経由で失敗）。停止しても 3042 番の node プロセスが残るため、待ち受けプロセスを止める。
  - 自動操作の Chrome タブは非表示扱い。「表示に戻ったときの再取得」を試すには、ページ内で `visibilityState` を visible に見せかけて `window` へ `visibilitychange` を送る（React Query は `window` のイベントを見る）。
  - vitest は古い JSX 変換を使うため、テストで描画するコンポーネントは `React` を import する。
  - 採用が確定したキーは、legacy と分岐を撤去する修正設計書を別途起こす。
- 関連ドキュメント: [実装設計書_表示デザインの管理画面切替_2026-09-13.md](実装設計書_表示デザインの管理画面切替_2026-09-13.md) / [設計レビュー_表示デザインの管理画面切替_2026-09-13.md](設計レビュー_表示デザインの管理画面切替_2026-09-13.md) / [設計書インデックス.md](設計書インデックス.md)

### Phase 31: 第19バッチ（クラレッタの追加・設計書 Phase C） — 完了

- 完了日: 2026-09-13
- 目的: 設計書 Phase C（D5）として、ZZZ の新特性「鋭御」のクラレッタ（`zzz:1611`）をカタログへ追加する（251→252名）。鋭御のコード側対応は Phase 30 で公開済み。
- 正本: [修正設計書_UID照会の比較値是正とZZZ鋭御追加_2026-09-11.md](修正設計書_UID照会の比較値是正とZZZ鋭御追加_2026-09-11.md) の Phase C と §10 Q2〜Q4。根拠は [batch-19-research-notes.md](batch-19-research-notes.md)。
- 実施済み:
  - カタログ（`characterGuideCatalog.ts`）へ追加し、`ZZZ_RUNTIME_PROFESSIONS` へ `Armorer` を登録。identity スナップショットは第18バッチ時点で `zzz:1611` を持つため差分なし。
  - **Q2（目標値の前提）**: 2つ目の出典（Game8）で「開幕ステータス＝戦闘外の画面値」を確認できたため登録した。防御力 2,451 / 1,800 / 1,000（GameWith の開幕値と、Game8 が挙げる棘まとう薔薇4セット効果の開幕防御力の閾値）、会心率 129 / 129 / 128.9。
    **会心ダメージは登録しない**（GameWith は98%を挙げるが Game8 は「暴傷ダメージが適用されるため盛る意味がない」とし、出典が割れているため）。理由は `targetContext` に記載。
  - **Q3（心象映画）**: 効果本文が2出典で全6段一致したため `curated` で登録。**名称が2出典で割れていた**ため、3つ目の出典（Game8 英語版の心象映画名）で裏を取り、英語名と対応する Game8 日本語版の表記を採用した。`targetChanges` は無条件の固定効果が無いため全段で空。
  - **Q4（推奨PT）**: Game8 の編成ページで3案目の実名編成を確認できたため3案で登録（案1・2 は GameWith とも一致）。メンバー名はカタログの正規名へ揃えた。
  - 締め: `characterBatches.ts` の第19バッチ、`characterGuideMetadata.ts` の日付ブロック、`guideUpdateHistory.ts` のバッチイベントとサイトイベント、`docs/all-character-update-ledger.md` を更新。
- 検証結果:
  - 型検査 pass / 全回帰 pass（40ファイル・**248テスト**。第19バッチの回帰4件を追加）/ `build:api`・Pages ビルド pass。
  - **推奨PT影響レビュー**（`party-impact.mjs --added zzz:クラレッタ`）: 言及済みPT **0件**・置換候補0件で、埋めるべき行なし（根拠メモへ転記済み）。
  - ゴールデン比較: `--allow zzz:クラレッタ` でキャラクター単位の7セクションが許容差分のみ。`--allow` が効かない4セクション（`ledger` / `history` / `referenceCatalog` / `metadata`）は機械照合で「追加はクラレッタ1名分と件数更新だけ・既存は不変」を確認してから baseline を取り直した。
  - 監査: 整合性警告0件、経路不一致0件（全252名）、`check-r8` の catalog-id mismatch 0 / name unresolved 0。
  - ローカル Chrome: 図鑑でクラレッタの推奨ビルド・目標2件・根拠説明・推奨PT3案・心象映画6段を確認。コンソールエラーなし。
- 反映コミット: `251639b`（PR #44 → main `7849c0e`）
- **本番**（Pages デプロイ成功・Railway 反映後）:
  - API `build.referenceCatalog` が total **252** / ZZZ **59**。`build.reference`（クラレッタ）が batch 19・`curated:batch19:zzz:クラレッタ`・目標2件・心象映画 curated 6段・推奨PT3案を返す。
  - Chrome で公開図鑑のクラレッタ（推奨ビルド・目標2件・根拠説明・推奨PT3案・心象映画6段）と `/updates` の第19バッチイベントを確認。コンソールエラーなし。
- 残課題・既知の問題:
  - **これで設計書『UID照会の比較値是正とZZZ鋭御追加』の Phase A〜E をすべて実装・公開した**（Phase A: フェーズ29、Phase B・D・E: フェーズ30、Phase C: 本フェーズ）。
  - 会心ダメージの目標は出典が割れたまま未登録。Game8・GameWith のどちらかが方針を変えたら再確認する。
  - 心象映画の日本語名は出典照合による判断（ZZZ には公式ゲームデータ源が無い）。日本語の公式データが見つかったら照合し直す。
  - 未登録の保留: ロクシー（`zzz:1621`、2026-09-30 開始予定）、アベンチュリン・波と戯れる夏、スンナ、原神4名（Enka に ID が無い）。
  - 推奨PTの役割ラベルが並び順で決まる制約（Phase 21 から既知）は本フェーズでも未対応。
- 次フェーズへの申し送り:
  - 新キャラクターの数値目標は、出典が割れたキーだけを落として残りを登録してよい（落とした理由は `targetContext` に残す）。
  - ZZZ の凸は名称が出典で割れることがある。英語版の出典で裏を取ると判断できる。
  - `--allow` が効かない4セクション（`ledger` / `history` / `referenceCatalog` / `metadata`）は、追加分と件数更新だけであることを機械照合してから baseline を取り直す。
- 関連ドキュメント: [batch-19-research-notes.md](batch-19-research-notes.md) / [all-character-update-ledger.md](all-character-update-ledger.md) / [修正設計書_UID照会の比較値是正とZZZ鋭御追加_2026-09-11.md](修正設計書_UID照会の比較値是正とZZZ鋭御追加_2026-09-11.md)

### Phase 30: UID照会の比較値是正（設計書 Phase B・D・E） — 完了

- 完了日: 2026-09-12
- 目的: 設計書の Phase E（装備提案の部位選択）・Phase B（ZZZ 新特性「鋭御」と防御力/HP 比較）・Phase D（Enka 経路の取りこぼし是正）を実装する。
- 正本: [修正設計書_UID照会の比較値是正とZZZ鋭御追加_2026-09-11.md](修正設計書_UID照会の比較値是正とZZZ鋭御追加_2026-09-11.md)。
  レビュー判定は Phase D が Go、Phase B・E は BDE-01〜03 を反映済み（[Phase B・D・E レビュー](設計レビュー_UID照会の比較値是正とZZZ鋭御追加_PhaseB-D-E_2026-09-12.md)）。
- 実装順: **Phase E → Phase B**（B の D3 は E が無いと新たな誤提案を生む）。Phase D は独立。
- 実施済み:
  - **Phase E（E3〜E5）**: ガイドが対象ステータスの主ステータス欄を持つときだけ「主ステータスを変更」を提案する。持たないときは部位を決め打ちせず「サブステータスを厳選」・`slotScope: "any"` を返す。
    既定部位（`defaultSlotsForStat`）は全251名の計測で使用75件すべてがガイドの推奨と衝突していたため削除した。
    サブステータスに存在しないキー（ZZZ の衝撃力・異常掌握・エネルギー自動回復・貫通率、HSR の EP回復効率）は装備アクション自体を返さない（E4）。
    画面は `slotScope` で分岐し、部位ラベルと説明文を3言語で出す（E5）。
  - **Phase B（D1・D2・D3・D6・D7）**: 特性 `Armorer` の表示名「鋭御」、`ZZZ_PROFILES.def`（目標は空。理由を `targetContext` に明記）、
    `zzzProfileIdFor` の分岐、比較値への `hp` / `defense` / `defPercent` 追加、`generatedZzzGuide` がプロファイル固有の説明を上書きしない修正、ZZZ の希望主ステータス名を割合表記にする対応。
  - **Phase D（E1・E2）**: Enka の `_flat.props` がサブに使う `Base` なし型（`field: ""`）を型名で `StatKey` へ対応付け、表示も百分率にした。
    `relic_sets.json` の段階別 `properties`（無条件効果）を装備数に応じて加算し、解決できないときは `unavailable`（fail closed）。
  - **付随して直した不具合**: 装備提案の希望ステータス名が、HSR のキャラクターにも原神の語「元素チャージ」を出していた（`STAT_MAIN_LABELS` の先頭が照合用の語だったため）。ゲーム別の呼び名を返すようにした。
- 検証結果:
  - 型検査 pass / 全回帰 pass（40ファイル・**244テスト**。新規19件）/ `build:api` pass / Pages ビルド pass。
  - ゴールデン比較: **全11セクション一致**（設計どおり Phase B・D・E は図鑑応答を変えない）。監査の整合性警告0件・経路不一致0件（251名）。
  - **実データ照合（設計 §8・§9）**: 同一 UID を MiHoMo 経路と Enka 経路で正規化し、8名×7項目で**不一致0件・最大差1.00**（端数のみ）。
    Phase A 時点で残っていた会心率・会心ダメージの数十%のずれが解消し、「両経路が端数を除いて一致する」を満たした。
  - 変異チェック（KN-009）: E3・E4・E5・D1・D2・D3・D6・D7・E1・E2 の**10種すべて**で、外すと対応するテストが落ちることを確認。
  - ローカル Chrome: HSR の UID 照会で最終値（緋英の速度138・会心61.2%・EP回復効率119.4%）と装備アクションを確認。コンソールエラーなし。
- 反映コミット: `2f2d176`（PR #42 → main `7084b2d`）。設計書の BDE 反映（`eda5ad3`）と main マージ（`84f94f6`）も同PRに含む。
- **本番**（Pages デプロイ成功・Railway 反映後）:
  - API `build.lookup`（HSR）が `dataSource: MiHoMo` / `statsStatus: final` を返し、装備アクションに `slotScope` が入る。
  - API `build.reference`（ZZZ シーザー）が `impact` / `hp` / `defense` の3目標を返す。`/api/health` 200。
  - Chrome で公開サイトの UID 照会を確認（緋英: HP 2950 / 攻撃力 2083 / 防御力 1103 / 速度 138 / 会心率 61.2% / 会心ダメージ 254.3% / EP回復効率 119.4%）。コンソールエラーなし。
- 残課題・既知の問題:
  - **Phase C（クラレッタの第19バッチ登録）が未着手**。出典2件での照合が要るため、`hoyoverse-character-update` のモード C で別途実施する。現状 ZZZ に `Armorer` のキャラクターはカタログに居ないため、鋭御の共通ガイドは実際には使われていない（Phase C で使われるようになる）。
  - 推奨PTのメンバーの役割ラベルが並び順で決まる制約（Phase 21 から既知、45名に影響）は本フェーズでも未対応。
- 次フェーズへの申し送り:
  - 装備提案は「ガイドが主ステータス欄を持つか」で分岐する。新しい目標キーをガイドへ足すときは、そのキーがそのゲームのサブステータスに存在するか（`SUBSTAT_CAPABLE_KEYS`）も確認する。
  - 画面の英語・中国語はサーバーの `reason` を使わず再生成するため、`EquipmentAction` に構造を足したらクライアントの分岐も同時に直す。
  - 取得元をまたぐ値は、必ず同一 UID の両経路照合で突き合わせる。
- 関連ドキュメント: [修正設計書_UID照会の比較値是正とZZZ鋭御追加_2026-09-11.md](修正設計書_UID照会の比較値是正とZZZ鋭御追加_2026-09-11.md) / [Phase B レビュー](設計レビュー_UID照会の比較値是正とZZZ鋭御追加_PhaseB_2026-09-12.md) / [Phase B・D・E レビュー](設計レビュー_UID照会の比較値是正とZZZ鋭御追加_PhaseB-D-E_2026-09-12.md)

### Phase 29: UID照会の比較値是正（設計書 Phase A: HSR MiHoMo 経路の最終値化） — 完了

- 完了日: 2026-09-12
- 実装した内容:
  - H1〜H4: `parseCharacter` の表示・比較を `statistics`（無ければ `attributes` + `additions` の合算）へ切り替え。照合を表示名の正規表現から `field` の対応表（`enkaFallback.ts` から共有）へ変更。表示ラベルを「基礎HP」→「HP」に是正。最終値を作れないときは `properties` へ戻らず `statsStatus: "unavailable"` で比較を保留する。
  - H5: EP回復効率を `(1 + sp_rate) * 100` で基礎100%込みにし、カフカの目標を単位換算（119.4 / 119.4 / 100）。
  - H6（DR-01）: 許容差 `meetsTarget`（1e-9）を達成判定と不足抽出の両方へ適用（MiHoMo・Enka・原神・ZZZ）。丸めて 0 になる不足は「あと 0.1%未満」と表示する。画面側（`Home.tsx`）の推奨PT調整後の判定にも同じ規則を入れた。
  - H1-b（自動レビュー DR-05）: 最終値を1名も作れない MiHoMo 応答はキャッシュ前に例外にして Enka へ回し、一部欠落はキャッシュしない（Enka 側の `isEnkaResultComplete` に揃えた）。
  - 更新履歴にサイト全体イベントを1件追加。MiHoMo 経路の注記を3言語で追加。
  - 削除: 使われなくなった `STAT_MATCHERS` / `statNumber` / `allStatsFor`（MiHoMo 専用の照合・表示）。
- 反映コミット: `f954888` / `0c6fd34` / `e32f88e`（PR #41 → main `7619594`）
- 検証結果:
  - 型検査 pass / 全回帰 pass（40ファイル・225テスト）/ `build:api` pass / Pages ビルド pass。
  - ゴールデン比較: 差分はカフカの `energyRecharge` 目標3水準・`targetContext` と、新規サイトイベント1件だけ。`--allow` が効かない `history` は「追加1件を除くと baseline と完全一致」を機械照合してから baseline を取り直した。
  - 監査: 整合性警告0件・経路不一致0件（251名）。
  - 変異チェック（KN-009）: `statistics`→`properties`、H5 の換算、H6 の許容差、H1-b の判定をそれぞれ外すと対応テストが落ちることを確認。
  - ローカル Chrome: 図鑑のカフカ（119.4%）と `/updates` の告知を確認。
  - **本番**: API の `build.reference`（カフカの目標が 119.4 / 119.4 / 100）と `build.lookup`（`dataSource: MiHoMo` / `statsStatus: final` / 緋英の速度 138・HP 2950・攻撃力 2083・会心率 61.2%）を確認。
  - 実データ照合: MiHoMo 経路は取得元の最終値と一致。HP・攻撃力・防御力は Enka 経路とも ±1 で一致した。
- 残課題・既知の問題:
  - 実データ照合で、**Enka 経路が遺物のサブ（`Base` なし型）と遺物セット効果を取りこぼしている**ことが判明した。Phase 18 からの既存不具合で本フェーズの変更が原因ではないが、設計 §9 の「両経路が一致する」は本フェーズ時点で未達だった。→ **Phase 30（設計書 Phase D）で解消済み**。
  - 装備提案がガイド自身の推奨主ステータスと衝突する問題（全251名で75件）も、この過程で判明した。→ **Phase 30（設計書 Phase E）で解消済み**。
- 次フェーズへの申し送り: MiHoMo は `statistics` が戦闘外の最終値。`properties` は加算分だけなので表示・比較に使わない。取得元をまたぐ値は、必ず同一 UID の両経路照合で突き合わせる（片側だけ直すと差が残る）。
- 関連ドキュメント: [修正設計書_UID照会の比較値是正とZZZ鋭御追加_2026-09-11.md](修正設計書_UID照会の比較値是正とZZZ鋭御追加_2026-09-11.md) / [設計レビュー（初回）](設計レビュー_UID照会の比較値是正とZZZ鋭御追加_2026-09-12.md)


### Phase 27: 第18バッチ（監査残課題の解消と新キャラクター3名の追加） — 完了

- 完了日: 2026-09-11
- 実装した内容:
  - 監査の残課題を公開ガイドで調査し（[batch-18-research-notes.md](batch-18-research-notes.md)）、ユーザー承認のうえ申し送り1〜4を1バッチで実施した。
  - (1) 推奨PTの別表記4件を正規名へ: パーマンソー・テラエ→丹恒・騰荒（英語名 Permansor Terrae）、アシュヴェイル→不死途（Ashveil）、イーシェン→儀玄、ザオ→照。
  - (2) ジェイド・ジェパードの個別ガイドと、出典で実名が確認できた推奨PTを登録。台帳上は第10バッチのまま、メタデータ（`curated:batch18:hsr:*`）と個別の更新イベントで記録。
  - (3) HSR 3名（千冶・刃 hsr:1507 / 姫子・旅立ち hsr:1510 / ロビン・夏空の歌 hsr:1512）をカタログへ追加（248→251名）。星魂は StarRailRes の公式ゲームデータ（jp/en/cn）を正本にした（Game8 の要約に誤りがあったため）。モーテナックス／モルトナックス・ブレード（Mortenax Blade の音写）を千冶・刃へ統一。推奨PT影響レビュー19行を全行埋めた。
  - (4) 監査の `classifyMissingName()` の「既存名を含む＝派生」判定を廃止し、お試しと調査済みの除外（炎神 genshin:11000046、銀狼LV.999）だけを対象外にした。
  - 付随して直した不具合: `refresh-identity-snapshot.mjs --write` が `EXCLUDED_EMPTY_GENSHIN_CATALOG_IDS` の export を消していた / 監査出力と図鑑の見出し（3言語）が「248」固定 / 台帳ドキュメントの第15バッチ行が古いまま。
- 反映コミット: `2c2c09d`（調査記録）/ `3c72be3` / `0265a91` / `54e625c` / `340cfd6`（PR #38 → main `ea915d0`）
- 検証結果:
  - 型検査 pass / 全回帰 pass（40ファイル・216テスト）/ `build:api` pass / Pages ビルド pass。
  - 監査: 整合性警告 1→0件、P2 7→5名（残りは凸データが公式未実装の5名）、経路不一致0件（全251名）、unknown 0件。P0 は保留中の3名（アベンチュリン・波と戯れる夏／クラレッタ／ロクシー）だけ。
  - ゴールデン比較: 追加3名と、表記統一のギルガメッシュ・黄泉（名前だけの差分を機械照合）以外は全セクション不変。台帳・図鑑一覧・メタデータ・履歴も既存キャラクター分は不変であることを確認してから baseline を取り直した。
  - 変異チェック: ジェイドのガイド・姫子・旅立ちの凸データを外すと追加テストが落ちることを確認（KN-009）。
  - ローカル実機（Chrome、ローカル API + ローカル Pages）: 3名の図鑑表示・英語表示・更新履歴を確認。
  - **本番**（Pages デプロイ成功・Railway 反映後）: API の `build.referenceCatalog` が total 251。`build.reference` で3名が batch 18・`curated:batch18:hsr:*`・推奨PT3案・凸 curated 6段、ジェイドが `curated:batch18:hsr:ジェイド` と個別目標を返す。Chrome で公開図鑑（「251キャラ収録」・千冶・刃がバッチ18）、ジェイドの目標値、`/updates`（第18バッチのイベント・251 / 251 精査完了）を確認。コンソールエラーなし。
- 残課題・既知の問題:
  - 保留: アベンチュリン・波と戯れる夏（2026-09-12 実装）、ロクシー（2026-09-30 開始）、クラレッタ（実装済みだが新特性「鋭御」の対応が要るため別設計）、スンナ（未実装）、原神4名（イルーガ・茲白・リンネア・オデット。実装済みだが Enka に ID が無い）。
  - 推奨PTのメンバーの役割ラベルが並び順で決まる（`manualMember` の位置ラベル）ため、新規3名の案でもサポーターが「主力・副火力」と出る箇所がある。Phase 21 から既知の制約（45名に影響）で本フェーズでは直していない。
  - 別セッションの PR #37（`Vary: Origin` の修正、Phase 28）が本フェーズと並行して起票されている。`docs/実装ログ.md` と設計書インデックスを双方が編集しているため、後からマージする側で衝突の解消が要る。
- 次フェーズへの申し送り: 新キャラクターは「名前に既存名を含むか」では判定しない（監査は明示リストで除外）。星魂本文は公式ゲームデータを先に引き、ガイドサイトの要約は照合用に使う。アベンチュリン・波と戯れる夏は 9/12 以降にモード C で追加できる。
- 関連ドキュメント: [batch-18-research-notes.md](batch-18-research-notes.md) / [all-character-update-ledger.md](all-character-update-ledger.md)

### Phase 26: API 応答の圧縮導入（計画書 §4.3 #5） — 完了

- 完了日: 2026-09-11
- 実装した内容:
  - `build.guideHistory` が 414,137 バイトを**無圧縮**で返していた（`Content-Encoding` なし、`compression` は依存にも無かった）。計画書が想定した「イベント本文の重複」ではなく圧縮ミドルウェア不在が主因だったため、`compression` 1.8.1 を追加し、`server/_core/index.ts` の CORS の直後・ボディパーサの前に `app.use(compression())` を1行入れた。応答の JSON は不変。
  - 回帰ゲート `server/_core/compression.test.ts` を新設。`index.ts` を実プロセスで起動し、`node:http` で受信バイト数を測る（fetch は自動解凍して転送量が見えないため）。ミドルウェアを外すと4件中2件が落ちることを確認済み。CI（Linux）でも pass。
  - 計画書が提案していたイベント本文の正規化は、圧縮後の追加削減が約8kB にとどまるため見送り（設計 §5 に再検討条件）。
  - **設計の前提誤りを発見・訂正**: CORS の `Vary: Origin` は圧縮導入前から tRPC が `vary: trpc-accept` で上書きして消えていた（本番で確認）。`appendHeader` でも直らない。圧縮とは無関係の既存不具合のため本フェーズでは直さず、設計書 §2 D1 を訂正・受入条件2 を差し替え・§6 残課題へ移した（design-doc で追記）。
- 反映コミット: `39b5bb4`（PR #35 → main `d73c47b`）。設計書は PR #34 → `a0fe871`。
- 検証結果:
  - 型検査 pass / 全回帰 pass（40ファイル・206テスト。新規4件）/ `build:api` pass / Pages ビルド pass / ゴールデン比較 全11セクション一致。
  - **本番 API**（Railway 反映後）: `build.guideHistory` 無圧縮 414,137 → gzip 18,928 / **br 13,902 バイト**（96.6% 減）。gzip を解凍した本文が無圧縮の本文と完全一致。`build.referenceCatalog` は br 2,309 バイト。`/api/health` 200。
  - **ブラウザと同じ要求**（Chrome が送る `batch=1` の URL・`Accept-Encoding: gzip, deflate, br, zstd`・Pages の Origin）: 414,139 → **13,877 バイト**、`Content-Encoding: br`、`Access-Control-Allow-Origin` 維持、`Vary: trpc-accept, Accept-Encoding`。
  - **`/updates` を Chrome で確認**: サイト全体の更新・キャラクター別更新 248件（表の行数 248）が従来どおり表示。コンソールエラーなし。
- 残課題・既知の問題:
  - `Vary: Origin` の欠落（既存不具合）。実害は低い（前段にキャッシュする CDN なし・応答に `Cache-Control` なし）が、CORS のキャッシュ正しさの問題。tRPC の `responseMeta` で `vary` を返す形が候補。別の修正設計で扱う（設計書 §6）。
  - イベント本文の正規化と、画面が描画していない `summary`（85.8kB）の除去は見送り（設計 §5）。
  - これで計画書 §4.3 のコード側の項目はすべて回収済み。残りはキャラクターデータ側（`hsr:ジェイド` / `ジェパード` の個別ガイド、カタログ未登録6件、`upcoming` 11表記）。
- 次フェーズへの申し送り: 圧縮は `server/_core/index.ts` の1行で、`compression.test.ts` が並びを守る。ミドルウェアを足すときは圧縮より前に応答を書き切るもの（ヘルスチェック等の早期 return）を置かない。
- 関連ドキュメント: [修正設計書_更新履歴APIの転送量削減_2026-09-10.md](修正設計書_更新履歴APIの転送量削減_2026-09-10.md) / [リファクタリング計画書_2026-09-08.md](リファクタリング計画書_2026-09-08.md) §4.3 #5

### Phase 25: Pages のルート単位コード分割（計画書 §4.3 #6） — 完了

- 完了日: 2026-09-10
- 実装した内容:
  - 全7ルートの画面が1本の JS に入っており、`/` を開くだけで管理画面まで転送されていた。`client/src/App.tsx` の6ルート（`/`・`/characters`・`/updates`・`/feedback`・`/admin`・`/admin/feedback`）を `React.lazy` へ変え、`<Switch>` を `<Suspense>` で包んだ。
  - `NotFound` は catch-all かつ 2.1kB と小さいため同期のまま残した（遅延にすると存在しないパスで 404 の前にフォールバックが一瞬出る）。
  - フォールバックは外枠だけの空要素（`min-h-screen`）。文言やスピナーを置くと `/` の初回表示でちらつくため。
  - `manualChunks` によるベンダー分割はしていない（全ルート共有のため初回転送量が減らず、チャンクだけ増える）。
- 反映コミット: `258a8c4`（PR #32 → main `702624e`）
- 検証結果:
  - Pages ビルド: entry `529.98 kB` → **`439.07 kB`**（gzip 157.86 → 138.47 kB）。**500kB 超の警告が消えた**。画面は Home 40.66 / CharacterCatalog 14.87 / FeedbackAdmin 11.98 / GuideHistory 9.02 / AdminHome 8.51 / TranslationFeedback 5.19 kB の独立チャンクへ分離。
  - 型検査 pass / 全回帰 pass（39ファイル・202テスト。テストは画面を直接 import しており App を経由しないため変更なし）/ `build:api` pass / ゴールデン比較 全11セクション一致。
  - ローカルの Pages ビルドを `vite preview` で Chrome 確認: `/`・`/characters`・`/updates`・`/feedback`・`/admin` が表示、存在しないパスは 404 が即時表示、チャンク取得エラーなし（コンソールのエラーはローカルに API が無いことによる照会失敗のみ）。
  - **本番 Pages を Chrome で確認**（デプロイ run 34488010832 成功後）: 直リンクで `/`・`/characters`・`/updates`・`/feedback`・`/admin` が実データ付きで表示。entry は `assets/index-mBCvy-kL.js` 439,176 バイト。ルート遷移で `AdminHome-*.js` / `TranslationFeedback-*.js` が 200 で取得され、アプリ内リンクの遷移でも `CharacterCatalog-XwyT1lef.js` を 200 で取得して描画。チャンク関連のコンソールエラーなし。
- 残課題・既知の問題:
  - `/` は landing route だが遅延化したため、初回表示が画面チャンク1往復ぶん遅くなる（設計 D3 で受容）。
  - 計画書 §4.3 の #5（`build.guideHistory` 非圧縮 210KB）は未着手。これで §4.3 の未着手はキャラクターデータ側（ジェイド・ジェパードの個別ガイド、未登録6件）と #5 だけになった。
- 次フェーズへの申し送り: ルートを追加するときは `App.tsx` で `lazy()` に揃える。同期 import を1本足すと entry が再び 500kB へ寄る。
- 関連ドキュメント: [修正設計書_Pagesのルート単位コード分割_2026-09-10.md](修正設計書_Pagesのルート単位コード分割_2026-09-10.md) / [リファクタリング計画書_2026-09-08.md](リファクタリング計画書_2026-09-08.md) §4.3 #6

### Phase 24: リクエストボディ上限の縮小（計画書 §4.3 #4） — 完了

- 完了日: 2026-09-10
- 実装した内容:
  - テンプレート既定の `express.json({ limit: "50mb" })` / `urlencoded` を **256kb** へ縮小した。実需の最大は `feedback.submit` の zod 上限（4,055文字＝最悪32KB弱）と GitHub コード交換（約100バイト）だけで、50MB を受け付ける理由が無く、未認証の `POST /api/trpc/*` で 1 リクエスト 50MB をメモリへ載せられる状態だった。
  - 上限超過時、`/api` 配下は `{ "error": "payload_too_large", "limit": "256kb" }` を 413 の JSON で返す（既定の HTML 応答では tRPC クライアントがパースできず原因が分からないため）。それ以外のパス・それ以外の例外は素通し。
  - 設定を `server/_core/bodyLimits.ts` へ切り出した（`index.ts` は末尾で `startServer()` を実行するためテストから import できない）。実際に express を起動して 413 / 200 を確認する回帰テストを追加。
- 反映コミット: `0ea3ac5`（PR #30 → main `28f7424`）
- 検証結果:
  - 型検査 pass / 全回帰 pass（39ファイル・202テスト。新規4テスト）/ `build:api` pass / Pages ビルド pass
  - ゴールデン比較: 全11セクション一致（baseline 更新なし＝応答内容は不変）。
  - ローカル API 実測（`API_ONLY=true` / port 3042）: `/api/health` 200、`POST /api/trpc/feedback.submit` へ 512KB を投げて **413 + `application/json`** を確認。上限内のボディはハンドラまで到達（ローカルは DB 未接続のため保存段階で 500）。
- 残課題・既知の問題:
  - レート制限は未導入（本設計の対象外。導入は運用方針の決定が要る）。
  - 計画書 §4.3 の #5（`build.guideHistory` 非圧縮 210KB）・#6（Pages 500kB 超チャンク）は未着手。Pages ビルドの 500kB 警告は本フェーズでも出続けている。
- 次フェーズへの申し送り: ボディ上限は `server/_core/bodyLimits.ts` の `REQUEST_BODY_LIMIT` 一箇所。大きな本文を受ける API を足すときはこの値と `bodyLimits.test.ts` を先に見直す。
- 関連ドキュメント: [修正設計書_リクエストボディ上限の縮小_2026-09-10.md](修正設計書_リクエストボディ上限の縮小_2026-09-10.md) / [リファクタリング計画書_2026-09-08.md](リファクタリング計画書_2026-09-08.md) §4.3 #4

### Phase 23: 推奨PTメンバー名の表記統一（スキル再設計 §4.3 ①） — 完了

- 完了日: 2026-09-10
- 実装した内容:
  - 親リポ設計書 §4.3 タスク① を実施し、推奨PTのメンバー名16表記・31箇所をカタログ名へ統一した（`server/partyRecommendations.ts`）。旧表記・新表記・根拠は [party-member-notation-normalization-2026-09-10.md](party-member-notation-normalization-2026-09-10.md) に記録した。
  - `PARTY_MEMBER_ALIASES` を空にし（設計書の完了条件）、元素情報を残す `genshin:旅人（草）` は `NON_CATALOG_PARTY_MEMBERS` へ「正当な派生」として登録した。
  - 監査の整合性警告のうち「`docs/all-character-update-ledger.md` が第17バッチを記載していない」を解消（同ドキュメントが第17バッチだけ「パッチ」表記だったため4箇所を統一）。
- 反映コミット: 本フェーズのコミット（PR は下記）。
- 検証結果:
  - 型検査 pass / 全回帰 pass（38ファイル。`batch14CuratedGuides.test.ts` の期待値1件を正規名へ更新）/ `build:api` pass / Pages ビルド pass
  - ゴールデン比較: 差分は `parties` / `referenceCore` の21名のみで、**変わったのは `members[].name` だけ**（案の数・順序・id・rank・出典・役割ラベル・targetChanges は不変）であることを機械照合してから baseline を更新した。
  - 監査 CLI: 整合性警告が2件→1件（残りは `hsr:ジェイド` / `hsr:ジェパード` の個別ガイド欠落）。`unknown` 0件・経路不一致0件を維持。
- 残課題・既知の問題:
  - `hsr:ジェイド` / `hsr:ジェパード` の個別ガイド欠落（第10バッチ扱いなのにロール共通値のまま）。出典調査が要るため `hoyoverse-character-update` のモード A で別途。
  - カタログ未登録の実装済みキャラクター6件（`hsr:1512 ロビン・夏空の歌` / `hsr:1513 アベンチュリン・波と戯れる夏` / `hsr:1510 姫子・旅立ち` / `hsr:1507 千冶・刃` / `zzz:1611 クラレッタ` / `zzz:1621 ロクシー`）はモード C で追加する（§4.3 ③）。
  - `UPCOMING_PARTY_MEMBERS` 11表記は実装待ちのまま。`hsr:モーテナックス・ブレード` と `モルトナックス・ブレード` の同一性は未確認。
- 次フェーズへの申し送り: 新しい表記ゆれを持ち込むと `partyMemberAliases.test.ts` が落ちる。別名表は空を保ち、データ側を正規名で書く。
- 関連ドキュメント: [party-member-notation-normalization-2026-09-10.md](party-member-notation-normalization-2026-09-10.md) / 親リポ `my-app2/docs/修正設計書_hoyoverseスキル再設計_2026-09-08.md` §4.3 ①

### Phase 22: hoyoverse 運用スキル再設計 §4.1（親リポ設計書 2026-09-08） — 完了

- 完了日: 2026-09-10
- 実装した内容:
  - 親リポ `docs/修正設計書_hoyoverseスキル再設計_2026-09-08.md` の §4.1（リポジトリ側の判定基盤）を実装した。§4.2（スキル本体の改訂・`hoyoverse-character-curation` 撤去）は親リポ Phase 5 で実施。
  - `server/partyMemberAliases.ts` / `.test.ts`: 推奨PTのメンバー名をカタログ名へ解決する表と `resolvePartyMember()`。カタログ外39表記を alias 16 / variant 12 / upcoming 11 に分類。全248名の全案の全メンバーが `unknown` にならないことを回帰ゲートで固定した（表示には使わず監査専用）。
  - `server/partyImpact.ts` / `.test.ts`: `partyImpactFor()` と `formatPartyImpactMarkdown()`。新キャラクター追加時に「既に言及している推奨PT」をチェックリストで出す。原神は同役割候補を出さない（設計 §10-2 の決定）。
  - `server/auditRules.ts` / `.test.ts`: ガイド種別判定・監査レコード・P0〜P3 分類・次候補・メンバー名集計に加え、**図鑑と UID 照会の経路一致検査 `routeMismatches()`** を新設。
  - `scripts/audit/audit-characters.mjs`: スキル配下から移設・改訂。判定は `auditRules` へ委譲し、出力に経路一致・メンバー名・実装待ちの言及を追加。P0 行に影響レビュー待ち件数を併記。
  - `scripts/audit/party-impact.mjs` / `refresh-identity-snapshot.mjs`: 影響レビューの出力と、公開メタデータとの差分表示（`--write` でのみ書き換え）。
  - `scripts/golden-snapshot.mjs`: `--allow <game:name,...>` を追加。キーがキャラクター名のセクションだけキー単位で差分を許容し、`metadata` / `ledger` / `history` はセクション単位のまま。
- 着手前に確定した設計判断（ユーザー確認）:
  - §10-1 メンバー名の分類。identity スナップショット照合・Game8・公開情報で36件を根拠付きで確定し、`hsr:永夜`→`長夜月`、`zzz:パン`→`潘引壺` はユーザー判断で alias 確定。
  - §10-2 原神の同役割判定は行わない（`sameRoleOwners` は常に空）。これに伴い §4.1.4 の identity スナップショット型拡張（element / weaponType）は不要になったため実装しない。
- 反映コミット: `4a1555f`（解決表・影響レビュー）/ `01fc468`（auditRules）/ `679d851`（監査CLI 3本・--allow）と本コミット。
- 検証結果:
  - 型検査 pass / 全回帰 pass（38ファイル。partyMemberAliases 9件・partyImpact 7件・auditRules 10件を追加）/ `build:api` pass / Pages ビルド pass
  - ゴールデン比較 全11セクション一致（**公開 API の応答は不変**。解決表・影響レビューは監査専用で表示に使わない）
  - CLI スモーク pass: `audit-characters.mjs --offline` が 248/248・経路不一致0件・`unknown` 0件・実装待ちの言及11件を出力。`party-impact.mjs --added genshin:オデット` が言及済み2案を出力。`refresh-identity-snapshot.mjs` は差分表示のみでファイル不変。
  - `--allow` の動作確認 pass: 許容した名前の差分だけを無視し、他の名前が変われば exit 1 で落ちる。
- 実測で判明したこと（次の作業の入力）:
  - 公開メタデータに**カタログ未登録の実装済みキャラクターが4件**ある: `hsr:1512 ロビン・夏空の歌` / `hsr:1513 アベンチュリン・波と戯れる夏` / `zzz:1611 クラレッタ` / `zzz:1621 ロクシー`。加えて `hsr:1510 姫子・旅立ち` / `hsr:1507 千冶・刃` も未登録。`hoyoverse-character-update` のモード C の初回対象になる。
  - 監査の整合性警告が2件出る: `docs/all-character-update-ledger.md` が第17バッチ未記載、`hsr:ジェイド` / `hsr:ジェパード` が第10バッチ扱いなのに個別ガイドが無い（いずれも従来からの課題）。
- 残課題・既知の問題:
  - 設計書 §4.3 の運用タスク①（メンバー名の表記ゆれを PT データ側で正規名へ書き換える）と③（`upcoming` 11件の実装状況確認）は未実施。分類は確定済みで、書き換えは表示文言の変更になるため根拠メモ付きで別途行う。
  - `hsr:モーテナックス・ブレード` と `hsr:モルトナックス・ブレード` が同一キャラクターかは未確認（両方 upcoming として登録し注記を付けた）。
- 次フェーズへの申し送り:
  - 監査の判定を変えるときは CLI ではなく `server/auditRules.ts` と `server/auditRules.test.ts` を直す。
  - 新しい表記ゆれを推奨PTへ持ち込むと `partyMemberAliases.test.ts` が落ちる。分類を足すか、表記を正規名へ直す。
  - データ更新の締めでは `golden-snapshot.mjs compare --allow <更新した名前>` を使い、通ったら `snapshot` で baseline を取り直す。
- 関連ドキュメント: 親リポ `my-app2/docs/修正設計書_hoyoverseスキル再設計_2026-09-08.md`（リポジトリ横断のためリンクなし）/ [設計書インデックス.md](設計書インデックス.md)

### Phase 21: 第15バッチの基底データ正規化（修正設計書 2026-09-10） — 完了

- 完了日: 2026-09-10
- 実装した内容:
  - [修正設計書_第15バッチの基底データ正規化_2026-09-10.md](修正設計書_第15バッチの基底データ正規化_2026-09-10.md) の B15-0〜B15-7 を実施し、第15バッチ20名の API 境界上書き（`server/batch15*.ts`）を撤去した。これで全17バッチが基底データ1系統に揃い、リファクタリング計画書 §4.3 #3 を回収した。
  - B15-1: 根拠メモ `docs/batch-15-research-notes.md` を作成（実装済みデータからの転記であり再精査ではないことを明記）。
  - B15-2: `buildAdvisor.ts` に `GUIDE_OVERRIDES_BY_SOURCE_ID` を追加し、`guideFor` が名前キーより先に source ID を引くようにした。`characterConstellations.ts` に `catalogSourceIdFor()` を追加し、図鑑の HSR 経路が凸と同じ確認済み ID をガイド解決へ渡す。
  - B15-3: ガイド20名を基底テーブルへ移設（HSR 7名は `GUIDE_OVERRIDES`、三月なのか（存護）は `GUIDE_OVERRIDES_BY_SOURCE_ID["1001"]`、原神8名は `GI_GUIDE_OVERRIDES`、ZZZ 4名は `ZZZ_CHARACTER_GUIDES`）。値は `batch15GuideFor` の戻り値（マージ済み完成形）から生成した。
  - B15-4: 凸20名を `CURATED` へ `game:sourceId` キーで移設。
  - B15-5: 推奨PT20名60案を `MANUALLY_CURATED_HIGH_USAGE_CATALOG` へリテラル登録し、`communitySources` だけ共通出典を付与した。
  - B15-6: メタデータ日付（20名を `2026-09-05` と第15バッチの出典へ）と履歴イベント（`BATCH_EVENT_TEMPLATES[15]`、サイトイベントを日付昇順の位置へ）を基底へ移設。
  - B15-7: `routers.ts` / `characterReference.ts` の上書き呼び出しと `server/batch15*.ts` 6ファイルを削除し、`scripts/golden-snapshot.mjs` の batch15 分岐も撤去した。
  - B15-0: 親リポの監査・精査スキル文書3件を正規化後の実態へ更新（コードは batch15 を名指し import していないため変更不要）。
- 実装中に確定した設計変更（いずれも `design-doc` で設計書へ反映済み）:
  - **D2 の訂正**: 起票時の「監査スクリプトが `batch15*` を import しているため親リポ先行が必須」は誤り。実測では `batchNNGuides.ts` の最大番号を動的検出する実装（検出 `[15, 17]`・採用 17）で、`batch15*.ts` を消しても壊れない。B15-0 を「撤去後の文書更新」へ変更し、順序制約を撤回した。
  - **D4 / V5 の追加**: 第15バッチ20名の凸 `rankLabel` が他228名と違っていた（英 `Eidolon`/`Constellation`、簡体字 `命座`/`意象影画`）。基底の正準ラベルへ揃うことを許可差分 V5 とした（日本語は不変）。
  - **D5 / V2 の撤回**: 推奨PTを `manualOptions` 経由で登録する案は不可。`members[0]` に対象の役割を付ける実装で、第15バッチは対象が先頭でない編成が60案中26案ある。役割付与を「本人かどうか」へ変える案は**既存カタログ側45名の応答も変える**ことを実測で確認したため却下し、リテラル登録＋`communitySources` 追加のみへ変更した。
- 反映コミット: `24df9e5`（Phase 開始・D2 訂正）/ `2806ba2`（B15-1）/ `a4dca27`（B15-2）/ `b3d1ca1`（B15-3）/ `4490624`（B15-4）/ `b0a9438`（B15-5）/ `509b901`（B15-6）/ `0e02765`（B15-7）。差分合計 22ファイル・+286 / −495 行。親リポ側は `ea0f80d`（B15-0）。main へ PR [#26](https://github.com/Sitar-sitar/hoyoverse-builder/pull/26) 経由でマージ（マージコミット `e241aa0`、CI `validate` pass 確認済み）。
- 検証結果:
  - 型検査 pass（`corepack pnpm check`）。各項目のコミット前に毎回実行。
  - 全回帰 pass（`corepack pnpm test`。36ファイル → 35ファイル172件。`batch15CuratedData.test.ts` の削除と、正規化に伴う既存期待値3件の更新を含む）
  - `build:api` pass / Pages 本番設定ビルド pass
  - ゴールデン比較 pass: 最終的な差分は `referenceCore`（20件）・`referenceConstellations`（20件）・`history` のみで、内容が V1・V4・V5 に限られることを専用の照合スクリプトで機械確認した。`referenceCore` の差分は全件 `communitySources` 0件→1件以上、`referenceConstellations` の差分は全件 `rankLabel` の英・簡体字のみ（日本語不変）、`history` はサイトイベント 9→8件・第15バッチのイベント重複 2→1件・日付昇順。
  - `check-r8` pass: `total 248 / catalog-id mismatch 0 / not curated 5 / name unresolved 0` を維持。
  - 三月なのかの分離 pass: `hsr:1224`（巡狩）は `profileId=crit` の自動生成ガイドと `preparing` の凸のままで、存護（`hsr:1001`）のデータを受け取らない。
  - ローカル API 実機確認 pass（port 3043）: 御空・ノエル・猫又が `dataStatus: "curated"`・`dataAsOf: "2026-09-05"`・`communitySources` 付き、`build.guideHistory` も正常。
  - 本番確認 pass（2026-09-10、マージ後）: Railway の自動デプロイ完了後（merge から約2分）に公開 API `build.reference` で御空・ノエル・猫又が `dataStatus: "curated"`・`dataAsOf: "2026-09-05"`・`communitySources` の `checkedAt: "2026-08-25"` を返し、`rankLabel` が正準ラベル（`Eidolons` / `Constellations`・`命之座` / `心象电影`）になっていることを確認。`build.guideHistory` の `siteEvents` は8件・第15バッチのイベント1件・日付昇順。Google Chrome で公開図鑑の御空に推奨PT3案（現行推奨／代替編成／所持対応）と星魂1〜6段が表示されることを確認した。
- 残課題・既知の問題:
  - 第15バッチ20名の推奨PTは、文言と `id`（`batch15-*`）体系が他200名（`curated-*`）と異なるまま残る（D5 の割り切り）。統一するなら、既存45名の役割ラベルの扱いと合わせて別設計で行う。
  - 20名の出典を訪問し直しての再精査は未実施（根拠メモに明記）。次回の個別精査バッチで実施する。
  - リファクタリング計画書 §4.3 の残り5件（メンバー名の表記ゆれ37種、`express.json` の 50MB 上限、`build.guideHistory` の応答サイズ、Pages の 500kB 超チャンク、監査スクリプトの移設）は未着手。
- 次フェーズへの申し送り:
  - **オーバーライド形式のバッチは今後作らない**。全17バッチが基底データ直接更新に揃った。新バッチは `characterBatches.ts` の `REVIEWED_BATCHES` へ1行足し、ガイド・凸・PT・メタデータ・履歴を各基底ファイルへ登録する。
  - 同名で複数実装があるキャラクター（三月なのか 1001/1224 など）のガイドは `GUIDE_OVERRIDES_BY_SOURCE_ID` へ登録する。名前キーの `GUIDE_OVERRIDES` は別実装にも当たるため使わない。
  - 図鑑の HSR ガイド解決は `catalogSourceIdFor()` 経由で identity を渡している。新キャラクター追加時は `server/fixtures/identityCatalogSnapshot.ts` の更新が引き続き必須。
  - 推奨PTの共通ヘルパー `manualOptions` は `members[0]` に対象の役割を付ける実装で、対象が先頭でない45名では役割が別メンバーへ付いている（既存の data 品質問題）。触ると広範囲に影響するため、直すなら独立した設計で行う。
- 関連ドキュメント: [修正設計書_第15バッチの基底データ正規化_2026-09-10.md](修正設計書_第15バッチの基底データ正規化_2026-09-10.md) / [batch-15-research-notes.md](batch-15-research-notes.md) / [リファクタリング計画書_2026-09-08.md](リファクタリング計画書_2026-09-08.md) §4.3 #3 / [設計書インデックス.md](設計書インデックス.md)（実装後メモは版番号を動かさないため作成しない）

### Phase 20: リファクタリング第1期（計画書 2026-09-08） — 完了

- 完了日: 2026-09-09
- 実装した内容:
  - [リファクタリング計画書_2026-09-08.md](リファクタリング計画書_2026-09-08.md)（[設計レビュー](設計レビュー_リファクタリング計画書_2026-09-09.md) DR-01〜DR-05 反映版）の R0〜R9 を実施した。挙動不変が原則で、唯一の挙動変更は R8。
  - R0: ゴールデン比較スクリプト `scripts/golden-snapshot.mjs` を新設（11セクション。`import` しても副作用がない `main()` 隔離、図鑑の凸と UID 照会経路の一致を検査する `check-r8` サブコマンド付き）。
  - R1: 参照元ゼロの Manus テンプレート残骸 14ファイル・3,924行を削除（`server/_core/{llm,imageGeneration,map,voiceTranscription,dataApi,heartbeat}.ts`、`server/storage.ts`、client の ComponentShowcase / DashboardLayout / DashboardLayoutSkeleton / AIChatBox / Map / ManusDialog、`catalog-role-report.mjs`）。
  - R2: ロール分類 Set 9個と HSR 運命ラベル表の重複を統合。ラベル表は `characterGuideCatalog.ts` を正本にし、分類は `individualGuides.ts` の `hsrProfileIdFor` / `genshinProfileIdFor` / `zzzProfileIdFor` へ集約（`expectedGuideProfiles.ts`・`enkaFallback.ts`・`partyRecommendations.ts` が参照）。
  - R3: 推奨PTのバッチ別ラッパー13本（83行）を `datedOptions(date, resetTargetChanges)` へ統合。200件の呼び出しと `manualOptions` 本体は不変。
  - R4: `UidResponseCache.set()` に閾値超過時の期限切れ掃除を追加し、掃除を観測するための `size` を公開（常駐プロセスでの無制限増加を防止）。
  - R5: `characterUpdateLedger()` / `characterReferenceCatalog()` / `guideUpdateHistory()` をメモ化し、図鑑1件照会の台帳全体線形探索を `ledgerEntryFor()` の Map 索引へ置換。
  - R6: 3ファイルに二重保持されていたバッチ名リストを `server/characterBatches.ts`（`REVIEWED_BATCHES` / `batchIdFor` / `batchNames`）へ一元化。台帳・更新履歴・メタデータ・`BATCH15_CHARACTERS` がここを参照する。
  - R7: 更新履歴のバッチ別イベント関数16本を `BATCH_EVENT_TEMPLATES` + `batchEventFor()` のテーブル1本へ集約（文言は既存リテラルを機械的に移設）。
  - R8（唯一の挙動変更）: 図鑑の `CATALOG_NAME_TO_SOURCE_ID`（手書き48件）を `ACTIVE_CATALOG_IDENTITIES` からの機械生成へ変更。図鑑（`build.reference`）の凸が UID 照会経路と同じ curated データを返すようになった。
- 反映コミット: `730a730`（レビュー反映）/ `3c8a2a4`（R0）/ `f71eca7`（R1）/ `2aa7cc9`（R2）/ `3bf9f5c`（R3）/ `088c7e7`（R4）/ `70b48b0`（R5）/ `4ad1141`（R6）/ `5750850`（R7）/ `ca7f509`（R8）/ `8d7ab7b`（締め記録）。差分合計 37ファイル・+1,635 / −4,505 行。main へ PR [#22](https://github.com/Sitar-sitar/hoyoverse-builder/pull/22) 経由でマージ（マージコミット `4644955`、CI `validate` pass 確認済み）。
- 検証結果:
  - 型検査 pass（`corepack pnpm check`、エラー0）。各項目のコミット前に毎回実行。
  - 全回帰 pass（`corepack pnpm test`。開始時 34ファイル183件 → 完了時 36ファイル。R2・R6・R8 でテストを追加）
  - `build:api` pass / Pages 本番設定ビルド pass（全項目で実行）
  - ゴールデン比較 pass: R1〜R7 は全11セクション一致。R8 のみ `referenceConstellations` と `catalogConstellations` の172件が差分で、`referenceCore`（図鑑応答の guide / partyRecommendations / status / batch）を含む他9セクションは一致。許可した2セクションだけを baseline へ反映した。
  - R8 受入判定 pass: `check-r8` が `total 248 / catalog-id mismatch 0 / not curated 5 / name unresolved 0`。変更対象集合が着手前に保存した172名と完全一致（`same set: true`）。変更後も準備中の5名は `genshin:アーロイ` / `genshin:コロンビーナ` / `zzz:シーシィア` / `zzz:ニコ` / `zzz:ピュロイス`（凸データ自体が未登録）。
  - R4 の追加テストは、掃除ブロックを外すと `expected 301 to be 1` で失敗することを実際に確認（確認後に復元）。
  - ローカル API 実機確認 pass（`node dist/index.js`、`API_ONLY=true`、port 3042）: `/api/health` が `{"ok":true,"service":"hoyoverse-builder-api"}`、`build.reference` の hsr:ホタル・zzz:星見雅 が `"dataStatus":"curated"`、zzz:ニコ が `"preparing"`、`build.guideHistory` が `"result"` を返す。
  - 本番確認 pass（2026-09-09、マージ後）: Pages デプロイ workflow success。Railway の自動デプロイ完了後（merge から約2分）に公開 API `build.reference` が hsr:ホタル・zzz:星見雅 で `"dataStatus":"curated"`、凸データ未登録の zzz:ニコ・genshin:コロンビーナ は `"preparing"`、`build.guideHistory` は `"result"` を返す。Google Chrome で `https://sitar-sitar.github.io/hoyoverse-builder/characters` のホタルに星魂1〜6が表示されることを確認（R8 前は「準備中」）。反映前は公開 API が旧データを返すため、直後の `preparing` は未デプロイを先に疑う。
- 残課題・既知の問題:
  - 計画書 §4.3 の別対応項目は未着手のまま（第15バッチの API 境界上書き正規化、推奨PTメンバー名の表記ゆれ37種、`express.json` の 50MB 上限、`build.guideHistory` の応答サイズ、Pages の 500kB 超チャンク、監査スクリプトの移設）。
  - 凸データ未登録の5名（上記）は R8 の対象外。データ登録は個別精査バッチの担当。
- 次フェーズへの申し送り:
  - バッチ名の追加は `server/characterBatches.ts` の `REVIEWED_BATCHES` へ1行足すだけでよい（台帳・履歴・メタデータ・第15バッチ定義が同じ表を参照する）。履歴文言は `BATCH_EVENT_TEMPLATES`、日付・出典は `characterGuideMetadata.ts` に足す。
  - データ更新時は `corepack pnpm exec tsx scripts/golden-snapshot.mjs snapshot` → 変更 → `compare` で「更新対象以外が変わっていない」ことを確認できる。図鑑の凸整合は `check-r8`。
  - 図鑑の凸は `ACTIVE_CATALOG_IDENTITIES` 由来の索引で解決するため、新キャラクター追加時は `server/fixtures/identityCatalogSnapshot.ts` の更新が必要。
  - §4.3 の残件は親リポ `my-app2/docs/修正設計書_hoyoverseスキル再設計_2026-09-08.md` 側で扱う。
- 関連ドキュメント: [リファクタリング計画書_2026-09-08.md](リファクタリング計画書_2026-09-08.md) / [設計レビュー_リファクタリング計画書_2026-09-09.md](設計レビュー_リファクタリング計画書_2026-09-09.md) / [設計書インデックス.md](設計書インデックス.md)（実装後メモは版番号を動かさないため作成しない）

### Phase 19: MiHoMo正規化経路の「現在のステータス」欄ラベル重複を修正 — 完了

- 完了日: 2026-09-08
- 実装した内容:
  - Phase 18の残課題（MiHoMo経路で同一ステータス名が絶対値・割合値の両方で重複表示される既存の表示上の粗さ）を調査・修正した。
  - `server/buildAdvisor.ts` の `parseCharacter` は MiHoMo `sr_info_parsed` の `properties[].name` をそのまま `allStats[].name` へ使っていた。StarRailRes `index_new/jp/properties.json`（MiHoMoが参照する本家データ）を直接確認したところ、`AttackAddedRatio`/`AttackDelta` はいずれも `name: "攻撃力"`、`HPAddedRatio`/`HPDelta` はいずれも `name: "HP"` で、`field` も `atk`/`hp` と同一、区別できるのは `percent` フラグのみだった（推測ではなく実データで確認）。このため割合ステータスと絶対値ステータスがUI上で同じラベルの行として重複表示されていた。
  - `isPercentStat()` ヘルパーと `allStatsFor()` を追加し、`properties` 配列内で `name` が重複するステータスに限り、`percent: true` 側へ `%` を付与してラベルを区別する（例:「攻撃力」/「攻撃力%」、「HP」/「HP%」）。重複のない単独の割合ステータス（会心率・会心ダメージ等）には不要な `%` を付与せず、既存表示を変えない。
  - 横展開確認：原神（`server/gameProviders.ts` の `GI_PROP_NAMES`）は `FIGHT_PROP_HP_PERCENT` 等に個別の `%` 付きラベルを持つ独自マップを使用しており重複なし。ZZZ（`finalZzzStats`）とHSR Enkaフォールバック経路（`enkaFallback.ts`）はいずれも絶対値・割合を合成した単一の最終値エントリしか出力しないため重複なし。本バグはMiHoMo正規化経路のみに存在した。
  - `server/buildAdvisor.test.ts` に実データ相当（`name` 重複あり）の回帰テストを追加。修正前は重複行、修正後は「HP」/「HP%」・「攻撃力」/「攻撃力%」に分離されること、単独の割合ステータス（会心率）には不要な `%` が付かないことを検証した。
- 反映コミット: `8f1f8d5`（実装・テスト） / `b1a31fa`・`b5fd5f7`（記録）。mainへ PR [#20](https://github.com/Sitar-sitar/hoyoverse-builder/pull/20) 経由でマージ（マージコミット `3e230ff`、CI `validate` pass確認済み）。
- 検証結果:
  - 型検査 pass（`tsc --noEmit`、エラー0）
  - 全回帰 pass（34ファイル / 183テスト。新規1テスト追加、既存182件も継続pass）
  - `npm run build:api` pass / Pages本番設定ビルド（`vite build --config vite.pages.config.ts`）pass
  - 実UID確認: 未実施。本バグはUIDに依存しないロジックバグ（MiHoMoの `properties[].name` を扱う純粋な整形処理）であり、StarRailRes実データ構造の直接確認とユニットテストで再現・修正を検証済みのため、実機再現は必須としなかった。次回MiHoMo経路が発火する実UID確認の機会があれば、あわせて表示を目視確認する。
- 残課題・既知の問題: なし（Phase 18の残課題であったMiHoMo経路の表示重複はこれで解消）。
- 次フェーズへの申し送り:
  - 次回、新規キャラクター追加・ガイド更新・不具合報告のいずれかを起点に着手する。
  - MiHoMo `sr_info_parsed` のレスポンス仕様が変わった場合は `server/buildAdvisor.ts` の `allStatsFor()`／`isPercentStat()` を見直す。
- 関連ドキュメント: なし（既存の設計書を更新する規模の変更ではないため新規設計書は作成していない）

### Phase 18: HSR Enkaフォールバック 戦闘外最終ステータス算出の修正 — 完了

- 完了日: 2026-09-08
- 実装した内容:
  - `server/enkaFallback.ts`を全面書き換え。遺物`_flat.props`の単純加算で最終値としていた不具合を修正し、キャラクター・光円錐のレベル別基礎値（`base + step × (level-1)`）、遺物メイン・サブ、解放済み軌跡（要求`promotion`/キャラクターレベルの正しい照合）、光円錐重畳ランクの常時有効プロパティを`StatKey`単位で分類・合成する方式へ改めた。`HP・攻撃力・防御力・速度 = 基礎値 × (1+割合) + 固定値`、会心率・会心ダメージ・EP回復効率は基準5%/50%/100%へ独立加算。
  - 静的データを`characters`/`light_cones`/`relic_sets`/`character_promotions`/`character_skill_trees`/`light_cone_promotions`/`light_cone_ranks`/`properties`の8種へ拡張し、全種非空をfail-closedで検証。取得は5秒上限・24時間キャッシュとし、失敗時は直近正常バンドル（最大7日）へ退避、両方失敗時は0補完せず空バンドルへ縮退する。
  - キャラクター単位で基礎値・光円錐基礎値・軌跡ノードのいずれかを解決できない場合は`statsStatus: "unavailable"`とし、`allStats`は空、`comparisons.current`はnull、強化提案は生成しない。不完全なEnka結果はUIDキャッシュへ保存しない。
  - 既存バグとして、UIDキャッシュへ`dataSource`が保存されずキャッシュヒット時に`ENKA FALLBACK`表示が失われる不具合も合わせて修正（`server/buildAdvisor.ts`）。
  - `client/src/pages/Home.tsx`・`LanguageContext.tsx`に`statsStatus`に応じた3言語の注記（算出済み／算出保留）と、比較保留時に「すべて目標水準に到達」と誤表示しない分岐を追加。
  - `server/buildAdvisor.test.ts`にStarRailRes実データ（`hsr:1502`のレベル別基礎値・軌跡要求Lv80ノード）を用いた回帰テストを追加し、修正前に失敗する差分（旧実装のレベル比較バグ）が修正後に合格することを確認した。
  - 【追加修正】公開後、ユーザー許可済みの公開UIDで実機確認したところ、Enkaフォールバック表示に「会心率 0」の重複行が出る回帰を発見。StarRailRes `properties.json` の `field: ""` 型（`MaxHP`/`CriticalChance`等、集計済み表示専用の予約名）が「その他」欄へ誤って表示されていたため、`classify()`へ`ignored`分類を追加して一般画面へ出さないようにした。実データ形をfixture化した回帰テストを追加。
- 反映コミット: `d7deb00`（実装・テスト） / `e295fcb`（本番実機で発見した表示重複の追加修正）。
- 検証結果:
  - 型検査 pass（`tsc --noEmit`、エラー0）
  - 全回帰 pass（34ファイル / 182テスト、追加修正後も同数）
  - `pnpm build:api` pass / Pages本番設定ビルド（`vite build --config vite.pages.config.ts`）pass（両コミットとも）
  - 実UID確認: **実施済み**（2026-09-08、ユーザー提供の公開UIDでHSR・原神・ZZZの3件を明示照会。UIDは記録しない）。
    - HSR: Enkaフォールバックが発火し、`ENKA FALLBACK`表示・キャラクター基礎値+光円錐+遺物+解放済み軌跡に基づくHP/攻撃力/防御力/速度/会心率/会心ダメ等の一覧表示・統一されたstatsNoteを確認。追加修正前は「会心率 0」の重複行を実機で検出し、追加修正後は型検査・テスト・ビルド全てpassで再デプロイ済み（MiHoMoが安定して主取得に成功する状態が続き、追加修正後のEnka経路の再度の実機再現は未実施）。
    - 原神: 12名の公開キャラクターを正常表示、コンソールエラー0件（本フィックスの対象外パスであり無変更）。
    - ZZZ: 同UIDで「データサービスに接続できません」を確認（`LOOKUP UNAVAILABLE`、TRPCClientError）。ZZZ取得経路は本パッチで一切変更していないため、本パッチによる回帰ではない。第三者データサービス側の一時的な問題かUIDの公開設定起因の可能性があり、原因切り分けは別タスクとする。
  - Railway `/api/health`: `{"ok":true,"service":"hoyoverse-builder-api"}` を確認。Pages配信（`https://sitar-sitar.github.io/hoyoverse-builder/`）はHTTP 200。
- 残課題・既知の問題:
  - ~~ZZZの対象UIDで実機からデータ取得できない事象が残る（本パッチの回帰ではないが未解決）。次回、別タスクとして原因調査する。~~ → 2026-09-08 追加調査でクローズ。`server/gameProviders.ts`のZZZ取得経路（エンドポイント・タイムアウト・エラーハンドリング）を確認したがコード側の欠陥はなく、`https://enka.network/api/zzz/uid/{uid}/`へ直接照会したところ実在UIDで`HTTP 424 Failed Dependency`（本文はクライアント表示文言と一致）を確認、外部データサービス（enka.network）側の問題と特定した。ユーザーが翌日（2026-09-09）のZZZアップデート関連の一時的事象と判断し、対応不要でクローズとした。
  - 遺物セットの無条件プロパティがキャラクター詳細画面へ反映される範囲は未検証（設計書§12の非ブロッキング事項）。実測fixtureでの採否確定は次回対応。
  - 光円錐の`equipment._flat.props`に`BaseHP`等が実際に含まれるかは、追加修正後のEnka経路実機再検証で確認できていない（フォールバック式は`light_cone_promotions`からの算出を常に持つため機能には影響しない）。
  - MiHoMo経路（本パッチのスコープ外）で、同一ステータス名（例:「攻撃力」）が絶対値・割合値の両方で重複表示される既存の表示上の粗さを実機で確認した。本パッチでは未着手・未修正。
- 次フェーズへの申し送り:
  - 実UIDでの本番同時点比較（HoYoLAB表示との数値照合）は、追加修正後のEnka経路実機再現ができなかったため未完了。次回Enkaフォールバックが発火するタイミングで実施する。
  - `docs/guide_sources.md`は本フェーズで算出方法の節を追記済み。今後Enka側のレスポンス仕様が変わった場合はここを更新する。
  - ZZZデータ取得不能の事象とMiHoMoの重複表示は、いずれも本パッチのスコープ外の既知事象として次回別タスクで扱う。
- 関連ドキュメント: [修正設計書_HSR_Enkaフォールバック最終ステータス算出_2026-09-08.md](修正設計書_HSR_Enkaフォールバック最終ステータス算出_2026-09-08.md) / [guide_sources.md](guide_sources.md) / [設計書インデックス.md](設計書インデックス.md)

### Phase 17: 第17パッチ 残り28体のキャラクター個別精査＋空目標表示修正 — 完了

- 完了日: 2026-09-08
- 実装した内容:
  - 第16パッチ時点で未精査だった原神28体を一括更新し、個別ビルド、実名4名PT各2案、通常27体と旅人7元素の全6段階命ノ星座を登録。34プロフィール・43 source ID・204効果をID優先で解決する。
  - 固定の戦闘外到達値が主出典にない場合は数値を推測せず `targets: []` とし、武器・PT・命ノ星座・戦闘中バフを公開プロフィール値へ加算しない。武器と代替はスキーマを拡張せず研究ノートへ保存した。
  - UID照会の優先強化欄を「未達あり」「比較あり・未達なし」「比較対象なし」の3状態へ分離。比較対象0件では「固定目標値が未登録」と3言語で表示し、「すべて目標水準に到達」と誤表示しない回帰テストを追加した。
  - 更新台帳を248/248（再調査待ち0）へ更新し、第18候補を空にした。図鑑のUID不要経路と更新履歴にも第17パッチを反映した。
- 反映コミット: `7b138dd`。mainへPR [#15](https://github.com/Sitar-sitar/hoyoverse-builder/pull/15) 経由でマージ（マージコミット `a003a3a`）。
- 検証結果:
  - 型検査 pass（`tsc --noEmit`、エラー0）
  - 全回帰 pass（34ファイル / 181テスト）
  - APIバンドル / Pages本番設定ビルド pass
  - PR CI `Validate HoYoverse external API` run `34169217730` pass
  - Pagesデプロイ run `34169275133`（`a003a3a`）success。公開indexと配信アセットはいずれもHTTP 200で、新しい固定目標未登録文言を含む。
  - 公開API（Railway）: Origin付き `/api/health` が200・CORS許可、更新台帳が248/248・pending 0・第18候補0、第17サイトイベントを返す。`build.reference`（夢見月瑞希）は `batch: 17`、`targets: []`、PT 2案、命ノ星座6段、`curated` を返す。
  - 公開画面（Chrome）: `/characters` で `REVIEWED 248 / 248`、夢見月瑞希のB17ビルド・固定目標なし表示・PT 2案・命ノ星座6段を確認。`/updates` で248/248・再調査待ち0・第17イベントを確認。コンソールエラー0件。
  - 実UID照会は、今回新しい公開UIDが提供されていないため未実施。UID不要の図鑑、API最終応答、状態分岐の画面単体テストで対象範囲を確認した。
- 残課題・既知の問題:
  - genshin.devで英語データを取得できない夢見月瑞希・藍硯・炎旅人・氷旅人、および全プロフィールの中国語は日本語主出典からの翻訳。公式訳を確認できた場合は差し替える。
  - リポジトリ内に `docs/設計書インデックス.md` は存在しないため、Phase 17で新規設計書や索引更新は行っていない。
- 次フェーズへの申し送り:
  - 初回個別精査は全248体で完了。次回はゲーム側の新規キャラクター追加、ガイド更新、公式ローカライズ確定のいずれかを差分起点にする。
  - `targets: []` は未達・達成を判定できない状態であり、今後も固定値の根拠なしに数値を補完しない。
  - `main` は直接push不可。引き続きPR、CI、PagesのマージSHA一致、Railway API、公開画面を別々に検証する。
- 関連ドキュメント: [batch-17-research-notes.md](batch-17-research-notes.md) / [all-character-update-ledger.md](all-character-update-ledger.md)

### Phase 16: 第16バッチ キャラクター個別精査（HSR 4名・原神 16名） — 完了

- 完了日: 2026-09-07
- 実装した内容:
  - 台帳の未精査20名（HSR: 不死途・乱破・椒丘・爻光／原神: バーバラ・ファルザン・フレミネ・ミカ・モナ・ヨォーヨ・ラウマ・リサ・
    リネ・リネット・レイラ・マーヴィカ・ムアラニ・フリンズ・ヤフォダ・リオセスリ）の個別ビルド・全6段階の凸・実名推奨PTを、
    更新日付きの公開ガイド（Game8日本語版が主出典、GameWith・崩壊スターレイルWikiが補助）から精査して登録。
  - 第15バッチのAPI境界上書きではなく基底ファイル（`buildAdvisor.ts` / `gameProviders.ts` / `characterConstellations.ts` /
    `partyRecommendations.ts` / `characterGuideMetadata.ts` / `guideUpdateHistory.ts`）を直接更新。
  - 図鑑（UID不要）の既存欠落を2件修正。①カタログ名を source ID として扱っていたため ID キーの凸へ到達しなかった →
    `constellationProfileForCatalogName()` を追加。②原神・ZZZのガイドが `generated*` へ直接フォールバックし、provider 経路にだけ
    登録された個別ビルドが図鑑へ届かなかった → `genshinGuide()` / `zzzGuide()` へ統一。第16バッチ以外にも影響していた。
  - 台帳を 200/248 → 220/248（残28、原神のみ）へ更新し、`NEXT_BATCH` を第17バッチ（原神20名）へ差し替え。
- 反映コミット: `bcd7205`（データ本体） / `b0e3d7c`（図鑑経路の修正） / `3051bf1`・`9ee37a2`（記録）。
  main へは PR [#12](https://github.com/Sitar-sitar/hoyoverse-builder/pull/12) 経由でマージ（マージコミット `ceb9393`）。
- 検証結果:
  - 型検査 pass（`tsc --noEmit`、エラー0）
  - テスト pass（33ファイル / 174テスト。新規 `server/batch16CuratedGuides.test.ts` 7テスト）
  - `pnpm build:api` pass / Pages ビルド（`vite build --config vite.pages.config.ts`）pass
  - PR CI `Validate HoYoverse external API` pass
  - Pages デプロイ run `34133146320`（`ceb9393`）success
  - 公開API（Railway）: `/api/health` ok、`/api/trpc/build.reference`（不死途）が `batch: 16` と精査済みガイドを返すことを確認。
    マージ直後は旧データを返し、Railwayの自動デプロイ完了後に切り替わった。
  - 公開画面（Chrome）: 図鑑で不死途・マーヴィカの精査済みビルド表示、`/updates` で「220 / 248 精査完了」「再調査待ち28」
    「第16バッチ20名の個別情報を更新」を確認。コンソールエラーなし。
  - `verify-live-site.ps1`: `IndexHasNoRootAbsoluteAssets` のみ fail。プロジェクトサイト（`base: "/hoyoverse-builder/"`）の
    仕様どおりの絶対パス参照で、参照アセットはすべて到達可能（既存仕様であり本フェーズの回帰ではない）。
  - 実UIDでの照会画面確認: **実施済み**（2026-09-07、ユーザーが提供した公開UIDでHSR・原神・ZZZの3件を明示照会。UIDは記録しない）。
    - HSR: ショーケースに第16バッチ対象は爻光のみ。`hsr:1502`、天命に従う旅の卜者 ×4 / 千の星が集う街 ×2、速度160・会心率62%・
      会心ダメ145%の比較表、星魂6段（未解放含む）、PLAN1先頭が本人。図鑑と推奨セット・目標値・PT構成が一致。
    - 原神: 対象はムアラニ・バーバラの2名。それぞれ `genshin:10000102` / `genshin:10000014` で個別セットと命ノ星座6段を表示。
      図鑑と一致。残る対象は当該UIDのショーケース外のため未表示（失敗ではない）。
    - ZZZ: 第16バッチ対象なし。既存データ（レミエール等）の表示崩れがないことのみ確認。
    - 3ゲームともコンソールエラー0件、取得不能なし。再読み込みでは保存UIDが復元されるだけで自動照会されないことを確認。
- 残課題・既知の問題:
  - **未育成キャラで「すべて目標水準に到達しています」と表示される**（実UID確認でムアラニ Lv.1・C0 にて確認）。
    出典に数値の明示がないキャラは設計どおり `targets: []` のため比較対象が0件になるが、`client/src/pages/Home.tsx` は
    未達0件を一律 `allTargetsMet` として扱うため、「目標が未登録」と「全項目達成」が同じ文言になる。第16バッチ固有ではなく
    `targets: []` の既存キャラ全体に該当。**ユーザー判断により第17バッチでまとめて対応する**（比較0件時だけ別文言へ分岐、
    対象は Home.tsx・図鑑側の同等表示・`LanguageContext.tsx` の3言語・回帰テスト）。
  - 凸の英語名・中国語名は公式訳を確認できず `Constellation N` / `Eidolon N` の汎用ラベルを使用（説明文は訳出済み）。
  - 第15バッチは根拠メモがなく、API境界上書きのまま残っている。
- 次フェーズへの申し送り:
  - 第17バッチ候補は原神20名（レザー、ロサリア、雲菫、煙緋、嘉明、甘雨、閑雲、凝光、九条裟羅、荒瀧一斗、刻晴、珊瑚宮心海、
    鹿野院平蔵、七七、重雲、申鶴、神里綾華、神里綾人、辛炎、千織）。残8名は第18バッチで母集団を一巡する。
  - **第17バッチの作業に「比較対象0件のときの文言分岐」を含める**（上記の残課題。データ精査とは別の小さなUI修正）。
  - `main` は branch ruleset により直接 push 不可。公開は PR 経由（`gh pr create` → CI → `gh pr merge`）。
  - キャラクターデータは Pages ではなく Railway API が返す。Pages run の成功だけでは反映の証拠にならない。
  - 新しいデータは基底ファイルへ登録すれば UID照会・図鑑の両方へ届く。実画面確認は必ず図鑑側でも行う
    （名前一致でテストが通り、実画面で初めて欠落が露見した）。
- 関連ドキュメント: [batch-16-research-notes.md](batch-16-research-notes.md) / [all-character-update-ledger.md](all-character-update-ledger.md)
