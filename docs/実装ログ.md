# 実装ログ（hoyoverse-builder）

`D:\my-app2\docs\実装フェーズ記録ルール.md` に従い、実装の過程をフェーズ単位で記録する。
**次フェーズ着手前に現フェーズを「締めチェックリスト」で必ずクローズする。**

- 対象アプリ: HoYoverse Builder（`Sitar-sitar/hoyoverse-builder`、公開 https://sitar-sitar.github.io/hoyoverse-builder/ ）
- 現在フェーズ: なし（Phase 23 完了。推奨PTメンバー名の表記統一）
- 現在ステータス: 完了（次回は新規追加・根拠更新・不具合報告時に着手）

> 本ファイルは 2026-09-07（第16バッチ着手時）に新規作成した。第1〜15バッチは本ログの運用開始前の作業であり、
> 経緯は [`docs/all-character-update-ledger.md`](all-character-update-ledger.md) と `docs/batch-N-research-notes.md`、
> コミット履歴を参照する。ここへ遡って再構成はしない。

---

## 現在進行中フェーズ

（なし）

---

## 完了フェーズ（締め済み）

### Phase 23: 推奨PTメンバー名の表記統一（スキル再設計 §4.3 ①） — 完了

- 完了日: 2026-09-10
- 実装した内容:
  - 親リポ設計書 §4.3 タスク① を実施し、推奨PTのメンバー名16表記・31箇所をカタログ名へ統一した（`server/partyRecommendations.ts`）。旧表記・新表記・根拠は [party-member-notation-normalization-2026-09-10.md](party-member-notation-normalization-2026-09-10.md) に記録した。
  - `PARTY_MEMBER_ALIASES` を空にし（設計書の完了条件）、元素情報を残す `genshin:旅人（草）` は `NON_CATALOG_PARTY_MEMBERS` へ「正当な派生」として登録した。
  - 監査の整合性警告のうち「`docs/all-character-update-ledger.md` が第17バッチを記載していない」を解消（同ドキュメントが第17バッチだけ「パッチ」表記だったため4箇所を統一）。
- 反映コミット: 本フェーズのコミット（PR は下記）。
- 検証結果:
  - 型検査 pass / 全回帰 pass（38ファイル。`batch14CuratedGuides.test.ts` の期待値1件を正規名へ更新）/ `build:api` pass / Pages ビルド pass
  - ゴールデン比較: 差分は `parties` / `referenceCore` の21名のみで、**変わったのは `members[].name` だけ**（案の数・順序・id・rank・出典・役割ラベル・targetChanges は不変）であることを機械照合してから baseline を更新した。
  - 監査 CLI: 整合性警告が2件→1件（残りは `hsr:ジェイド` / `hsr:ジェパード` の個別ガイド欠落）。`unknown` 0件・経路不一致0件を維持。
- 残課題・既知の問題:
  - `hsr:ジェイド` / `hsr:ジェパード` の個別ガイド欠落（第10バッチ扱いなのにロール共通値のまま）。出典調査が要るため `hoyoverse-character-update` のモード A で別途。
  - カタログ未登録の実装済みキャラクター6件（`hsr:1512 ロビン・夏空の歌` / `hsr:1513 アベンチュリン・波と戯れる夏` / `hsr:1510 姫子・旅立ち` / `hsr:1507 千冶・刃` / `zzz:1611 クラレッタ` / `zzz:1621 ロクシー`）はモード C で追加する（§4.3 ③）。
  - `UPCOMING_PARTY_MEMBERS` 11表記は実装待ちのまま。`hsr:モーテナックス・ブレード` と `モルトナックス・ブレード` の同一性は未確認。
- 次フェーズへの申し送り: 新しい表記ゆれを持ち込むと `partyMemberAliases.test.ts` が落ちる。別名表は空を保ち、データ側を正規名で書く。
- 関連ドキュメント: [party-member-notation-normalization-2026-09-10.md](party-member-notation-normalization-2026-09-10.md) / 親リポ `my-app2/docs/修正設計書_hoyoverseスキル再設計_2026-09-08.md` §4.3 ①

### Phase 22: hoyoverse 運用スキル再設計 §4.1（親リポ設計書 2026-09-08） — 完了

- 完了日: 2026-09-10
- 実装した内容:
  - 親リポ `docs/修正設計書_hoyoverseスキル再設計_2026-09-08.md` の §4.1（リポジトリ側の判定基盤）を実装した。§4.2（スキル本体の改訂・`hoyoverse-character-curation` 撤去）は親リポ Phase 5 で実施。
  - `server/partyMemberAliases.ts` / `.test.ts`: 推奨PTのメンバー名をカタログ名へ解決する表と `resolvePartyMember()`。カタログ外39表記を alias 16 / variant 12 / upcoming 11 に分類。全248名の全案の全メンバーが `unknown` にならないことを回帰ゲートで固定した（表示には使わず監査専用）。
  - `server/partyImpact.ts` / `.test.ts`: `partyImpactFor()` と `formatPartyImpactMarkdown()`。新キャラクター追加時に「既に言及している推奨PT」をチェックリストで出す。原神は同役割候補を出さない（設計 §10-2 の決定）。
  - `server/auditRules.ts` / `.test.ts`: ガイド種別判定・監査レコード・P0〜P3 分類・次候補・メンバー名集計に加え、**図鑑と UID 照会の経路一致検査 `routeMismatches()`** を新設。
  - `scripts/audit/audit-characters.mjs`: スキル配下から移設・改訂。判定は `auditRules` へ委譲し、出力に経路一致・メンバー名・実装待ちの言及を追加。P0 行に影響レビュー待ち件数を併記。
  - `scripts/audit/party-impact.mjs` / `refresh-identity-snapshot.mjs`: 影響レビューの出力と、公開メタデータとの差分表示（`--write` でのみ書き換え）。
  - `scripts/golden-snapshot.mjs`: `--allow <game:name,...>` を追加。キーがキャラクター名のセクションだけキー単位で差分を許容し、`metadata` / `ledger` / `history` はセクション単位のまま。
- 着手前に確定した設計判断（ユーザー確認）:
  - §10-1 メンバー名の分類。identity スナップショット照合・Game8・公開情報で36件を根拠付きで確定し、`hsr:永夜`→`長夜月`、`zzz:パン`→`潘引壺` はユーザー判断で alias 確定。
  - §10-2 原神の同役割判定は行わない（`sameRoleOwners` は常に空）。これに伴い §4.1.4 の identity スナップショット型拡張（element / weaponType）は不要になったため実装しない。
- 反映コミット: `4a1555f`（解決表・影響レビュー）/ `01fc468`（auditRules）/ `679d851`（監査CLI 3本・--allow）と本コミット。
- 検証結果:
  - 型検査 pass / 全回帰 pass（38ファイル。partyMemberAliases 9件・partyImpact 7件・auditRules 10件を追加）/ `build:api` pass / Pages ビルド pass
  - ゴールデン比較 全11セクション一致（**公開 API の応答は不変**。解決表・影響レビューは監査専用で表示に使わない）
  - CLI スモーク pass: `audit-characters.mjs --offline` が 248/248・経路不一致0件・`unknown` 0件・実装待ちの言及11件を出力。`party-impact.mjs --added genshin:オデット` が言及済み2案を出力。`refresh-identity-snapshot.mjs` は差分表示のみでファイル不変。
  - `--allow` の動作確認 pass: 許容した名前の差分だけを無視し、他の名前が変われば exit 1 で落ちる。
- 実測で判明したこと（次の作業の入力）:
  - 公開メタデータに**カタログ未登録の実装済みキャラクターが4件**ある: `hsr:1512 ロビン・夏空の歌` / `hsr:1513 アベンチュリン・波と戯れる夏` / `zzz:1611 クラレッタ` / `zzz:1621 ロクシー`。加えて `hsr:1510 姫子・旅立ち` / `hsr:1507 千冶・刃` も未登録。`hoyoverse-character-update` のモード C の初回対象になる。
  - 監査の整合性警告が2件出る: `docs/all-character-update-ledger.md` が第17バッチ未記載、`hsr:ジェイド` / `hsr:ジェパード` が第10バッチ扱いなのに個別ガイドが無い（いずれも従来からの課題）。
- 残課題・既知の問題:
  - 設計書 §4.3 の運用タスク①（メンバー名の表記ゆれを PT データ側で正規名へ書き換える）と③（`upcoming` 11件の実装状況確認）は未実施。分類は確定済みで、書き換えは表示文言の変更になるため根拠メモ付きで別途行う。
  - `hsr:モーテナックス・ブレード` と `hsr:モルトナックス・ブレード` が同一キャラクターかは未確認（両方 upcoming として登録し注記を付けた）。
- 次フェーズへの申し送り:
  - 監査の判定を変えるときは CLI ではなく `server/auditRules.ts` と `server/auditRules.test.ts` を直す。
  - 新しい表記ゆれを推奨PTへ持ち込むと `partyMemberAliases.test.ts` が落ちる。分類を足すか、表記を正規名へ直す。
  - データ更新の締めでは `golden-snapshot.mjs compare --allow <更新した名前>` を使い、通ったら `snapshot` で baseline を取り直す。
- 関連ドキュメント: 親リポ `my-app2/docs/修正設計書_hoyoverseスキル再設計_2026-09-08.md`（リポジトリ横断のためリンクなし）/ [設計書インデックス.md](設計書インデックス.md)

### Phase 21: 第15バッチの基底データ正規化（修正設計書 2026-09-10） — 完了

- 完了日: 2026-09-10
- 実装した内容:
  - [修正設計書_第15バッチの基底データ正規化_2026-09-10.md](修正設計書_第15バッチの基底データ正規化_2026-09-10.md) の B15-0〜B15-7 を実施し、第15バッチ20名の API 境界上書き（`server/batch15*.ts`）を撤去した。これで全17バッチが基底データ1系統に揃い、リファクタリング計画書 §4.3 #3 を回収した。
  - B15-1: 根拠メモ `docs/batch-15-research-notes.md` を作成（実装済みデータからの転記であり再精査ではないことを明記）。
  - B15-2: `buildAdvisor.ts` に `GUIDE_OVERRIDES_BY_SOURCE_ID` を追加し、`guideFor` が名前キーより先に source ID を引くようにした。`characterConstellations.ts` に `catalogSourceIdFor()` を追加し、図鑑の HSR 経路が凸と同じ確認済み ID をガイド解決へ渡す。
  - B15-3: ガイド20名を基底テーブルへ移設（HSR 7名は `GUIDE_OVERRIDES`、三月なのか（存護）は `GUIDE_OVERRIDES_BY_SOURCE_ID["1001"]`、原神8名は `GI_GUIDE_OVERRIDES`、ZZZ 4名は `ZZZ_CHARACTER_GUIDES`）。値は `batch15GuideFor` の戻り値（マージ済み完成形）から生成した。
  - B15-4: 凸20名を `CURATED` へ `game:sourceId` キーで移設。
  - B15-5: 推奨PT20名60案を `MANUALLY_CURATED_HIGH_USAGE_CATALOG` へリテラル登録し、`communitySources` だけ共通出典を付与した。
  - B15-6: メタデータ日付（20名を `2026-09-05` と第15バッチの出典へ）と履歴イベント（`BATCH_EVENT_TEMPLATES[15]`、サイトイベントを日付昇順の位置へ）を基底へ移設。
  - B15-7: `routers.ts` / `characterReference.ts` の上書き呼び出しと `server/batch15*.ts` 6ファイルを削除し、`scripts/golden-snapshot.mjs` の batch15 分岐も撤去した。
  - B15-0: 親リポの監査・精査スキル文書3件を正規化後の実態へ更新（コードは batch15 を名指し import していないため変更不要）。
- 実装中に確定した設計変更（いずれも `design-doc` で設計書へ反映済み）:
  - **D2 の訂正**: 起票時の「監査スクリプトが `batch15*` を import しているため親リポ先行が必須」は誤り。実測では `batchNNGuides.ts` の最大番号を動的検出する実装（検出 `[15, 17]`・採用 17）で、`batch15*.ts` を消しても壊れない。B15-0 を「撤去後の文書更新」へ変更し、順序制約を撤回した。
  - **D4 / V5 の追加**: 第15バッチ20名の凸 `rankLabel` が他228名と違っていた（英 `Eidolon`/`Constellation`、簡体字 `命座`/`意象影画`）。基底の正準ラベルへ揃うことを許可差分 V5 とした（日本語は不変）。
  - **D5 / V2 の撤回**: 推奨PTを `manualOptions` 経由で登録する案は不可。`members[0]` に対象の役割を付ける実装で、第15バッチは対象が先頭でない編成が60案中26案ある。役割付与を「本人かどうか」へ変える案は**既存カタログ側45名の応答も変える**ことを実測で確認したため却下し、リテラル登録＋`communitySources` 追加のみへ変更した。
- 反映コミット: `24df9e5`（Phase 開始・D2 訂正）/ `2806ba2`（B15-1）/ `a4dca27`（B15-2）/ `b3d1ca1`（B15-3）/ `4490624`（B15-4）/ `b0a9438`（B15-5）/ `509b901`（B15-6）/ `0e02765`（B15-7）。差分合計 22ファイル・+286 / −495 行。親リポ側は `ea0f80d`（B15-0）。main へ PR [#26](https://github.com/Sitar-sitar/hoyoverse-builder/pull/26) 経由でマージ（マージコミット `e241aa0`、CI `validate` pass 確認済み）。
- 検証結果:
  - 型検査 pass（`corepack pnpm check`）。各項目のコミット前に毎回実行。
  - 全回帰 pass（`corepack pnpm test`。36ファイル → 35ファイル172件。`batch15CuratedData.test.ts` の削除と、正規化に伴う既存期待値3件の更新を含む）
  - `build:api` pass / Pages 本番設定ビルド pass
  - ゴールデン比較 pass: 最終的な差分は `referenceCore`（20件）・`referenceConstellations`（20件）・`history` のみで、内容が V1・V4・V5 に限られることを専用の照合スクリプトで機械確認した。`referenceCore` の差分は全件 `communitySources` 0件→1件以上、`referenceConstellations` の差分は全件 `rankLabel` の英・簡体字のみ（日本語不変）、`history` はサイトイベント 9→8件・第15バッチのイベント重複 2→1件・日付昇順。
  - `check-r8` pass: `total 248 / catalog-id mismatch 0 / not curated 5 / name unresolved 0` を維持。
  - 三月なのかの分離 pass: `hsr:1224`（巡狩）は `profileId=crit` の自動生成ガイドと `preparing` の凸のままで、存護（`hsr:1001`）のデータを受け取らない。
  - ローカル API 実機確認 pass（port 3043）: 御空・ノエル・猫又が `dataStatus: "curated"`・`dataAsOf: "2026-09-05"`・`communitySources` 付き、`build.guideHistory` も正常。
  - 本番確認 pass（2026-09-10、マージ後）: Railway の自動デプロイ完了後（merge から約2分）に公開 API `build.reference` で御空・ノエル・猫又が `dataStatus: "curated"`・`dataAsOf: "2026-09-05"`・`communitySources` の `checkedAt: "2026-08-25"` を返し、`rankLabel` が正準ラベル（`Eidolons` / `Constellations`・`命之座` / `心象电影`）になっていることを確認。`build.guideHistory` の `siteEvents` は8件・第15バッチのイベント1件・日付昇順。Google Chrome で公開図鑑の御空に推奨PT3案（現行推奨／代替編成／所持対応）と星魂1〜6段が表示されることを確認した。
- 残課題・既知の問題:
  - 第15バッチ20名の推奨PTは、文言と `id`（`batch15-*`）体系が他200名（`curated-*`）と異なるまま残る（D5 の割り切り）。統一するなら、既存45名の役割ラベルの扱いと合わせて別設計で行う。
  - 20名の出典を訪問し直しての再精査は未実施（根拠メモに明記）。次回の個別精査バッチで実施する。
  - リファクタリング計画書 §4.3 の残り5件（メンバー名の表記ゆれ37種、`express.json` の 50MB 上限、`build.guideHistory` の応答サイズ、Pages の 500kB 超チャンク、監査スクリプトの移設）は未着手。
- 次フェーズへの申し送り:
  - **オーバーライド形式のバッチは今後作らない**。全17バッチが基底データ直接更新に揃った。新バッチは `characterBatches.ts` の `REVIEWED_BATCHES` へ1行足し、ガイド・凸・PT・メタデータ・履歴を各基底ファイルへ登録する。
  - 同名で複数実装があるキャラクター（三月なのか 1001/1224 など）のガイドは `GUIDE_OVERRIDES_BY_SOURCE_ID` へ登録する。名前キーの `GUIDE_OVERRIDES` は別実装にも当たるため使わない。
  - 図鑑の HSR ガイド解決は `catalogSourceIdFor()` 経由で identity を渡している。新キャラクター追加時は `server/fixtures/identityCatalogSnapshot.ts` の更新が引き続き必須。
  - 推奨PTの共通ヘルパー `manualOptions` は `members[0]` に対象の役割を付ける実装で、対象が先頭でない45名では役割が別メンバーへ付いている（既存の data 品質問題）。触ると広範囲に影響するため、直すなら独立した設計で行う。
- 関連ドキュメント: [修正設計書_第15バッチの基底データ正規化_2026-09-10.md](修正設計書_第15バッチの基底データ正規化_2026-09-10.md) / [batch-15-research-notes.md](batch-15-research-notes.md) / [リファクタリング計画書_2026-09-08.md](リファクタリング計画書_2026-09-08.md) §4.3 #3 / [設計書インデックス.md](設計書インデックス.md)（実装後メモは版番号を動かさないため作成しない）

### Phase 20: リファクタリング第1期（計画書 2026-09-08） — 完了

- 完了日: 2026-09-09
- 実装した内容:
  - [リファクタリング計画書_2026-09-08.md](リファクタリング計画書_2026-09-08.md)（[設計レビュー](設計レビュー_リファクタリング計画書_2026-09-09.md) DR-01〜DR-05 反映版）の R0〜R9 を実施した。挙動不変が原則で、唯一の挙動変更は R8。
  - R0: ゴールデン比較スクリプト `scripts/golden-snapshot.mjs` を新設（11セクション。`import` しても副作用がない `main()` 隔離、図鑑の凸と UID 照会経路の一致を検査する `check-r8` サブコマンド付き）。
  - R1: 参照元ゼロの Manus テンプレート残骸 14ファイル・3,924行を削除（`server/_core/{llm,imageGeneration,map,voiceTranscription,dataApi,heartbeat}.ts`、`server/storage.ts`、client の ComponentShowcase / DashboardLayout / DashboardLayoutSkeleton / AIChatBox / Map / ManusDialog、`catalog-role-report.mjs`）。
  - R2: ロール分類 Set 9個と HSR 運命ラベル表の重複を統合。ラベル表は `characterGuideCatalog.ts` を正本にし、分類は `individualGuides.ts` の `hsrProfileIdFor` / `genshinProfileIdFor` / `zzzProfileIdFor` へ集約（`expectedGuideProfiles.ts`・`enkaFallback.ts`・`partyRecommendations.ts` が参照）。
  - R3: 推奨PTのバッチ別ラッパー13本（83行）を `datedOptions(date, resetTargetChanges)` へ統合。200件の呼び出しと `manualOptions` 本体は不変。
  - R4: `UidResponseCache.set()` に閾値超過時の期限切れ掃除を追加し、掃除を観測するための `size` を公開（常駐プロセスでの無制限増加を防止）。
  - R5: `characterUpdateLedger()` / `characterReferenceCatalog()` / `guideUpdateHistory()` をメモ化し、図鑑1件照会の台帳全体線形探索を `ledgerEntryFor()` の Map 索引へ置換。
  - R6: 3ファイルに二重保持されていたバッチ名リストを `server/characterBatches.ts`（`REVIEWED_BATCHES` / `batchIdFor` / `batchNames`）へ一元化。台帳・更新履歴・メタデータ・`BATCH15_CHARACTERS` がここを参照する。
  - R7: 更新履歴のバッチ別イベント関数16本を `BATCH_EVENT_TEMPLATES` + `batchEventFor()` のテーブル1本へ集約（文言は既存リテラルを機械的に移設）。
  - R8（唯一の挙動変更）: 図鑑の `CATALOG_NAME_TO_SOURCE_ID`（手書き48件）を `ACTIVE_CATALOG_IDENTITIES` からの機械生成へ変更。図鑑（`build.reference`）の凸が UID 照会経路と同じ curated データを返すようになった。
- 反映コミット: `730a730`（レビュー反映）/ `3c8a2a4`（R0）/ `f71eca7`（R1）/ `2aa7cc9`（R2）/ `3bf9f5c`（R3）/ `088c7e7`（R4）/ `70b48b0`（R5）/ `4ad1141`（R6）/ `5750850`（R7）/ `ca7f509`（R8）/ `8d7ab7b`（締め記録）。差分合計 37ファイル・+1,635 / −4,505 行。main へ PR [#22](https://github.com/Sitar-sitar/hoyoverse-builder/pull/22) 経由でマージ（マージコミット `4644955`、CI `validate` pass 確認済み）。
- 検証結果:
  - 型検査 pass（`corepack pnpm check`、エラー0）。各項目のコミット前に毎回実行。
  - 全回帰 pass（`corepack pnpm test`。開始時 34ファイル183件 → 完了時 36ファイル。R2・R6・R8 でテストを追加）
  - `build:api` pass / Pages 本番設定ビルド pass（全項目で実行）
  - ゴールデン比較 pass: R1〜R7 は全11セクション一致。R8 のみ `referenceConstellations` と `catalogConstellations` の172件が差分で、`referenceCore`（図鑑応答の guide / partyRecommendations / status / batch）を含む他9セクションは一致。許可した2セクションだけを baseline へ反映した。
  - R8 受入判定 pass: `check-r8` が `total 248 / catalog-id mismatch 0 / not curated 5 / name unresolved 0`。変更対象集合が着手前に保存した172名と完全一致（`same set: true`）。変更後も準備中の5名は `genshin:アーロイ` / `genshin:コロンビーナ` / `zzz:シーシィア` / `zzz:ニコ` / `zzz:ピュロイス`（凸データ自体が未登録）。
  - R4 の追加テストは、掃除ブロックを外すと `expected 301 to be 1` で失敗することを実際に確認（確認後に復元）。
  - ローカル API 実機確認 pass（`node dist/index.js`、`API_ONLY=true`、port 3042）: `/api/health` が `{"ok":true,"service":"hoyoverse-builder-api"}`、`build.reference` の hsr:ホタル・zzz:星見雅 が `"dataStatus":"curated"`、zzz:ニコ が `"preparing"`、`build.guideHistory` が `"result"` を返す。
  - 本番確認 pass（2026-09-09、マージ後）: Pages デプロイ workflow success。Railway の自動デプロイ完了後（merge から約2分）に公開 API `build.reference` が hsr:ホタル・zzz:星見雅 で `"dataStatus":"curated"`、凸データ未登録の zzz:ニコ・genshin:コロンビーナ は `"preparing"`、`build.guideHistory` は `"result"` を返す。Google Chrome で `https://sitar-sitar.github.io/hoyoverse-builder/characters` のホタルに星魂1〜6が表示されることを確認（R8 前は「準備中」）。反映前は公開 API が旧データを返すため、直後の `preparing` は未デプロイを先に疑う。
- 残課題・既知の問題:
  - 計画書 §4.3 の別対応項目は未着手のまま（第15バッチの API 境界上書き正規化、推奨PTメンバー名の表記ゆれ37種、`express.json` の 50MB 上限、`build.guideHistory` の応答サイズ、Pages の 500kB 超チャンク、監査スクリプトの移設）。
  - 凸データ未登録の5名（上記）は R8 の対象外。データ登録は個別精査バッチの担当。
- 次フェーズへの申し送り:
  - バッチ名の追加は `server/characterBatches.ts` の `REVIEWED_BATCHES` へ1行足すだけでよい（台帳・履歴・メタデータ・第15バッチ定義が同じ表を参照する）。履歴文言は `BATCH_EVENT_TEMPLATES`、日付・出典は `characterGuideMetadata.ts` に足す。
  - データ更新時は `corepack pnpm exec tsx scripts/golden-snapshot.mjs snapshot` → 変更 → `compare` で「更新対象以外が変わっていない」ことを確認できる。図鑑の凸整合は `check-r8`。
  - 図鑑の凸は `ACTIVE_CATALOG_IDENTITIES` 由来の索引で解決するため、新キャラクター追加時は `server/fixtures/identityCatalogSnapshot.ts` の更新が必要。
  - §4.3 の残件は親リポ `my-app2/docs/修正設計書_hoyoverseスキル再設計_2026-09-08.md` 側で扱う。
- 関連ドキュメント: [リファクタリング計画書_2026-09-08.md](リファクタリング計画書_2026-09-08.md) / [設計レビュー_リファクタリング計画書_2026-09-09.md](設計レビュー_リファクタリング計画書_2026-09-09.md) / [設計書インデックス.md](設計書インデックス.md)（実装後メモは版番号を動かさないため作成しない）

### Phase 19: MiHoMo正規化経路の「現在のステータス」欄ラベル重複を修正 — 完了

- 完了日: 2026-09-08
- 実装した内容:
  - Phase 18の残課題（MiHoMo経路で同一ステータス名が絶対値・割合値の両方で重複表示される既存の表示上の粗さ）を調査・修正した。
  - `server/buildAdvisor.ts` の `parseCharacter` は MiHoMo `sr_info_parsed` の `properties[].name` をそのまま `allStats[].name` へ使っていた。StarRailRes `index_new/jp/properties.json`（MiHoMoが参照する本家データ）を直接確認したところ、`AttackAddedRatio`/`AttackDelta` はいずれも `name: "攻撃力"`、`HPAddedRatio`/`HPDelta` はいずれも `name: "HP"` で、`field` も `atk`/`hp` と同一、区別できるのは `percent` フラグのみだった（推測ではなく実データで確認）。このため割合ステータスと絶対値ステータスがUI上で同じラベルの行として重複表示されていた。
  - `isPercentStat()` ヘルパーと `allStatsFor()` を追加し、`properties` 配列内で `name` が重複するステータスに限り、`percent: true` 側へ `%` を付与してラベルを区別する（例:「攻撃力」/「攻撃力%」、「HP」/「HP%」）。重複のない単独の割合ステータス（会心率・会心ダメージ等）には不要な `%` を付与せず、既存表示を変えない。
  - 横展開確認：原神（`server/gameProviders.ts` の `GI_PROP_NAMES`）は `FIGHT_PROP_HP_PERCENT` 等に個別の `%` 付きラベルを持つ独自マップを使用しており重複なし。ZZZ（`finalZzzStats`）とHSR Enkaフォールバック経路（`enkaFallback.ts`）はいずれも絶対値・割合を合成した単一の最終値エントリしか出力しないため重複なし。本バグはMiHoMo正規化経路のみに存在した。
  - `server/buildAdvisor.test.ts` に実データ相当（`name` 重複あり）の回帰テストを追加。修正前は重複行、修正後は「HP」/「HP%」・「攻撃力」/「攻撃力%」に分離されること、単独の割合ステータス（会心率）には不要な `%` が付かないことを検証した。
- 反映コミット: `8f1f8d5`（実装・テスト） / `b1a31fa`・`b5fd5f7`（記録）。mainへ PR [#20](https://github.com/Sitar-sitar/hoyoverse-builder/pull/20) 経由でマージ（マージコミット `3e230ff`、CI `validate` pass確認済み）。
- 検証結果:
  - 型検査 pass（`tsc --noEmit`、エラー0）
  - 全回帰 pass（34ファイル / 183テスト。新規1テスト追加、既存182件も継続pass）
  - `npm run build:api` pass / Pages本番設定ビルド（`vite build --config vite.pages.config.ts`）pass
  - 実UID確認: 未実施。本バグはUIDに依存しないロジックバグ（MiHoMoの `properties[].name` を扱う純粋な整形処理）であり、StarRailRes実データ構造の直接確認とユニットテストで再現・修正を検証済みのため、実機再現は必須としなかった。次回MiHoMo経路が発火する実UID確認の機会があれば、あわせて表示を目視確認する。
- 残課題・既知の問題: なし（Phase 18の残課題であったMiHoMo経路の表示重複はこれで解消）。
- 次フェーズへの申し送り:
  - 次回、新規キャラクター追加・ガイド更新・不具合報告のいずれかを起点に着手する。
  - MiHoMo `sr_info_parsed` のレスポンス仕様が変わった場合は `server/buildAdvisor.ts` の `allStatsFor()`／`isPercentStat()` を見直す。
- 関連ドキュメント: なし（既存の設計書を更新する規模の変更ではないため新規設計書は作成していない）

### Phase 18: HSR Enkaフォールバック 戦闘外最終ステータス算出の修正 — 完了

- 完了日: 2026-09-08
- 実装した内容:
  - `server/enkaFallback.ts`を全面書き換え。遺物`_flat.props`の単純加算で最終値としていた不具合を修正し、キャラクター・光円錐のレベル別基礎値（`base + step × (level-1)`）、遺物メイン・サブ、解放済み軌跡（要求`promotion`/キャラクターレベルの正しい照合）、光円錐重畳ランクの常時有効プロパティを`StatKey`単位で分類・合成する方式へ改めた。`HP・攻撃力・防御力・速度 = 基礎値 × (1+割合) + 固定値`、会心率・会心ダメージ・EP回復効率は基準5%/50%/100%へ独立加算。
  - 静的データを`characters`/`light_cones`/`relic_sets`/`character_promotions`/`character_skill_trees`/`light_cone_promotions`/`light_cone_ranks`/`properties`の8種へ拡張し、全種非空をfail-closedで検証。取得は5秒上限・24時間キャッシュとし、失敗時は直近正常バンドル（最大7日）へ退避、両方失敗時は0補完せず空バンドルへ縮退する。
  - キャラクター単位で基礎値・光円錐基礎値・軌跡ノードのいずれかを解決できない場合は`statsStatus: "unavailable"`とし、`allStats`は空、`comparisons.current`はnull、強化提案は生成しない。不完全なEnka結果はUIDキャッシュへ保存しない。
  - 既存バグとして、UIDキャッシュへ`dataSource`が保存されずキャッシュヒット時に`ENKA FALLBACK`表示が失われる不具合も合わせて修正（`server/buildAdvisor.ts`）。
  - `client/src/pages/Home.tsx`・`LanguageContext.tsx`に`statsStatus`に応じた3言語の注記（算出済み／算出保留）と、比較保留時に「すべて目標水準に到達」と誤表示しない分岐を追加。
  - `server/buildAdvisor.test.ts`にStarRailRes実データ（`hsr:1502`のレベル別基礎値・軌跡要求Lv80ノード）を用いた回帰テストを追加し、修正前に失敗する差分（旧実装のレベル比較バグ）が修正後に合格することを確認した。
  - 【追加修正】公開後、ユーザー許可済みの公開UIDで実機確認したところ、Enkaフォールバック表示に「会心率 0」の重複行が出る回帰を発見。StarRailRes `properties.json` の `field: ""` 型（`MaxHP`/`CriticalChance`等、集計済み表示専用の予約名）が「その他」欄へ誤って表示されていたため、`classify()`へ`ignored`分類を追加して一般画面へ出さないようにした。実データ形をfixture化した回帰テストを追加。
- 反映コミット: `d7deb00`（実装・テスト） / `e295fcb`（本番実機で発見した表示重複の追加修正）。
- 検証結果:
  - 型検査 pass（`tsc --noEmit`、エラー0）
  - 全回帰 pass（34ファイル / 182テスト、追加修正後も同数）
  - `pnpm build:api` pass / Pages本番設定ビルド（`vite build --config vite.pages.config.ts`）pass（両コミットとも）
  - 実UID確認: **実施済み**（2026-09-08、ユーザー提供の公開UIDでHSR・原神・ZZZの3件を明示照会。UIDは記録しない）。
    - HSR: Enkaフォールバックが発火し、`ENKA FALLBACK`表示・キャラクター基礎値+光円錐+遺物+解放済み軌跡に基づくHP/攻撃力/防御力/速度/会心率/会心ダメ等の一覧表示・統一されたstatsNoteを確認。追加修正前は「会心率 0」の重複行を実機で検出し、追加修正後は型検査・テスト・ビルド全てpassで再デプロイ済み（MiHoMoが安定して主取得に成功する状態が続き、追加修正後のEnka経路の再度の実機再現は未実施）。
    - 原神: 12名の公開キャラクターを正常表示、コンソールエラー0件（本フィックスの対象外パスであり無変更）。
    - ZZZ: 同UIDで「データサービスに接続できません」を確認（`LOOKUP UNAVAILABLE`、TRPCClientError）。ZZZ取得経路は本パッチで一切変更していないため、本パッチによる回帰ではない。第三者データサービス側の一時的な問題かUIDの公開設定起因の可能性があり、原因切り分けは別タスクとする。
  - Railway `/api/health`: `{"ok":true,"service":"hoyoverse-builder-api"}` を確認。Pages配信（`https://sitar-sitar.github.io/hoyoverse-builder/`）はHTTP 200。
- 残課題・既知の問題:
  - ~~ZZZの対象UIDで実機からデータ取得できない事象が残る（本パッチの回帰ではないが未解決）。次回、別タスクとして原因調査する。~~ → 2026-09-08 追加調査でクローズ。`server/gameProviders.ts`のZZZ取得経路（エンドポイント・タイムアウト・エラーハンドリング）を確認したがコード側の欠陥はなく、`https://enka.network/api/zzz/uid/{uid}/`へ直接照会したところ実在UIDで`HTTP 424 Failed Dependency`（本文はクライアント表示文言と一致）を確認、外部データサービス（enka.network）側の問題と特定した。ユーザーが翌日（2026-09-09）のZZZアップデート関連の一時的事象と判断し、対応不要でクローズとした。
  - 遺物セットの無条件プロパティがキャラクター詳細画面へ反映される範囲は未検証（設計書§12の非ブロッキング事項）。実測fixtureでの採否確定は次回対応。
  - 光円錐の`equipment._flat.props`に`BaseHP`等が実際に含まれるかは、追加修正後のEnka経路実機再検証で確認できていない（フォールバック式は`light_cone_promotions`からの算出を常に持つため機能には影響しない）。
  - MiHoMo経路（本パッチのスコープ外）で、同一ステータス名（例:「攻撃力」）が絶対値・割合値の両方で重複表示される既存の表示上の粗さを実機で確認した。本パッチでは未着手・未修正。
- 次フェーズへの申し送り:
  - 実UIDでの本番同時点比較（HoYoLAB表示との数値照合）は、追加修正後のEnka経路実機再現ができなかったため未完了。次回Enkaフォールバックが発火するタイミングで実施する。
  - `docs/guide_sources.md`は本フェーズで算出方法の節を追記済み。今後Enka側のレスポンス仕様が変わった場合はここを更新する。
  - ZZZデータ取得不能の事象とMiHoMoの重複表示は、いずれも本パッチのスコープ外の既知事象として次回別タスクで扱う。
- 関連ドキュメント: [修正設計書_HSR_Enkaフォールバック最終ステータス算出_2026-09-08.md](修正設計書_HSR_Enkaフォールバック最終ステータス算出_2026-09-08.md) / [guide_sources.md](guide_sources.md) / [設計書インデックス.md](設計書インデックス.md)

### Phase 17: 第17パッチ 残り28体のキャラクター個別精査＋空目標表示修正 — 完了

- 完了日: 2026-09-08
- 実装した内容:
  - 第16パッチ時点で未精査だった原神28体を一括更新し、個別ビルド、実名4名PT各2案、通常27体と旅人7元素の全6段階命ノ星座を登録。34プロフィール・43 source ID・204効果をID優先で解決する。
  - 固定の戦闘外到達値が主出典にない場合は数値を推測せず `targets: []` とし、武器・PT・命ノ星座・戦闘中バフを公開プロフィール値へ加算しない。武器と代替はスキーマを拡張せず研究ノートへ保存した。
  - UID照会の優先強化欄を「未達あり」「比較あり・未達なし」「比較対象なし」の3状態へ分離。比較対象0件では「固定目標値が未登録」と3言語で表示し、「すべて目標水準に到達」と誤表示しない回帰テストを追加した。
  - 更新台帳を248/248（再調査待ち0）へ更新し、第18候補を空にした。図鑑のUID不要経路と更新履歴にも第17パッチを反映した。
- 反映コミット: `7b138dd`。mainへPR [#15](https://github.com/Sitar-sitar/hoyoverse-builder/pull/15) 経由でマージ（マージコミット `a003a3a`）。
- 検証結果:
  - 型検査 pass（`tsc --noEmit`、エラー0）
  - 全回帰 pass（34ファイル / 181テスト）
  - APIバンドル / Pages本番設定ビルド pass
  - PR CI `Validate HoYoverse external API` run `34169217730` pass
  - Pagesデプロイ run `34169275133`（`a003a3a`）success。公開indexと配信アセットはいずれもHTTP 200で、新しい固定目標未登録文言を含む。
  - 公開API（Railway）: Origin付き `/api/health` が200・CORS許可、更新台帳が248/248・pending 0・第18候補0、第17サイトイベントを返す。`build.reference`（夢見月瑞希）は `batch: 17`、`targets: []`、PT 2案、命ノ星座6段、`curated` を返す。
  - 公開画面（Chrome）: `/characters` で `REVIEWED 248 / 248`、夢見月瑞希のB17ビルド・固定目標なし表示・PT 2案・命ノ星座6段を確認。`/updates` で248/248・再調査待ち0・第17イベントを確認。コンソールエラー0件。
  - 実UID照会は、今回新しい公開UIDが提供されていないため未実施。UID不要の図鑑、API最終応答、状態分岐の画面単体テストで対象範囲を確認した。
- 残課題・既知の問題:
  - genshin.devで英語データを取得できない夢見月瑞希・藍硯・炎旅人・氷旅人、および全プロフィールの中国語は日本語主出典からの翻訳。公式訳を確認できた場合は差し替える。
  - リポジトリ内に `docs/設計書インデックス.md` は存在しないため、Phase 17で新規設計書や索引更新は行っていない。
- 次フェーズへの申し送り:
  - 初回個別精査は全248体で完了。次回はゲーム側の新規キャラクター追加、ガイド更新、公式ローカライズ確定のいずれかを差分起点にする。
  - `targets: []` は未達・達成を判定できない状態であり、今後も固定値の根拠なしに数値を補完しない。
  - `main` は直接push不可。引き続きPR、CI、PagesのマージSHA一致、Railway API、公開画面を別々に検証する。
- 関連ドキュメント: [batch-17-research-notes.md](batch-17-research-notes.md) / [all-character-update-ledger.md](all-character-update-ledger.md)

### Phase 16: 第16バッチ キャラクター個別精査（HSR 4名・原神 16名） — 完了

- 完了日: 2026-09-07
- 実装した内容:
  - 台帳の未精査20名（HSR: 不死途・乱破・椒丘・爻光／原神: バーバラ・ファルザン・フレミネ・ミカ・モナ・ヨォーヨ・ラウマ・リサ・
    リネ・リネット・レイラ・マーヴィカ・ムアラニ・フリンズ・ヤフォダ・リオセスリ）の個別ビルド・全6段階の凸・実名推奨PTを、
    更新日付きの公開ガイド（Game8日本語版が主出典、GameWith・崩壊スターレイルWikiが補助）から精査して登録。
  - 第15バッチのAPI境界上書きではなく基底ファイル（`buildAdvisor.ts` / `gameProviders.ts` / `characterConstellations.ts` /
    `partyRecommendations.ts` / `characterGuideMetadata.ts` / `guideUpdateHistory.ts`）を直接更新。
  - 図鑑（UID不要）の既存欠落を2件修正。①カタログ名を source ID として扱っていたため ID キーの凸へ到達しなかった →
    `constellationProfileForCatalogName()` を追加。②原神・ZZZのガイドが `generated*` へ直接フォールバックし、provider 経路にだけ
    登録された個別ビルドが図鑑へ届かなかった → `genshinGuide()` / `zzzGuide()` へ統一。第16バッチ以外にも影響していた。
  - 台帳を 200/248 → 220/248（残28、原神のみ）へ更新し、`NEXT_BATCH` を第17バッチ（原神20名）へ差し替え。
- 反映コミット: `bcd7205`（データ本体） / `b0e3d7c`（図鑑経路の修正） / `3051bf1`・`9ee37a2`（記録）。
  main へは PR [#12](https://github.com/Sitar-sitar/hoyoverse-builder/pull/12) 経由でマージ（マージコミット `ceb9393`）。
- 検証結果:
  - 型検査 pass（`tsc --noEmit`、エラー0）
  - テスト pass（33ファイル / 174テスト。新規 `server/batch16CuratedGuides.test.ts` 7テスト）
  - `pnpm build:api` pass / Pages ビルド（`vite build --config vite.pages.config.ts`）pass
  - PR CI `Validate HoYoverse external API` pass
  - Pages デプロイ run `34133146320`（`ceb9393`）success
  - 公開API（Railway）: `/api/health` ok、`/api/trpc/build.reference`（不死途）が `batch: 16` と精査済みガイドを返すことを確認。
    マージ直後は旧データを返し、Railwayの自動デプロイ完了後に切り替わった。
  - 公開画面（Chrome）: 図鑑で不死途・マーヴィカの精査済みビルド表示、`/updates` で「220 / 248 精査完了」「再調査待ち28」
    「第16バッチ20名の個別情報を更新」を確認。コンソールエラーなし。
  - `verify-live-site.ps1`: `IndexHasNoRootAbsoluteAssets` のみ fail。プロジェクトサイト（`base: "/hoyoverse-builder/"`）の
    仕様どおりの絶対パス参照で、参照アセットはすべて到達可能（既存仕様であり本フェーズの回帰ではない）。
  - 実UIDでの照会画面確認: **実施済み**（2026-09-07、ユーザーが提供した公開UIDでHSR・原神・ZZZの3件を明示照会。UIDは記録しない）。
    - HSR: ショーケースに第16バッチ対象は爻光のみ。`hsr:1502`、天命に従う旅の卜者 ×4 / 千の星が集う街 ×2、速度160・会心率62%・
      会心ダメ145%の比較表、星魂6段（未解放含む）、PLAN1先頭が本人。図鑑と推奨セット・目標値・PT構成が一致。
    - 原神: 対象はムアラニ・バーバラの2名。それぞれ `genshin:10000102` / `genshin:10000014` で個別セットと命ノ星座6段を表示。
      図鑑と一致。残る対象は当該UIDのショーケース外のため未表示（失敗ではない）。
    - ZZZ: 第16バッチ対象なし。既存データ（レミエール等）の表示崩れがないことのみ確認。
    - 3ゲームともコンソールエラー0件、取得不能なし。再読み込みでは保存UIDが復元されるだけで自動照会されないことを確認。
- 残課題・既知の問題:
  - **未育成キャラで「すべて目標水準に到達しています」と表示される**（実UID確認でムアラニ Lv.1・C0 にて確認）。
    出典に数値の明示がないキャラは設計どおり `targets: []` のため比較対象が0件になるが、`client/src/pages/Home.tsx` は
    未達0件を一律 `allTargetsMet` として扱うため、「目標が未登録」と「全項目達成」が同じ文言になる。第16バッチ固有ではなく
    `targets: []` の既存キャラ全体に該当。**ユーザー判断により第17バッチでまとめて対応する**（比較0件時だけ別文言へ分岐、
    対象は Home.tsx・図鑑側の同等表示・`LanguageContext.tsx` の3言語・回帰テスト）。
  - 凸の英語名・中国語名は公式訳を確認できず `Constellation N` / `Eidolon N` の汎用ラベルを使用（説明文は訳出済み）。
  - 第15バッチは根拠メモがなく、API境界上書きのまま残っている。
- 次フェーズへの申し送り:
  - 第17バッチ候補は原神20名（レザー、ロサリア、雲菫、煙緋、嘉明、甘雨、閑雲、凝光、九条裟羅、荒瀧一斗、刻晴、珊瑚宮心海、
    鹿野院平蔵、七七、重雲、申鶴、神里綾華、神里綾人、辛炎、千織）。残8名は第18バッチで母集団を一巡する。
  - **第17バッチの作業に「比較対象0件のときの文言分岐」を含める**（上記の残課題。データ精査とは別の小さなUI修正）。
  - `main` は branch ruleset により直接 push 不可。公開は PR 経由（`gh pr create` → CI → `gh pr merge`）。
  - キャラクターデータは Pages ではなく Railway API が返す。Pages run の成功だけでは反映の証拠にならない。
  - 新しいデータは基底ファイルへ登録すれば UID照会・図鑑の両方へ届く。実画面確認は必ず図鑑側でも行う
    （名前一致でテストが通り、実画面で初めて欠落が露見した）。
- 関連ドキュメント: [batch-16-research-notes.md](batch-16-research-notes.md) / [all-character-update-ledger.md](all-character-update-ledger.md)
