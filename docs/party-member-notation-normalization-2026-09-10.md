# 推奨PTメンバー名の表記統一（2026-09-10）

親リポ `my-app2/docs/修正設計書_hoyoverseスキル再設計_2026-09-08.md` §4.3 タスク① の実施記録。
**表示文言の変更**にあたるため、旧表記・新表記・根拠をここへ残す。

- 対象: `server/partyRecommendations.ts` の推奨PTメンバー名（`members[].name.ja`）
- 前提コミット: `84e6053`
- 完了条件: `server/partyMemberAliases.ts` の `PARTY_MEMBER_ALIASES` が空、`partyMemberAliases.test.ts` が green

## 何を変えたか

推奨PTのメンバー名は自由文字列で、同じキャラクターに複数の表記が混在していた。
カタログ名（`server/characterGuideCatalog.ts`）へ統一し、監査の別名表を空にした。

| 旧表記 | 新表記 | 件数 | 根拠 |
|---|---|---:|---|
| `トリガー` | `「トリガー」` | 12 | カタログ名は鉤括弧付き |
| `フーフー` | `橘福福` | 3 | 橘福福（チー・フーフー）の愛称 |
| `パン` | `潘引壺` | 3 | 姓「潘」の略称 |
| `パン・インフー` | `潘引壺` | 1 | 潘引壺の読み |
| `ジェーン・ドゥ` | `ジェーン` | 1 | フルネーム表記 |
| `パイパー・ウィール` | `パイパー` | 1 | 同上 |
| `グレース・ハワード` | `グレース` | 1 | 同上 |
| `オルペウス` | `オルペウス&「鬼火」` | 2 | カタログ名は相棒を含む |
| `スターライト・ビリー` | `スターライト･ビリー` | 1 | 中黒を半角へ（カタログ表記に一致。NFKC 正規化では同一） |
| `飲月` | `丹恒・飲月` | 1 | 略称 |
| `ルオチャ` | `羅刹` | 1 | Luocha の音写 |
| `永夜` | `長夜月` | 1 | ユーザー確認で同一と確定（2026-09-10） |
| `銀狼LV.999` | `銀狼Lv.999` | 1 | 大文字/小文字の揺れ。リポジトリ内の正規表記（`characterIdentity.ts` の hsr:1506）へ合わせた |
| `主人公（草）` / `主人公(草)` / `草主人公` | `旅人（草）` | 各1 | 3表記を1つへ統一。**元素情報を残すためカタログ名（旅人）へは寄せない** |

合計16表記・31箇所を書き換えた。

## 判断の根拠

- ZZZ の愛称・読み（`フーフー`＝橘福福、`パン・インフー`＝潘引壺）は公開情報で確認した（2026-09-10）。
- `hsr:永夜` と `zzz:パン` は公開メタデータ281件に該当が無く、ユーザー確認のうえ既存キャラクターの別表記として確定した。
- `旅人（草）` はカタログに無い表記だが、元素別実装を区別する情報を捨てないため残し、`NON_CATALOG_PARTY_MEMBERS` へ「正当な派生」として登録した（HSR の `開拓者（記憶）` と同じ扱い）。

## 変わらないもの

- 案の数・順序・`id`・`rank`・出典（`sourceUrl` / `dataAsOf` / `communitySources`）・役割ラベル（`role`）・`targetChanges`・`synergy`
- メンバーの**並び順**（名前だけを置換した）
- ガイド・凸・メタデータ・更新履歴・台帳

ゴールデン比較で、差分が出た21名すべてについて「変わったのは `members[].name` だけ」であることを機械照合した。

## 影響を受けたキャラクター（21名）

HSR: キャストリス / サンデー / セイレンス / 火花
原神: カーヴェ / ティナリ / リサ
ZZZ: 「11号」 / 「シード」 / 0号・アンビー / アストラ / イドリー / オルペウス&「鬼火」 / ノルムー / ビリー / プルクラ / リュシア / レミエール / 儀玄 / 狛野真斗 / 葉瞬光

## 残課題

- `UPCOMING_PARTY_MEMBERS` の11表記（未実装・未登録）は今回の対象外。実装が確認できた時点で `hoyoverse-character-update` のモード C（推奨PT影響レビュー込み）で扱う。
- `hsr:モーテナックス・ブレード` と `hsr:モルトナックス・ブレード` が同一キャラクターかは未確認のため、両方を実装待ちとして残している。
